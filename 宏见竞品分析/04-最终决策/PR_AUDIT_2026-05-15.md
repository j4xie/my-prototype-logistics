# PR Audit — 2026-05-15

> 审计人: PR 审计员 (Claude Opus 4.7)
> 审计时点: 2026-05-15
> 范围: 6 Track 11 个开放 PR (#649 #650 #651 #653 #654 #656 #657 #658 #659 #660 #661)

---

## §1 总览表

| PR# | Title (缩) | Verdict | 关键发现 | Merge order |
|---|---|---|---|---|
| **#651** | [A] C-CANVAS-AI AILayoutAssistant 接真 LLM | 🟡 小改后 merge | 修了 4 处历史 dead-code,12 单测 PASS; e2e-pr-gate fail 是 infra (postgres `role "root"` 问题, 跟代码无关); Vercel 是 rate-limit | **3** |
| **#654** | [A] C-CANVAS-PAGE PageEditor 挂导航 | 🟢 直接 merge | 92 行纯 wiring,types/screen/navigator/menu,所有 CI 全绿; 0 `as any`,无 breaking | **1** (无依赖) |
| **#657** | [A] C-CANVAS-REPO Canvas Tool Repository 统一 | 🟢 直接 merge | 9 单测 PASS,FactoryHomeLayoutToolStore @Transactional; Flyway V20260514_06 idempotent backfill; mergeable=MERGEABLE | **4** (依 #651) |
| **#649** | [B2] W-ABA-1 抄码品识别 | 🟠 deploy-test 后 merge | 12 文件 +1046 行; **❗MaterialAbacaController 缺 @RequirePermission**; 自己 parse `Authorization` header (绕开 SecurityUtils); F006 seed 用 LIKE 防御; Flyway V20260516_02 与 #658 #659 同号但内容不同 (PR #658 V20260516_01) **无冲突** | **5** (需先 fix RBAC) |
| **#653** | [B2] PDF 扫码 RN 端 | 🟠 deploy-test 后 merge | **Stacked 在 #649 base 之上**; expo-camera + WHReceiptCreate; 沿用 PR #649 abaca API; iOS NSCameraUsageDescription 需 organizer 确认 plist; 真扫码需 device-level smoke (CI 无法验) | **6** (依 #649) |
| **#658** | [C] C-ATT-1 通用 Attachment 系统 | 🔴 NOT merge as-is | **❗AttachmentController 8 endpoint 全部缺 @RequirePermission/@PreAuthorize** (除 SecurityUtils.getCurrentUserId 自查); 23 文件 2668 行; 26 单测 PASS; OSS 未 deploy 验证; Brief 偏离 6 处 (路径/枚举/接口签名/接入数量) | **延后** |
| **#659** | [C] C-PRT-1 单据打印 PDF | 🔴 NOT merge as-is | **python-lint-test FAIL** (4 flake8: 1 unused import + 3 行 >120 chars in pdf_renderer.py); **PrintController 5 endpoint 全部缺 @RequirePermission**; Flyway V20260516_02 与 PR #649 同号(同分支会冲突); reportlab 偏离 brief 指定 weasyprint | **延后** |
| **#660** | [C] 三价对比 bug 修复 | 🟢 直接 merge | 4 文件 +172/-5,5 单测 PASS; DTO 加 nullable dataSourceHint,前端 banner+tooltip + 移除 stale priceLoaded 短路; 全部 CI 绿(除 Vercel rate-limit + e2e infra noise) | **2** |
| **#661** | [C] C-RBAC-1 仓管隔离审计 | 🟡 小改后 merge | 静态注解 4 单测 PASS,RBACAuditTool + 5x5 bash regression script; 不修任何业务代码,纯审计; 但 5x5 regression 实跑结果仍未提供 (需 deploy-test 跑) | **7** (depends on #649 fix) |
| **#656** | [D1] M-BOM-1 + 2 bugs | 🟠 deploy-test 后 merge | 26 文件 +3895 行,3 项目 bundled; **mvn compile PASS,2 Flyway (V20260516_02 与 #649 同号冲突)**; 24 处 @PriceSensitive 标注; 5 AI Tool; brief 偏离 3 处主动 disclosed; UnitConversionService 提取干净; dry-run 35 active items → 31 migrated | **8** (大量 schema 变动,先单独 deploy-test) |
| **#650** | [D2] M-WP-1/M-WP-2 工序管理 | 🟠 deploy-test 后 merge | 23 文件 +3380 行; **Flyway V20260516_03 与 #656 同号冲突**; WorkProcessTaskController 有完整 @RequirePermission + @PreAuthorize; 状态机 assertTransition + BusinessException; 5 AI Tool; **未做 unit test** (per brief Day 4); spawn 接 ProductionBatch 留 follow-up | **9** (需重排 Flyway 序号) |

**总结**:
- 🟢 直接 merge: **3** (#654, #660, #657)
- 🟡 小改后 merge: **2** (#651, #661)
- 🟠 deploy-test 后 merge: **4** (#649, #653, #656, #650)
- 🔴 NOT merge: **2** (#658, #659)

---

## §2 逐 PR 详细

### PR #651 — [Track-A] C-CANVAS-AI AILayoutAssistant 接真 LLM 🟡

**统计**: 5 files / +874 -201 / `feature/asap-track-a-canvas-ai`

#### 1. Scope
合规 — 仅改 `dto/decoration/` + `service/decoration/impl/` + 新增 test。无跨 track 文件污染。

#### 2. 测试
**12/12 PASS** (`DecorationServiceImplAITest`):
- happy path / module normalization / markdown fence stripping
- 5 fallback 分支 (LLM throw / garbage / missing modules / illegal-only / oversized)
- request context propagation / system prompt assertions / persistence

#### 3. CI 状态
- `java-build-test` ✅, `python-lint-test` ✅, `rn-test` ✅, `vue-build-check` ✅
- `e2e-pr-gate` ❌ — **不是代码问题**: postgres docker 报 `role "root" does not exist` 是 setup-actions infra 问题,所有 PR 都失败
- `Vercel – backend/xiaochengxu` ❌ — Vercel rate-limit (24h 后自恢复)

#### 4. 代码 quality
- ✅ Cretas 规范: `snake_case` 表名 (table 未变),camelCase Java 字段,JSON camelCase
- ✅ 错误处理: LLM 失败 → 走 rule-based-fallback (有 try/catch 但不吃 exception,WARN log)
- ✅ 5-type whitelist + `LayoutValidator` 防御; 任何畸形 LLM 输出都安全 fallback
- ✅ 关键 magic number 有注释 (5-type / 2-col Bento / `qwen-flash` 模型名)

#### 5. 安全性
- ✅ 无 SQL 注入 (无 raw SQL, 走 Repository)
- ✅ 无价格字段 (布局生成不涉及价格)
- ✅ JWT/Auth: 上游路由已守, 此 Service 层不重复
- ✅ 输入验证: `LayoutValidator` 检查每个 module + 网格冲突
- ⚠️ **LLM cost 风险**: 每次 AILayoutAssistant 触发都打 Python `/api/llm/chat`,需观察 token 用量,建议加速率限制 (但不阻塞 merge)

#### Verdict 🟡 **小改后 merge**
- **修正点**: 无 functional 改动需求,可直接 merge
- **建议 follow-up** (不阻塞):
  - `modelUsed` 硬编码 `qwen-flash`,但实际由 PythonRouter 决定 — 后续 thread real provider name back
- **依赖**: 无 (#657 依赖本 PR 的 ModuleConfig 字段定义)

---

### PR #654 — [Track-A] C-CANVAS-PAGE PageEditor 挂导航 🟢

**统计**: 4 files / +92 -0 / `feature/asap-track-a-canvas-page`

#### 1. Scope
完全符合 — 全 RN frontend,4 文件: types/screen wrapper/navigator/menu。0 后端改动。

#### 2. 测试
- typecheck baseline 47175 vs main 47233 = **-58 errors** (减少,不引入新错误)
- 无新 unit test (wiring 性质,组件本身有 propTypes 校验)

#### 3. CI 状态
**全绿** — java-build-test ✅, python-lint-test ✅, rn-test ✅, vue-build-check ✅ (e2e/Vercel 跟代码无关)

#### 4. 代码 quality
- ✅ `useRoute<RouteProp<...>>` 严格类型 (per `.claude/rules/typescript-type-safety.md`)
- ✅ 无 `as any` 引入
- ✅ 5 pageType 完整覆盖 string-literal union
- ✅ `i18n` 用 fallback "页面编辑器" 中文兜底 (TODO 加翻译 key,不阻塞)

#### 5. 安全性
- 纯导航 wiring,无外部输入处理,不涉及安全

#### Verdict 🟢 **直接 merge** — 最低风险 PR,可作为 Sprint 首 merge

---

### PR #657 — [Track-A] C-CANVAS-REPO Canvas Tool Repository 统一 🟢

**统计**: 6 files / +483 -110 / `feature/asap-track-a-canvas-repo` / **mergeable=MERGEABLE**

#### 1. Scope
合规 — 3 Tool 改实现 + 1 NEW store helper + 1 Flyway migration + 1 test 文件。

#### 2. 测试
**9/9 PASS** (`FactoryHomeLayoutToolStoreTest`):
- flatToNested / nestedToFlat 双向
- 旧 colSpan/rowSpan fallback
- defaults on missing/blank/malformed JSON
- save 保留 status/version (update path)

#### 3. CI 状态
- java-build-test ✅, python-lint-test ✅, rn-test ✅, vue-build-check ✅
- `factoryId Isolation Audit` ✅ — 新增 Tool 全部 factory-scoped
- e2e-pr-gate ❌ (同 infra 噪音)

#### 4. 代码 quality
- ✅ `@Transactional` 在 store helper
- ✅ Flyway migration **idempotent** — `WHERE NOT EXISTS` 防 overwrite,**不 drop** legacy 数据 (rollback safety)
- ✅ Bean 名称无冲突 (FactoryHomeLayoutToolStore 唯一)
- ✅ Schema bridge 注释清晰 (Tool 端 flat / FactoryHomeLayout nested)

#### 5. 安全性
- ✅ factoryId 强制 (`saveFlatLayout(factoryId, ...)` 接口设计强制传)
- ✅ 无价格相关
- ⚠️ Flyway version 用 `V20260514_06` — 跟其他 PR 同周期 migration 不冲突 (V20260514_05 是已有的)

#### Verdict 🟢 **直接 merge**
- **依赖关系**: 此 PR 引用 PR #651 的 `ModuleConfig.gridPosition` 字段。Brief 已设定二者协同; 但 PR #657 编译 PASS 表明字段定义已在 main 或 #651 已被引入 — 建议 merge 顺序: **#651 → #657**

---

### PR #649 — [Track-B2] W-ABA-1 抄码品识别 🟠

**统计**: 12 files / +1109 -6 / `feature/asap-track-b2-w-aba-1`

#### 1. Scope
合规 — backend abaca entity / repo / service / controller / 3 AI Tool / Flyway / RN 2 屏。

#### 2. 测试
- ❌ **没有 unit test** (PR body 全是 manual test plan)
- 后端 RBAC 无 test 覆盖

#### 3. CI 状态
- java-build-test ✅, factoryId Isolation Audit ✅, python-lint-test ✅, rn-test ✅, vue-build-check ✅
- e2e-pr-gate ❌ (infra noise)

#### 4. 代码 quality
- ✅ Entity 字段命名规范 (snake_case 列, camelCase Java)
- ✅ `@Transient` 没误用
- ✅ Flyway V20260516_02 防御式 seed `LIKE '%牛肉%'` 不存在不报错
- ✅ 双签 verify 拒绝 weighedBy === verifierUserId
- ✅ 软删拒绝已 verified 行 (历史不可篡改)
- ⚠️ `AbacaWeightLogTool.weighedBy` 取 `getUserId(context)`, 缺 userId 抛 IllegalArgumentException (per AI Tool 规范, OK)

#### 5. 安全性 — **核心问题**
- 🔴 **`MaterialAbacaController` 8 endpoint 全部缺 `@RequirePermission`** (file:line `controller/material/MaterialAbacaController.java` class-level / 各 endpoint)
- 🔴 **自定义 `currentUserId(authorization)` parse JWT** — 绕开标准 SecurityUtils.getCurrentUserId() (在 controller 内自己 decode `Authorization` header)
  - 风险: 跟 PR #423 的 PriceFieldResponseAdvice 走的 RBAC 链不通,新增的 endpoint 不受统一权限拦截
  - 任何 authenticated user 都可以调 create / verify / softDelete (有 factoryId path,但没角色 gate)

#### Verdict 🟠 **deploy-test 后 merge** (含必修)
- **强制修正前必做**:
  1. 给 6 endpoint 加 `@RequirePermission` (建议: list/get → `procurement:read`; create/batch/verify/delete → `procurement:read_write`)
  2. 删 `currentUserId(authorization)` 改用 `SecurityUtils.getCurrentUserId()` (PR #423 标准)
  3. 加 unit test 覆盖 verify 双签拒绝 + softDelete 已 verified 拒绝逻辑
- **deploy-test 验证**:
  - F006 seed 是否成功命中牛肉/猪肉/鸭肉
  - V20260516_02 migration 在 test env 干净 apply
  - RBAC: 用 warehouse_mgr token 调 verify endpoint 应返 403
- **依赖**: #653 stacked 在本 PR 上,本 PR 修完 #653 自动 rebase OK

---

### PR #653 — [Track-B2] PDF 扫码 RN 端 🟠

**统计**: 11 files / +873 -225 / `feature/asap-track-b2-pdf-scan` / **stacked on #649**

#### 1. Scope
合规 — backend +1 endpoint (`getOrderByNumber`),RN 扫码 + 收货页 + abaca client。

#### 2. 测试
- ❌ 无 unit test (real-camera scan / RN 闭环都靠 manual test)

#### 3. CI 状态
- e2e-pr-gate ❌ (infra noise) + Vercel rate-limit
- 其他 build 检查需重跑

#### 4. 代码 quality
- ✅ `getOrderByNumber` endpoint **有 @RequirePermission** (`procurement:read_write`, `procurement:read`)
- ✅ `findByFactoryIdAndOrderNumber` 走 repo (无 raw SQL,无注入)
- ✅ expo-camera `barcodeScannerSettings={['qr','code128']}`,2s scan cooldown 合理
- ✅ Network error 友好 alert (无吃 exception)
- ⚠️ iOS plist `NSCameraUsageDescription` 项目里 LabelScanScreen 已用过 expo-camera (Brief 自述),应已 OK,但需 organizer 确认

#### 5. 安全性
- ✅ `getOrderByNumber` 有 RBAC
- ⚠️ **依赖 PR #649 abaca API** — 若 #649 RBAC 修复前 merge,这里调用 abaca create 也会绕过 RBAC

#### Verdict 🟠 **deploy-test 后 merge**
- **必须先 #649 修复 RBAC 再 merge 本 PR**
- **deploy-test 验证**:
  - 真 device 扫 PDF QR (含 PR #413 PO PDF 二维码)
  - 全闭环: 扫 → orderNumber → getOrderByNumber → WHReceiptCreate → confirmReceive → abaca-log
  - 错误路径: 不存在订单号 → 友好提示
- **依赖**: stacked on #649 → 必须 #649 先 merge / 或本 PR rebase main 一起 squash

---

### PR #658 — [Track-C] C-ATT-1 通用 Attachment 系统 🔴

**统计**: 23 files / +2668 -0 / `feature/asap-track-c-att-1` / **mergeable=MERGEABLE**

#### 1. Scope
合规 — backend attachment 全栈 + RN 通用组件 + web-admin 通用组件 + 4 接入点。

#### 2. 测试
**26/26 PASS** (`AttachmentServiceImplTest` 17 + `AttachmentControllerTest` 9):
- Register / Query / SoftDelete / Update / StripQuery 全覆盖
- Factory 隔离 / dedup / 401 unauth / 403 non-owner 覆盖

#### 3. CI 状态
- java-build-test ✅, python-lint-test ✅, rn-test ✅, vue-build-check ✅
- e2e-pr-gate ❌ (infra)
- Vercel backend ✅ (rate-limit 例外)

#### 4. 代码 quality
- ✅ SHA256 dedup
- ✅ `@Async` 缩略图,失败不阻塞 register
- ✅ Flyway DDL 用 pglast PARSE OK 验证 (real PG parser)
- ✅ 18 EntityType 完整 + CHECK constraint
- ✅ `@Where(clause = "deleted_at IS NULL")` 软删支持
- ⚠️ Brief 偏离 6 处 (Flyway 路径 / Entity 时机 / enums 内嵌 / uploadUrl 签名 / 接入数量 / 测试方法) — 大部分合理,但应在 review 时正式批准

#### 5. 安全性 — **致命问题**
- 🔴 **`AttachmentController` 8 endpoint 全部缺 `@RequirePermission` / `@PreAuthorize`**
  - 仅靠 `SecurityUtils.getCurrentUserId()` 拿 userId,但 **不验证用户角色 / 模块权限**
  - 任何 authenticated user 都可以 list / get / download / upload-url / register / update / softDelete 任意 factory 的附件
  - 内部代码确实有 owner-check (softDelete/update 仅 uploader),但**这是业务逻辑层**,不是 RBAC 守卫
  - `list` / `download` 完全无守卫: warehouse_manager 可以 download 别人的 PAYMENT_VOUCHER 文件
- 🔴 **OSS pre-signed URL 生成端 缺访问限制** — 任何 user 都可调 `/upload-url` 生成有效的 OSS PUT URL,可绕开本系统直接 upload 到 bucket

#### Verdict 🔴 **NOT merge as-is**
- **必须修正前提**:
  1. **每个 endpoint 加 `@RequirePermission`** — 至少: list/get/download → 跟 entityType 对应模块的 read; upload-url/register → 对应模块 read_write; softDelete → admin or owner
  2. 加 entity-type-aware 权限映射 (PURCHASE_ORDER → procurement, PAYMENT_VOUCHER → finance, QUALITY_CHECK → quality)
  3. PAYMENT_VOUCHER + QUALITY_CHECK 等敏感 EntityType 必须 finance:read / quality:read 守卫
  4. OSS upload-url 加 size limit + content-type whitelist (防被滥用上传非业务文件)
- **deploy-test 验证**:
  - OSS 端到端 (test env 需开 `aliyun.oss.enabled=true`)
  - 跨 factory 隔离: F001 user 不能 download F002 附件
  - 跨角色: warehouse_manager 不能 download PAYMENT_VOUCHER

---

### PR #659 — [Track-C] C-PRT-1 单据打印 PDF 🔴

**统计**: 10 files / +898 -0 / `feature/asap-track-c-prt-1` / **mergeable=MERGEABLE**

#### 1. Scope
合规 — Python `printing/` 模块新建 + Java PrintController 代理 + AI Tool + Flyway intent。

#### 2. 测试
- ✅ 5 PDF 渲染本地实测 valid (header `%PDF`, 二维码 OK)
- ✅ SQL pglast PARSE OK
- ❌ 无 Python unit test (`pdf_renderer.py` 无 `_test.py` 配对)
- ❌ 无 Java Controller test

#### 3. CI 状态 — **必修**
- 🔴 **`python-lint-test` FAIL**:
  ```
  ./printing/services/pdf_renderer.py:137:5: F401 'reportlab.lib.units.cm' imported but unused
  ./printing/services/pdf_renderer.py:172:121: E501 line too long (151 > 120 characters)
  ./printing/services/pdf_renderer.py:305:121: E501 line too long (151 > 120 characters)
  ./printing/services/pdf_renderer.py:341:121: E501 line too long (157 > 120 characters)
  ```
- java-build-test ✅, factoryId Isolation Audit ✅, rn-test ✅, vue-build-check ✅
- e2e-pr-gate ❌ (infra)

#### 4. 代码 quality
- ✅ 中文字体 fallback chain (wqy / Noto / msyh / simhei → Helvetica)
- ✅ 二维码 qrcode lib 失败 graceful skip
- ✅ Java `PrintController` 复用 `pythonAiRestTemplate` (无新 config)
- ⚠️ Brief 偏离: 用 reportlab 替代 weasyprint — Brief 偏离合理但需 organizer 接受
- ⚠️ Java payload 当前 stub (query overrides),实际 fetch entity 留 follow-up — 部分功能未闭环

#### 5. 安全性 — **关键问题**
- 🔴 **PrintController 5 endpoint 缺 @RequirePermission**:
  ```java
  @GetMapping("/sales-order/{id}")
  public ResponseEntity<byte[]> printSalesOrder(...)
  ```
  - 无任何权限守卫,任何 authenticated user 可调任何单据的 PDF
  - 价格敏感数据 (PO/SO/quotation/material-requisition) 全在 PDF 里 → 仓管员可绕 RBAC 拿到完整价格
  - **直接破坏 PR #423 的 RBAC 价格保护框架** (PriceMaskResolver 用于 PDF/Excel binary 守卫的合约)
- 🔴 **stub payload 接口仍打公网 endpoint**: 当前 controller 不查 entity,而是收 query parameters 当作"data",任何外部调用者都可发送伪造数据生成假 PDF
- 🟠 **`AIChat PrintDocumentTool`** 缺敏感性等级声明 (intent_config sensitivity_level LOW — 但打印价格敏感单据应是 MEDIUM/HIGH)

#### Verdict 🔴 **NOT merge as-is**
- **必修**:
  1. 修 4 个 flake8 错误 (rm unused import, line wrap)
  2. **5 endpoint 加 @RequirePermission**: sales-order/quotation/purchase-order → 对应模块 read; production-task → production:read; material-requisition → procurement:read_write
  3. **PriceMaskResolver hook**: Java controller 拉真 entity, 走 RBAC strip 才生成 PDF (不能让仓管员通过打印接口拿到原价)
  4. Flyway version: `V20260516_02__print_document_intent.sql` 跟 PR #649 (`V20260516_02__abaca.sql`) **同号同分支会冲突** — 需改 V20260516_05 或更高
  5. AIChat intent sensitivity_level 调整为 MEDIUM
- **deploy-test 验证**:
  - 5 单据真实数据 fetch + RBAC strip 后输出
  - 中文字体在 server 显示正常 (不出现 □)

---

### PR #660 — [Track-C] fix(smartbi): 三价对比 bug 🟢

**统计**: 4 files / +172 -5 / `feature/asap-track-c-3price-bug` / **mergeable=MERGEABLE**

#### 1. Scope
完全符合 — 4 文件 (DTO + service + test + Vue),无跨 track。

#### 2. 测试
**5/5 PASS** (`PurchaseServicePriceHintTest`):
- 双有 / 双 null / 仅 BOM null / 仅 avg null / 0 价边界
- 4 场景文案 + 关键词 assertion (含"不是 bug"对客户原话回应)

#### 3. CI 状态
- java-build-test ✅, python-lint-test ✅, rn-test ✅, vue-build-check ✅
- e2e-pr-gate ❌ (infra) + Vercel rate-limit

#### 4. 代码 quality
- ✅ `buildPriceDataSourceHint` 是 static pure function — 容易 test
- ✅ DTO 新字段 nullable + backward-compat (旧前端忽略不影响)
- ✅ 0 价边界正确处理 (业务允许 0 价: 赠品 / 内部调拨)
- ✅ 前端移除 `priceLoaded` 短路 — 修复 stale cache 问题
- ✅ Vue computed `priceDataSourceHintBanner` 干净

#### 5. 安全性
- ✅ 走现有 PurchaseService 路径 (`getPurchaseOrderById` 已有 RBAC)
- ✅ DTO 新字段是诊断文案 (无敏感数据)
- ✅ 前端 banner 只展示 hint string

#### Verdict 🟢 **直接 merge**
- 改动最小、测试最严、风险最低的 PR
- 推荐作为 Sprint 第 2 个 merge

---

### PR #661 — [Track-C] C-RBAC-1 仓管 RBAC 隔离审计 🟡

**统计**: 5 files / +590 -0 / `feature/asap-track-c-rbac-1`

#### 1. Scope
合规 — backend 仅加 test + AIChat tool + scripts/。**不改任何业务代码**。

#### 2. 测试
**4/4 PASS** (`RBACWarehouseManagerIsolationTest`):
- allKnownPriceFieldsAreAnnotated (反射枚举 8 entity)
- transientPriceGettersAreAnnotated (P0 invariant)
- auditScopeNonEmpty (防 misconfig)
- permissionStringIsStable (anchor test)

#### 3. CI 状态
- java-build-test ✅, factoryId Isolation Audit ✅, python-lint-test ✅, rn-test ✅, vue-build-check ✅
- Vercel backend ✅

#### 4. 代码 quality
- ✅ 反射 invariant test — sister chat 改字段漏 @PriceSensitive 立即抓住
- ✅ Tool 不主动跑 25 case (合理 — Chat 同步执行不适合长 batch)
- ✅ bash regression script `bash -n` PASS + chmod +x
- ⚠️ `expected-rbac-matrix.csv` 是 ground truth,后续若 RBAC 行为改动需同步更新

#### 5. 安全性
- 本 PR **本身是安全加固** — 反检测漏注解 + 实跑期望矩阵
- ⚠️ 但 5x5 regression 实跑结果**仍未提供** (per Brief Day 10-11 DoD 要 25/25 PASS)

#### Verdict 🟡 **小改后 merge**
- **建议补足**:
  - 在 deploy-test 跑 5x5 regression,把 report.md 附到 PR
  - 若发现 regression FAIL,需先 fix 漏注解字段再 merge
- **依赖关系**: 若 PR #649 / #658 / #659 merge 前没修 RBAC,此 PR 的 regression 会 FAIL — 建议 merge 顺序: **#649 #658 #659 RBAC 修完 → #661**

---

### PR #656 — [Track-D1] M-BOM-1 BOM 配方 + 2 bugs 🟠

**统计**: 26 files / +3895 -0 / `feature/asap-track-d1`

#### 1. Scope
3 项目 bundled (M-BOM-1 + Bug-2 物料选择器 + Bug-3 单位换算) — Brief 同意 bundle 因紧耦合。

#### 2. 测试
- ✅ Flyway dry-run on local cretas_db (BEGIN/ROLLBACK): 35 active items → 4 orphan → 31 migrated / 6 recipes
- ✅ mvn compile PASS (BUILD SUCCESS 2356 files, 0 errors)
- ✅ Unit normalization verified live (公斤→kg, 公升→L, 千克→kg)
- ❌ **没有 unit test 覆盖 BomRecipeServiceImpl business logic** — 状态机 transitions / cost calculation / clone-with-modify 都未测

#### 3. CI 状态
- java-build-test ✅, factoryId Isolation Audit ✅, python-lint-test ✅, rn-test ✅, vue-build-check ✅
- Vercel preview ✅ (deploy-staging 部署 OK)

#### 4. 代码 quality
- ✅ **24 处 @PriceSensitive** 标注 (BomRecipe 5 + BomRecipeItem 2 + RBAC 完整)
- ✅ `UnitConversionService` 提取为 public bean (避免私有 duplicate)
- ✅ Flyway migration 防御式 (orphan skip + 占位字段 + DRAFT status + COMMENT 标注)
- ✅ 单位 CHECK constraint 严格 ('g','kg','mg','ml','L','个','袋','箱','瓶','盒')
- ✅ partial unique index `is_current=TRUE` — 防同产品多版本同时 active
- ✅ Brief 偏离 3 处自动 disclosed (Flyway 路径 / Service 名 / MaterialSpec UI)
- ✅ MaterialSelectModal 字典 SELECT 替代手写 (客户原话回应)

#### 5. 安全性 — Flyway 冲突
- 🟠 **`V20260516_02__bom_redesign.sql` 跟 PR #649 / #659 同号冲突**:
  - PR #649: `V20260516_02__abaca.sql`
  - PR #659: `V20260516_02__print_document_intent.sql`
  - PR #656: `V20260516_02__bom_redesign.sql`
  - 3 个 PR 用同一 version,先 merge 的占住 V_02,后 merge 的会 Flyway resolution fail
- 🟠 `V20260516_03__bom_intent_configs.sql` 跟 PR #650 `V20260516_03__work_process_tasks.sql` 也同号冲突
- ✅ 所有 controller endpoint 有 @RequirePermission(`production:read_write`, `rd:read_write`, `finance:read_write`)
- ✅ @RequireModule("bom") class-level
- ✅ BomRecipeServiceImpl factory-scoped (factoryId 强制)

#### Verdict 🟠 **deploy-test 后 merge**
- **必修**:
  1. **Flyway 重排版本号**: V20260516_02 → V20260516_07 (V20260516_01 是 attachment, _02 abaca, _03 work-process, _04 work-process-intents, _05 print, _06 已被 #657 占; 本 PR 用 _07 + _08)
  2. 加 unit test 覆盖 status 状态机 (DRAFT → ACTIVE → ARCHIVED) + cost calculation
- **deploy-test 验证**:
  - V20260516_07 migration on test env (cretas_db 完整 schema)
  - AIChat: "给红烧肉建 BOM, 五花肉 200g 出成率 80%..." 触发 bom_recipe_create_from_text
  - RBAC: warehouse_mgr 看不到 unit_price / total_cost 字段
- **依赖**: 跟 #649 / #650 / #659 互相 Flyway 冲突 — 需 organizer 协调 version 编号

---

### PR #650 — [Track-D2] M-WP-1/M-WP-2 工序管理 🟠

**统计**: 23 files / +3380 -0 / `feature/asap-track-d2`

#### 1. Scope
合规 — backend workprocess + 5 AI Tool + 2 Flyway + RN 3 屏。

#### 2. 测试
- ✅ Flyway path correction (db/migration → db/flyway via pre-commit hook)
- ❌ **没有 unit test** — service / controller / state machine 都未测

#### 3. CI 状态
- java-build-test ✅, factoryId Isolation Audit ✅, python-lint-test ✅, rn-test ✅, vue-build-check ✅
- e2e-pr-gate ❌ (infra)

#### 4. 代码 quality
- ✅ **WorkProcessTaskController 全 endpoint 有 @RequirePermission + @PreAuthorize** (production:read_write + ADMIN/MANAGER)
- ✅ @RequireModule("production_plan") class-level
- ✅ 状态机 `assertTransition` 抛 BusinessException + actionHint
- ✅ PG-safe `CAST(:status AS string)` (per `.claude/rules/database-entity-sync.md` Rule)
- ✅ DTO factory-scoped finders
- ⚠️ `WorkProcessConfigUpdateTool` REPLACE 语义 + supportsPreview — 合理

#### 5. 安全性 — Flyway 冲突
- 🟠 **`V20260516_03__work_process_tasks.sql` 跟 PR #656 `V20260516_03__bom_intent_configs.sql` 同号冲突**
- 🟠 `V20260516_04__work_process_intents.sql` 干净 (#656 _02 _03,本 PR _03 _04)
- ✅ 所有 endpoint RBAC 守卫齐全 (本 Sprint 唯一 100% RBAC-clean 的新 controller)

#### Verdict 🟠 **deploy-test 后 merge**
- **必修**:
  1. **Flyway 版本重排**: V20260516_03/_04 → V20260516_09/_10 (避开 #656)
  2. 加 unit test 覆盖 state machine (PENDING → IN_PROGRESS → COMPLETED, illegal transitions)
- **deploy-test 验证**:
  - 5 道工序 (拆包/分割/卤制/分切/装筐) 创建 + 绑定到产品 (猪蹄)
  - AIChat: "给猪蹄加工序: 拆包→分割→卤制→分切→装筐" → preview → commit
  - 状态机非法 transition 抛 BusinessException + actionHint
- **依赖**: 跟 #656 Flyway 冲突 — 二选一先 merge,后者 rebase

---

## §3 Merge 顺序建议

按依赖关系 + 风险递增排序。每 merge 一个,deploy-test 验证后再下一个。

| Order | PR# | Track | 理由 |
|---|---|---|---|
| **1** | #654 | A | 最低风险,纯 wiring,无后端改动,无依赖。Sprint 首 merge 暖手。 |
| **2** | #660 | C | 改动最小 (172 行),5 单测 PASS,无 RBAC 问题,直接 fix 客户痛点。 |
| **3** | #651 | A | LLM 真接通 + 12 单测; 引入 PR #657 依赖的 ModuleConfig 字段定义。 |
| **4** | #657 | A | 依赖 #651。Tool Repository 统一,9 单测 PASS,Flyway V20260514_06。 |
| **5** | #661 | C | 静态 RBAC 审计 + Tool — 不改业务,可早 merge 提供 invariant。 |
| **6** | #649 | B2 | **必先修 RBAC**: 6 endpoint 加 @RequirePermission。后 deploy-test 验。 |
| **7** | #653 | B2 | Stacked on #649,跟着 merge。Device-level smoke 单测之外验证。 |
| **8** | #650 | D2 | **必先重排 Flyway** V20260516_03/_04 → _09/_10。WP 不依赖 BOM。 |
| **9** | #656 | D1 | **必先重排 Flyway** V20260516_02/_03 → _07/_08。BOM bundle 重型变动。 |
| **延后** | #658 | C | **必先修 RBAC** (8 endpoint),OSS 在 test env 端到端验后才考虑。 |
| **延后** | #659 | C | **必先修 flake8 + RBAC + Flyway 版本号 + 真实 entity fetch**。Brief 偏离需 organizer ack。 |

---

## §4 跨 PR 冲突检查

### 4.1 Flyway 版本号冲突 (HIGH)

| Version | PR# | Filename | 冲突 |
|---|---|---|---|
| `V20260514_06` | #657 | `__migrate_decoration_tool_layouts_to_factory_home_layout.sql` | ✅ 唯一 |
| **`V20260516_01`** | #658 | `__attachment.sql` | ✅ 唯一 (current) |
| **`V20260516_02`** | #649 | `__abaca.sql` | 🔴 **3 路冲突** |
| **`V20260516_02`** | #659 | `__print_document_intent.sql` | 🔴 同上 |
| **`V20260516_02`** | #656 | `__bom_redesign.sql` | 🔴 同上 |
| **`V20260516_03`** | #656 | `__bom_intent_configs.sql` | 🔴 **2 路冲突** |
| **`V20260516_03`** | #650 | `__work_process_tasks.sql` | 🔴 同上 |
| `V20260516_04` | #650 | `__work_process_intents.sql` | ✅ 唯一 |

**结论**: PR #649 / #659 / #656 / #650 必须**重排版本号**,推荐方案:
- #649 → V20260516_02 (保持,先 merge)
- #659 → V20260516_05
- #656 → V20260516_07 + V20260516_08
- #650 → V20260516_09 + V20260516_10

### 4.2 文件冲突

| 文件 | 涉及 PR | 冲突级别 |
|---|---|---|
| `dto/material/CreateAbacaQuantityLogRequest.java` | #649 (create) + #653 (modify: batchNumber/二选一) | 🟡 #653 stacked on #649 → 自动 OK |
| `service/material/AbacaQuantityLogService.java` | #649 (create) + #653 (modify: resolveBatchId) | 🟡 同上 |
| `service/inventory/impl/PurchaseServiceImpl.java` | #653 (`getPurchaseOrderByNumber`) + #660 (`buildPriceDataSourceHint`) | 🟠 **两个 PR 改同一文件** — 不同方法,合并 OK,但需 rebase 验证 |
| `web-admin/src/views/procurement/orders/detail.vue` | #658 (AttachmentList) + #660 (dataSourceHint) | 🟠 **两个 PR 改同一文件** — 修改区域不同,但需 rebase 后 vue-build 验 |
| `frontend/CretasFoodTrace/src/types/navigation.ts` | #654 (PageEditor) + #653 (WHReceiptCreate) + #649 + #656 (BOM) + #650 (WorkProcess) | 🟠 **5 PR 改同文件** — 都是新增 route,理论上 merge clean,但需 rebase 严格按顺序 |
| `frontend/CretasFoodTrace/src/navigation/factory-admin/FAManagementStackNavigator.tsx` | #654 + #650 | 🟡 同上 |
| `frontend/CretasFoodTrace/src/screens/factory-admin/management/FAManagementScreen.tsx` | #654 + #650 | 🟡 同上 |
| `frontend/CretasFoodTrace/src/navigation/ManagementStackNavigator.tsx` | #650 + #656 | 🟡 都是 stack 新增 route |

**结论**: 没有删 / 同行修改冲突,但 5 个 PR 改 `navigation.ts` 必须严格按 merge 顺序 rebase。

---

## §5 admin merge 之前必跑的 deploy-test 清单

### 5.1 单 PR 级别 deploy-test

| Order | PR# | deploy-test 步骤 | 预期结果 |
|---|---|---|---|
| 1 | #654 | RN expo 跑通 + 长按 FAHome 进 PageEditor | 拖拽 stats_card / save 生效 |
| 2 | #660 | F006 采购单详情 → 展开三价对比 → 看 banner + tooltip | 4 场景文案触发 |
| 3 | #651 | Java 启 → 调 `/decoration/home-layout/ai-generate` | modelUsed=qwen-flash + 真 layout |
| 4 | #657 | AI 触发 `home_layout_generate` → 查 `factory_home_layout` | wrapped `{"modules":[...]}` |
| 5 | #661 | 跑 `bash scripts/rbac-warehouse-mgr-audit-2026-05-15/run-regression.sh` | 25/25 PASS |
| 6 | #649 | V20260516_02 apply → F006 seed 命中 → abaca create + verify 双签 + warehouse_mgr 403 | seed 3 行 + 403 |
| 7 | #653 | 真 device 扫 PDF QR → WHReceiptCreate → confirmReceive → abaca log 自动落 | DB material_batches + abaca_quantity_log 各 1 行 |
| 8 | #650 | 5 工序创建 + 绑猪蹄 + AIChat preview/commit + 非法 transition 抛 BusinessException | actionHint 友好 |
| 9 | #656 | V20260516_07 apply → AIChat bom_recipe_create_from_text → DRAFT + warehouse_mgr 看不到 price | RBAC strip 生效 |

### 5.2 Sprint 级别 (全 merge 后) deploy-test

- **5×5 RBAC negative regression** (PR #661 script) — 必须 25/25 PASS
- **F006 e2e**: 抄码品全闭环 (PDF 扫码 → 入库 → 抄码 log → BOM 查物料 → 工序 spawn → 三价对比)
- **Playwright web-admin smoke** (`e2e-web-admin` skill) — 至少 R7-F2 多角色测试不 regression
- **Java/Python health 检查** — `47.100.235.168:10010/health`, `47.100.235.168:8083/health` 两个环境
- **Flyway tracker 完整性**: `SELECT version, success FROM flyway_schema_history` 看新 migrations 全 success=true
- **OSS 端到端** (PR #658 deferred): test env 开 `aliyun.oss.enabled=true` + 真 upload/download
- **PR #659 PDF 渲染**: test env 看 wqy-zenhei 字体是否安装,中文不出 □

### 5.3 已知 organizer 待确认事项

1. **iOS plist `NSCameraUsageDescription`** (PR #653) — expo-camera 用过 (LabelScanScreen),按理已 OK
2. **OSS bucket cretas-media 权限** (PR #658) — 账号 B,看 SSO/API 是否对 test env 开
3. **wqy-zenhei 字体** (PR #659) — server 47 是否 `fonts-wqy-zenhei` 已安装
4. **Brief 偏离批准** (PR #658 6 处, PR #659 3 处, PR #656 3 处, PR #650 1 处)

---

## §6 关键风险总结 (给 organizer)

### 🔴 必须修复才能 merge

1. **PR #658 AttachmentController 8 endpoint 全部 缺 RBAC** — 任何 user 可下载任意附件,完全绕过 PR #423 RBAC 框架
2. **PR #659 PrintController 5 endpoint 缺 RBAC + flake8 fail + stub payload** — 仓管员可通过打印接口拿到价格数据
3. **PR #649 MaterialAbacaController 8 endpoint 缺 RBAC + 自定义 JWT parse** — 绕开 SecurityUtils
4. **Flyway V20260516_02 三路冲突** (#649 #659 #656) — 后 merge 的会 fail

### 🟠 强烈建议修复

5. **PR #659 Flyway 版本号** + 真实 entity fetch (当前 stub)
6. **PR #656 / #650 Flyway 版本号** 重排
7. **PR #661 5×5 regression 实跑结果** 缺失

### 🟢 不阻塞但 follow-up

8. **PR #651 LLM cost** — token 用量观察 + 速率限制
9. **PR #649 / #650 / #656 / #650 全部** — 缺 unit test for state machine / business logic
10. **PR #658 OSS upload-url** — size limit + content-type whitelist
11. **PR #654 i18n key** `management.pageEditor` 加入翻译文件

---

## §7 审计员注记

- **e2e-pr-gate** 在多个 PR fail 都是 postgres docker init `role "root" does not exist` 的相同 infra 噪音,跟代码无关。建议 organizer 跟 DevOps 调 workflow 用 `POSTGRES_USER=postgres` 而非 `root`。
- **Vercel** 全部 fail 是 rate-limit (build-rate-limit, 24h 后自恢复),非阻塞。
- 大量 PR Brief 自述含"Brief 偏离" — 自我披露习惯良好,但 organizer 应正式 ack 而不是默认放行。
- 整 Sprint 共 **~10000 行新增代码**,跨 25+ 个组件; 强烈建议 PR 一个一个 merge,deploy-test 一个一个验,不要 batch merge 全部。

**审计 PR 数**: 11 / **总改动**: ~10100+ insertions / ~640+ deletions / **总文件**: ~125
**审计完成时间**: 2026-05-15
