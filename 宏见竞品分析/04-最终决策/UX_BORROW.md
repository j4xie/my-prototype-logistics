# 宏见 UI/UX 借鉴清单 (UX_BORROW)

> **范围**：**纯 UI/UX + 操作流程**，不讲功能、不讲业务实体、不讲战略。
>
> **基于**：UX_AUDIT_A_HONGJIAN.md（63 个独立 UX 模式 / 13 ⭐⭐⭐ ⭐⭐ 真值得抄）+ UX_AUDIT_B_CRETAS.md（Cretas 当前 UX 现状）。
>
> **核心认知**：Cretas 是「AI + 语音 + Bento + 移动卡片」**现代风格**；宏见是「密集 Web 表格 + 流程图 + 浮动菜单 + 权限矩阵」**传统风格**——两者**互补**。
>
> 借鉴策略 **按端分**：移动 App（RN）抄移动可用的；Web-Admin（Vue）抄 PC 密集场景的。

---

## A. Top 3 ⭐⭐⭐ 必抄（最高 ROI）

### A-1. 业务流程图导航（顶部可点击节点） ⭐⭐⭐
- **宏见做法**：每个模块顶部有"节点流程图"——粉色=待处理 / 绿色=进行中 / 蓝色=已完成。点击节点跳对应单据列表。
- **HD 帧证据**：`000043` 销售订单工作流图 / `001333` 采购流程图 / `001736` 财务流程图（5 张流程图卡片 page）
- **Cretas 现状**：❌ **完全无** WorkflowVisualizer 组件
- **Cretas 怎么用**（核心创新点）：
  - **移动 App**：放在首页 BentoGrid 顶部一个 1x2 大卡片，显示"今日工作流状态" + 3-5 个节点 + 数量徽章
  - **Web-Admin**：每个业务模块（销售/采购/生产）顶部加 horizontal flowchart bar
  - **AI 增强**：节点支持"AI 触发"——点节点 + 一句话即可（AIChat 进入该节点 context）
- **工作量**：组件抽象 5d / 5 模块接入 5d = **10 人天**
- **战略意义**：**onboarding 新员工神器** + 替代传统左侧菜单的认知方式

### A-2. 行末"操作 ▾" 下拉收纳次要动作 ⭐⭐⭐
- **宏见做法**：列表行末一个"操作 ▾"按钮，点开 8-14 个次要动作（转生产/转外购/退货/调拨/打印/复制/锁定 / ...）。**主行只显示 4-6 个 chip 状态**。
- **HD 帧证据**：`000122` 物料需求页 + `001435` 库存查询 + `001151` 销售订单 — 所有列表都这么做
- **Cretas 现状**：⚠️ 移动端用 long press / swipe 但 8+ 选项 BottomSheet 未抽象；Web-Admin 散用
- **Cretas 怎么用**：
  - **移动 App**：长按行 → BottomSheet 显示 8-10 个动作（已有 gesture handler，缺统一抽象）
  - **Web-Admin**：抽 `<RowActionMenu items={[]}/>` 组件
  - **AI 增强**：BottomSheet 顶部有「跟 AI 说」入口——"我要把这单转成生产"
- **工作量**：移动 BottomSheet 抽象 3d / Web RowActionMenu 3d / 8 列表接入 4d = **10 人天**
- **战略意义**：列表行不再拥挤，状态 chip + 动作下拉分离

### A-3. Sticky Footer 实时合计 + 分页同栏 ⭐⭐⭐
- **宏见做法**：列表底部固定栏一行显示 `共 3 条 [1/3] 总金额: ¥6,525 总数量: 122 总损耗: 5.2%`，跟分页器同栏
- **HD 帧证据**：`000949` 销售单底部 + `000745` 物料需求底部
- **Cretas 现状**：❌ **完全无** StickyFooterSummary 抽象
- **Cretas 怎么用**：
  - **移动 App**：列表底部 50px sticky bar（已有 SafeAreaView 基础）
  - **Web-Admin**：列表分页器升级为 `<TableFooter>` 含 stats
  - **AI 增强**：sticky bar 上一个图标点击 → AIChat "给我这页的统计分析"
- **工作量**：StickyFooterSummary 抽象 3d / 10 列表接入 4d = **7 人天**
- **战略意义**：财务/运营录入时**实时校验数据正确性**

---

## B. ⭐⭐ 重要借鉴（10 项 / 按端分）

### B-1. 移动 App (RN) 必抄 5 项

| # | UX 模式 | Cretas 现状 | 借鉴方式 | 工时 |
|---|---|---|---|---|
| M1 | **行级状态色块**（整行底色浅粉/浅绿/浅黄表达 status）| ❌ 仅 statusBadge | 抽 `RowStatusBackground` HOC，5 个核心列表接入 | 3d |
| M2 | **多维 mini chip 状态**（一行 5-7 个 chip 各代表一个维度：订单/物料/审核/已发货/已交付）| 🟡 部分有 | 抽 `MultiDimChipRow` 组件 | 3d |
| M3 | **Skeleton 加载**（替代 ActivityIndicator） | ❌ 0 业务实现 | 引入 `react-content-loader` 或自实，5 个核心页 | 3d |
| M4 | **Toast 抽象**（替代原生 Alert.alert） | ❌ 全用 Alert | 引入 `react-native-toast-message` + 用例规约 | 2d |
| M5 | **Haptic feedback**（按钮 / 提交 / 错误 触觉反馈）| ❌ 0 | `expo-haptics` 包装 + 重要操作接入 | 2d |

**小计：13 人天**

### B-2. Web-Admin (Vue) 必抄 5 项

| # | UX 模式 | Cretas 现状 | 借鉴方式 | 工时 |
|---|---|---|---|---|
| W1 | **累积多 Tab 持久化**（一个 window 开 20+ tab 切换不丢上下文）| ❌ 无 | 引入 Pinia tab store + Tab 组件 + 状态持久化 | 5d |
| W2 | **多维度联动筛选**（顶部 filter 改 → 表格刷新 + 左侧分类树 click → 切换分类） | 🟡 部分有 | 抽 `<MultiFilterPanel>` + tree sidebar 联动 | 5d |
| W3 | **权限矩阵编辑器**（员工 × 模块 × 权限点 3 维 checkbox 表格） | ❌ 无 | 用 element-plus el-table + cell renderer | 5d |
| W4 | **inline 分类树并排列表**（不是抽屉，左侧 200px 持续可见树） | 🟡 部分有 | 改造现有列表加 left sidebar slot | 3d |
| W5 | **历史搜索记录自动联想**（搜索框 dropdown 显示历史 keywords） | ❌ 无 | localStorage + autocomplete | 2d |

**小计：20 人天**

---

## C. ⭐ 值得参考（10 项 / 选择性）

### C-1. 视觉细节

| # | 模式 | 借鉴方式 |
|---|---|---|
| V1 | **米黄底色暗示列分组**（BOM 表里"原料组 / 辅料组 / 包材组"用列底色区分）| 移动卡片用，3d |
| V2 | **字段后 ⓘ tooltip**（复杂业务字段右侧小问号，点击显示说明）| 抽 `<FieldHint>` 包装，2d |
| V3 | **"当前节点: XXX" 流程指示文字**（轻量步骤条替代）| 抽 `<CurrentNodeBadge>`，1d |
| V4 | **明显数字大字 + 单位小字层级**（金额 / 进度 / 完成率 视觉权重）| 抽 `<MetricNumber>` typography 组件，2d |
| V5 | **显式"自动计算"按钮**（工资场景让用户掌控刷新时机，不要自动跑）| 高复杂计算场景接入，2d |

### C-2. 操作动线

| # | 模式 | 借鉴方式 |
|---|---|---|
| F1 | **多区域 6 区大行布局**（一行 6 区表达"做不做/谁负责/进度/物料/状态/操作"）| 移动卡片不抄，Web 工厂操作页接入，5d |
| F2 | **行内 QR 浮层**（列表行点 QR 图标弹出 QR + 单据说明）| Cretas 已有 LabelScanScreen 反向适配，2d |
| F3 | **二次确认按钮带计时**（删除/撤销重要操作 3s 倒计时）| 抽 `<ConfirmCountdown>`，2d |

### C-3. 状态反馈

| # | 模式 | 借鉴方式 |
|---|---|---|
| S1 | **多维 Error/Warn 视觉重量级**（INFO/WARN/ERROR/CRITICAL 4 档颜色 + icon）| 抽 `<AlertBanner>`，2d |
| S2 | **"已审核 by 张三 @2026-05-14"内嵌时间线**（审批进度内嵌） | 抽 `<ApprovalTimeline>`，3d |

---

## D. 🔴 ❌ 不抄 (移动范式冲突 / 客户群不需要)

### D-1. ❌ 移动端不抄

| # | 项 | 不抄原因 |
|---|---|---|
| N1 | 17 列宽表 | 移动端改为卡片 + drill-down |
| N2 | 累积多 Tab | 移动端用 stack + 历史栈 |
| N3 | 左侧 200+ 子菜单 | 移动端用 BottomTab + scene 入口 |
| N4 | hover 触发的下拉 | 移动端无 hover, 用长按 |
| N5 | 弹窗内 复杂表单 | 移动端用 Drawer 全屏 |

### D-2. 🔴 太老旧 / Cretas 已超越

| # | 项 | 不抄原因 |
|---|---|---|
| O1 | 12 模块顶部菜单 | Cretas Bento Grid 现代化更佳 |
| O2 | 静态业绩报表 | Cretas SmartBI NL Query 更强 |
| O3 | 抽屉式深层菜单 | Cretas 角色路由分离更清 |
| O4 | 工具栏图标密集 + 不带文字 | 现代设计趋势相反 |
| O5 | 系统右上角"用户名 + 部门" 显示 | Cretas 简化为头像 |

---

## E. 工时合计

| 优先级 | 项数 | 工时 |
|---|---|---|
| **Top 3 ⭐⭐⭐** (业务流程图导航 + 行末操作下拉 + Sticky Footer) | 3 | **27 人天** |
| **⭐⭐ 移动 App** | 5 | **13 人天** |
| **⭐⭐ Web-Admin** | 5 | **20 人天** |
| **⭐ 值得参考** | 10 | **27 人天**（选做） |
| **合计** | **23** | **60-87 人天** |

**最小必做集** (Top 3 + 5 移动 + 5 Web) = **60 人天 ≈ 12 周（单人）/ 6 周（双人）**

---

## F. 关键示意（怎么改 Cretas）

### F-1. 业务流程图导航 — 在哪用？

**移动 App** — 首页 BentoGrid 加新卡片类型：

```
┌────────────────────────────────────────┐
│  📊 今日销售工作流          [3 待办]    │
│                                          │
│  [⭕ 待审 5] → [🟢 进行 3] → [✅ 完成 12]│
│   ↑ 点击进入                              │
└────────────────────────────────────────┘
```

**Web-Admin** — 业务模块顶部 horizontal bar：

```
┌─────────────────────────────────────────┐
│  销售管理 / 销售订单                       │
├─────────────────────────────────────────┤
│ ⭕ 待审 (5) → 🟢 进行 (12) → ✅ 完成 (87)│
├─────────────────────────────────────────┤
│  [当前列表显示在这]                       │
└─────────────────────────────────────────┘
```

### F-2. 行末操作下拉 — 在哪用？

**移动 App** — 列表卡片长按：

```
长按订单卡片 → BottomSheet:
┌────────────────────┐
│  💬 跟 AI 说...     │  ← AI 增强入口
├────────────────────┤
│  📋 转生产任务      │
│  🛒 转采购单        │
│  📦 转外购          │
│  ✏️ 编辑           │
│  📄 打印 PDF        │
│  🔒 锁定            │
│  ↩️ 撤销审批         │
│  🗑️ 取消订单         │
└────────────────────┘
```

**Web-Admin** — 列表行末「操作 ▾」按钮：

```
| 订单号 | 客户 | 状态chips | ... | [操作 ▾] |
                                      ↓
                              ┌────────────────────┐
                              │ 💬 跟 AI 说...      │
                              │ 📋 转生产任务       │
                              │ 🛒 转采购单         │
                              │ ...                 │
                              └────────────────────┘
```

### F-3. Sticky Footer 实时合计 — 在哪用？

**移动 App** — 列表底部 sticky bar：

```
┌────────────────────────────────────────┐
│  订单卡片 #1                            │
│  订单卡片 #2                            │
│  订单卡片 #3                            │
├────────────────────────────────────────┤
│ 共 3 条 │ 金额 ¥6,525 │ 损耗 5.2%  📊 │ ← sticky
└────────────────────────────────────────┘
                                       ↑ 点 📊 = AI 分析
```

**Web-Admin** — 表格底部分页器 + 合计同栏：

```
┌────────────────────────────────────────┐
│  表格内容                                │
├────────────────────────────────────────┤
│ 共 87 条 [1/9] ‹ › │ ¥58,750 │ 损耗 4.1%│
└────────────────────────────────────────┘
```

---

## G. 战略融合 (Cretas 现代 + 宏见传统)

最终 Cretas UI/UX 设计语言（取两家所长）：

| 维度 | 选哪家 | 理由 |
|---|---|---|
| 整体设计语言 | Cretas Neo Minimal | 现代趋势，配色优雅 |
| 移动端导航 | Cretas Bento Grid + BottomTab | 角色分离 + 触感友好 |
| Web-Admin 导航 | **借鉴宏见累积 Tab + Cretas 配色** | 复杂场景多任务并行 |
| 列表行设计 | **借鉴宏见 chip + 操作下拉 + Cretas 圆角间距** | 信息密度 + 视觉舒适 |
| 表单 | **Cretas Formily AI-First** | 客户主诉求 |
| 弹窗 | Cretas BottomSheet 抽象（统一） | 移动友好 |
| 状态反馈 | **借鉴宏见 Skeleton + Toast + Cretas 配色** | 现代体验 |
| 状态可视化 | **借鉴宏见流程图 + Cretas 卡片** | onboarding 友好 |
| AI 增强 | **Cretas AIChat + 钉钉机器人** | 客户原话 |
| 微交互 | **Cretas Haptic 补充 + 长按 BottomSheet** | 触感优先 |

---

## H. Sprint 建议

### Sprint UX-1（Week 1-2，13 人天）
- Top 3 ⭐⭐⭐ 中的移动端部分：
  - 业务流程图导航 BentoGrid 卡片
  - 行末操作 BottomSheet 抽象
  - Sticky Footer 移动端实现
- 客户立即可感知

### Sprint UX-2（Week 3-4，13 人天）
- 移动 App ⭐⭐ 5 项全做：
  - 行级状态色块 + 多维 chip + Skeleton + Toast + Haptic

### Sprint UX-3（Week 5-7，20 人天）
- Web-Admin ⭐⭐ 5 项全做（如果有 Web-Admin 在用）

### Sprint UX-4（Week 8-10，选择性）
- C 类⭐ 值得参考的，按客户反馈选

---

## I. 元层认知

1. **不要全抄**：宏见 PC Web 范式有 30% 不适合移动端
2. **AI 增强是 Cretas 独家**：每个借鉴模式都加"AI 触发"入口（行末下拉顶部「跟 AI 说」/ sticky footer「📊 AI 分析」/ 流程节点「AI 进入」）
3. **Cretas 现代设计是优势**：不要为了对齐宏见放弃 Neo Minimal
4. **角色化 UI 已经领先**：10 角色独立 Navigator 是宏见做不到的
5. **Bento Grid + 流程图导航融合**：这是创新点——首页 Bento 卡片中放流程图，融合两家所长

---

## J. 文档地图

```
UX_BORROW.md  ← 纯 UX 借鉴入口（本文件）
    ↓ 证据底稿
UX_AUDIT_A_HONGJIAN.md  ← 宏见 63 UX 模式
UX_AUDIT_B_CRETAS.md  ← Cretas 当前 UX 现状
    ↓ 关联
MUST_COPY.md  ← 功能必抄清单（业务层）
REVISED_STRATEGY.md  ← Hybrid 战略
```

UX_BORROW 跟 MUST_COPY 是**两个独立维度**：MUST_COPY = 业务功能；UX_BORROW = 设计语言。互补，不重复。
