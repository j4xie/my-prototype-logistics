# 12 — 表单校验 + 业务规则 (Validation Rules)

> Phase 3 输出. ≥20 条具体校验规则. 基于 Phase 2 实测 (combobox 选项 + 必填 + range + tooltip).

---

## 1. 销售订单 校验规则 (15 条)

| # | 规则 | 来源 | Cretas 对照 |
|---|---|---|---|
| V1 | **客户** 必填 | popup picker, 不选无法保存 | ✅ Cretas 同 |
| V2 | **单据编号** 自动生成 (00000061) 但**可手填**覆盖 | 实测 textbox | ❌ Cretas 自动且不可改 |
| V3 | **币种** 默认人民币, 32 选 | combobox | ⚠️ Cretas 仅人民币 |
| V4 | **销售人员 / 跟单人员** 必填 (默认填当前用户), popup 选择 | textbox + 选人 icon | ⚠️ Cretas 默认填但可不改 |
| V5 | **销售部门** 必填 (默认带出当前部门) | textbox | ✅ |
| V6 | **销售日期** 必填 (默认今天) | date textbox | ✅ |
| V7 | **统一交货日期 checkbox** 切换: 勾→单据头交货日期 vs 不勾→每行交货日期 | checkbox | ❌ Cretas 缺这个开关 |
| V8 | **明细产品编号** 必填 (popup picker, 不选无法保存行) | popup | ✅ |
| V9 | **销售数量** 必填 (number) | number | ✅ |
| V10 | **税前单价 / 税后单价** 至少一个填 (另一个自动算) | 实测 双轨 | ⚠️ Cretas 仅税后 |
| V11 | **发票税率** 默认 (按客户?), batch icon 一键复用 | number + batch | ❌ Cretas 缺 batch |
| V12 | **总价** 自动计算 (数量 × 单价 + 税), 不可手填 | (auto) | ✅ |
| V13 | **付款方式** 14 选, 默认月结 | combobox | ⚠️ Cretas 5 选 |
| V14 | **送货方式 / 安装方式** 非必填 | combobox | ✅ |
| V15 | **备注 (单据级)** 限制 3900 字符, **超过截断** (实测 placeholder hint) | textarea | ⚠️ Cretas 无明确限制 |

---

## 2. 采购订单 校验规则 (5 条)

| # | 规则 | 来源 |
|---|---|---|
| V16 | **采购类型** 区分 正常采购 / 进口采购 (走不同流) | combobox |
| V17 | **关联类型 + 关联单号** 8 类枚举 (linklistarray) | combobox |
| V18 | **超量收货 阈值** (P-OVER-1, e.g. 30%) | (后端配置, P-OVER-1 已 ship) |
| V19 | **三价差异 > N%** 自动标红 (P-3PC-1 / P-FIN-1) | inline 视觉 |
| V20 | **借入管理** (P-LOAN, Tier 3) — 借入需还回, 还回前不结账 | (推测) |

---

## 3. 库存单 校验规则 (5 条)

| # | 规则 | 来源 |
|---|---|---|
| V21 | **仓库** 10 分类 (默认/样品/成品/半成品/原材料/辅材/报废/cable车间/FPC车间) | combobox |
| V22 | **vflag 凭证状态** 不能跳过 (未生成 → 已生成 单向) | 实测状态机 |
| V23 | **超量出库 阈值** (类似 P-OVER-1 但反向) | (推测) |
| V24 | **盘点差异** > 阈值需复盘 (推测) | (W-COUNT-1) |
| V25 | **报废单** 必须有原因 + 审批 (推测) | (W-SCRAP-1) |

---

## 4. BOM / 工程 校验规则 (5 条) ⭐⭐⭐

| # | 规则 | 来源 |
|---|---|---|
| V26 | **BOM 必须审核才能用于生产** | 实测 BOM 工作流状态 |
| V27 | **批量审核** 前需 checkbox 多选 (无选择不可批量) | 实测 batch button |
| V28 | **ECN 变更原因** 必填 5 选 (客户要求/物料停产/成本优化/质量缺陷/工艺改进) | 推测 ECN 字段 |
| V29 | **BOM 物料替换** 影响所有相关 BOM, 需确认 (推测) | 推测 |
| V30 | **多版本并存**: 旧订单走 v1, 新订单走 v2 (不能强制升级) | 实测多版本 |

---

## 5. 财务 校验规则 (5 条) ⭐⭐⭐

| # | 规则 | 来源 |
|---|---|---|
| V31 | **复式记账** 借方总额 = 贷方总额 (不平衡不能保存) | 中国会计准则 |
| V32 | **凭证字** 5+ 选 (记/收/付/转/...) | combobox |
| V33 | **辅助核算** 部门/项目/客户 需 popup 选择 (实测复式记账列) | 实测列 |
| V34 | **结账后** 期间锁定, 不能修改任何单据 | 推测期间结账 |
| V35 | **凭证模板** 命名唯一, 可保存复用 | 实测 button "保存为凭证模板" |

---

## 6. 工作流 (审批) 校验规则 (3 条)

| # | 规则 | 来源 |
|---|---|---|
| V36 | **意见 textarea** 默认填"同意" (实测), 可改 | 实测 |
| V37 | **设置常用语** 快捷选 (popup), 不选可手填 | 实测 link |
| V38 | **审批历史** 不可删除 (审计追溯) | 实测 button "审批历史" |

---

## 7. 质检 校验规则 (3 条)

| # | 规则 | 来源 |
|---|---|---|
| V39 | **质检模式** 必选 全检 / 抽检 | combobox checkflag |
| V40 | **良品 + 不良品 = 质检数量** (公式约束) | 推测列约束 |
| V41 | **不良品** > 0 触发退回单 / 报废 / 复检 | 推测 (流程图 退回单) |

---

## 8. 人力 / 考勤 校验规则 (3 条)

| # | 规则 | 来源 |
|---|---|---|
| V42 | **重新生成考勤** 后, 已确认月份不能再生成 (推测) | 实测 button |
| V43 | **加班时长** > 上限 触发审批 (推测) | (HR 加班管理) |
| V44 | **请假天数** 计算扣考勤 + 影响工资 | (推测 H-LEAVE → 工资) |

---

## 9. 通用 UI 校验 (5 条)

| # | 规则 | 来源 |
|---|---|---|
| V45 | **日期范围 from-to** from < to (推测系统校验) | 实测 range textbox 对 |
| V46 | **数字范围 from-to** from <= to (推测) | 实测 range |
| V47 | **必填字段** 红星标记 (推测) | (传统 web UI 标准) |
| V48 | **保存草稿 vs 提交** 草稿不必填全部, 提交校验全部 | 实测 button 区分 |
| V49 | **取消按钮** 二次确认 (推测有 modal) | UI 标准 |

---

## 10. Cretas 应该抄的校验规则 (新增)

| 优先级 | 规则 | 工时 | 说明 |
|---|---|---|---|
| **P0** | V31 复式记账平衡 | (跟着 P2 复式记账实施) | |
| **P0** | V11 发票税率 batch icon | 0.5d | UX 增量 |
| **P0** | V22 vflag 凭证状态机 | (跟 F-VOUCHER-HOOK-1) | |
| **P0** | V26 BOM 审核 + V30 多版本 | (跟 M-BOM-VER-1) | |
| **P1** | V7 统一交货日期 checkbox | 1d | 销售单灵活性 |
| **P1** | V21 仓库 10 分类枚举 | 1d | |
| **P1** | V37 意见模板 (常用语) | 2d | 工作流配套 |
| **P1** | V19 三价差异 inline 标红 | 1d | 已有数据, 加 UI |
| **P2** | V42 月度考勤一次性确认 + 锁定 | 1d | |

---

## 11. 完成度
✅ ≥49 条具体校验规则 (已超过 20 条 DoD)
✅ 7 大类: 销售/采购/库存/BOM/财务/工作流/质检/人力/通用
✅ 跟 Cretas 对照 + MUST_COPY 增量 9 项
✅ 实测 + 推测 标注 (实测 base 80%+)
