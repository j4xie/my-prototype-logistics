# Sub-Project B: 实体解析 + 形态路由 (Entity Resolution + Shape Router)

**所属**: 数据织网 (Data Fabric) 4 个 Sub-Project 之 B
**版本**: v1.0
**日期**: 2026-04-25
**关联**:
- `01-总览路线图.md` v1.2
- `02-A-能力驱动渲染.md` v1.4 (A 是 B 的契约层, B 必须填 A 的 metadata)

**工作量**: **5.5-8 周** (单工, 含 Sheet Merger / 标注 / 并发 / 4 MVP 模板)
**关键依赖**: A 的 `requires:` 契约 schema 和 `_ALIAS_TO_ATTR` 表 (`smartbi/canonical/aliases.py`)
**关键产出**: 智能识别上传文件形态 → 多 Agent 实体解析 → Sheet Merger → 写入对应 Silver writer

---

## 1. 目标与边界

### 1.1 目标

让客户上传**任何形态**的餐饮业务文件 (账单流水 / 商品汇总 / 评论 / 财务 / 库存 / 排班 / 会员) 系统自动:

1. **识别形态** (LLM-driven Shape Detector)
2. **路由到对应 Silver 写入路径** (Shape Router → 4-5 种 writer)
3. **跨文件用 AI 识别同一店/菜/人** (Multi-Agent RAG 渐进策略)
4. **多 sheet 智能时间合并** (Sheet Merger, A v1.4 §11 移交需求)

### 1.2 在 Data Fabric 体系中的角色

```
Bronze 层 (raw row_data JSONB)
       ↓
[B] Shape Detector → Shape Router         ← 本 spec
       ↓
[B] Silver Writer (4-5 种 per shape)      ← 本 spec
       ↓
[B] Multi-Agent Entity Resolution         ← 本 spec
       ↓ stable entity_id
[B] Sheet Merger (merge_status)           ← 本 spec
       ↓
Silver 层 (fact_pos_* / dim_* / 新增 4 张)
       ↓
[A] Capability Calculator 自动暴露字段     ← 已完成
```

### 1.3 in-scope vs out-of-scope

**In Scope**:
- ✅ Shape Detector (LLM + rule hybrid)
- ✅ Shape Router (路由规则)
- ✅ Multi-Agent Entity Resolution (B1 baseline 2 agent + B2 升级 3 agent)
- ✅ 标记数据集 SOP (双盲 60/20/20)
- ✅ Sheet Merger (`merge_status` + 时间推断)
- ✅ 4 个新 Silver writer (商品汇总 / 评论 / 财务 / 库存)
- ✅ 并发模型 (advisory_xact_lock + asyncio.Lock + cache 失效)
- ✅ 4 个 MVP 模板 (1 个/形态)
- ✅ Admin UI (低置信度实体配对裁决)
- ✅ A 契约元数据填充 (`requires:` for new templates + new fields)
- ✅ `_ALIAS_TO_ATTR` 扩展 (新增 ~10-15 个 canonical 字段)

**Out of Scope (留给 C/D)**:
- ❌ Cell-level field provenance (留给 C, B 用简单 `merge_status` 兜底)
- ❌ 字段冲突解决规则 + 继承 cascade (C)
- ❌ Federated Query Engine (D)
- ❌ Multi-Agent RAG 中 7+ agent / 排班 / 会员 形态 (B 仅做 4 个高优 + 排班/会员留 C 后扩展)
- ❌ POS API connector (后期产品化)

---

## 2. Multi-Agent RAG 实体解析

### 2.1 渐进策略 (从 A v1.4 §4.2 继承)

```
阶段 B1 (MVP, 1.5-2 周): baseline 2-agent
  ├── DeterministicAgent (升级 alias_normalizer.py)
  └── EmbeddingAgent (Qwen text-embedding-v3 + 余弦)
       ↓
跑标记数据集 holdout (§4.2.1, B1 末尾)
   - 店名 ≥ 88% / 菜名 ≥ 88% / 人名 ≥ 80% → ✅ ship B1, 跳过 B2
   - 任一 < 88% / < 80% → 进 B2
   - 任一 < 75% → 急停, 重新设计 baseline (e.g., 改 embedding 模型)

阶段 B2 (条件触发, 1-1.5 周): 升级到 5-agent
  ├── DeterministicAgent (B1 已有)
  ├── EmbeddingAgent (B1 已有)
  ├── ContextualAgent (新: 店名+城市+区, 菜名+品类+店 上下文 embedding)
  ├── TransitiveAgent (新: A=B, B=C → A=C 闭包)
  └── LLMArbitrator (新: 仅低置信度上 qwen-plus, 不是 qwen-max)
       ↓
B2 holdout: 店名 ≥ 92% / 菜名 ≥ 92% / 人名 ≥ 85% 才 ship
```

**为什么不一开始就 5 agent**:
1. 工作量翻倍 (3-4 周 → 6-8 周), 不一定值得
2. 单 LLM 在简单 case 浪费钱 (99% 实体匹配应该走便宜 deterministic + embedding)
3. 量化阈值让决策可证伪, 不是 vibes

### 2.2 EntityResolutionOrchestrator 接口

```python
# backend/python/smartbi/canonical/entity_resolution/orchestrator.py
from dataclasses import dataclass
from enum import Enum
from typing import List, Optional

class EntityType(Enum):
    STORE = "store"
    PRODUCT = "product"
    STAFF = "staff"
    # 后期: BILL, TABLE, INGREDIENT, CUSTOMER

@dataclass(frozen=True)
class ResolutionInput:
    """输入: 一个待解析实体名 + 上下文."""
    raw_name: str
    entity_type: EntityType
    factory_id: str
    context: dict  # 如 store: {"city": "上海", "district": "徐汇"}; product: {"category": "川菜", "store_id": 42}

@dataclass(frozen=True)
class ResolutionOutput:
    """输出: 解析决策 + 置信度 + 来源 agent."""
    matched_entity_id: Optional[int]   # None 表示新建
    confidence: float                  # 0-1
    decided_by_agent: str              # 决策 agent 名
    reasoning: str                     # 人类可读 (用于 admin 队列)
    fallback_chain: List[str]          # 调用了哪些 agent (debug)

class EntityResolutionOrchestrator:
    """协调多个 agent, 按规则聚合."""
    
    def __init__(self, pool, agents: List["BaseAgent"]):
        self._pool = pool
        self._agents = agents  # [DeterministicAgent, EmbeddingAgent, ...]
    
    async def resolve(self, input: ResolutionInput) -> ResolutionOutput:
        """按 agent 顺序跑, 第一个 ≥ 高置信度阈值 的胜出.
        
        阈值 (调研可调):
          DeterministicAgent: 0.95 (只有完全相等才 ship)
          EmbeddingAgent: 0.90
          ContextualAgent: 0.85
          TransitiveAgent: 0.80
          LLMArbitrator: 0.70 (低置信度 fallback)
        
        全部 < 阈值 → 写 admin 队列 (mapper_method='manual', 等人工裁决)
        """
        chain = []
        for agent in self._agents:
            result = await agent.resolve(input, self._pool)
            chain.append(agent.name)
            if result.confidence >= agent.ship_threshold:
                return ResolutionOutput(
                    matched_entity_id=result.matched_entity_id,
                    confidence=result.confidence,
                    decided_by_agent=agent.name,
                    reasoning=result.reasoning,
                    fallback_chain=chain,
                )
        
        # 所有 agent 都低置信度 → 进 admin 队列, 临时返新建 entity
        return ResolutionOutput(
            matched_entity_id=None,
            confidence=0.0,
            decided_by_agent="admin_queue",
            reasoning="所有 agent 置信度均不足, 进入人工审核",
            fallback_chain=chain,
        )
```

### 2.3 5 个 Agent 详细设计

#### 2.3.1 DeterministicAgent (B1)

**职责**: 简繁/空白/标点归一化 + 完全相等匹配. 99% 简单 case 走这.

```python
# backend/python/smartbi/canonical/entity_resolution/agents/deterministic.py
class DeterministicAgent(BaseAgent):
    name = "deterministic"
    ship_threshold = 0.95
    
    async def resolve(self, input: ResolutionInput, pool) -> AgentResult:
        # 1. 用 alias_normalizer 归一化 (复用现有 smartbi/canonical/alias_normalizer.py)
        normalized = normalize_for_dim(input.raw_name)  # 简繁/空白/标点
        
        # 2. 在 dim_{entity_type} 表查 normalized_name 完全相等
        async with pool.acquire() as conn:
            row = await conn.fetchrow(f"""
                SELECT {input.entity_type.value}_id 
                  FROM dim_{input.entity_type.value}
                 WHERE factory_id = $1 AND normalized_name = $2
            """, input.factory_id, normalized)
        
        if row:
            return AgentResult(
                matched_entity_id=row[0],
                confidence=1.0,  # 完全相等
                reasoning=f"normalized 完全匹配: '{input.raw_name}' → '{normalized}'",
            )
        return AgentResult(matched_entity_id=None, confidence=0.0, reasoning="无完全匹配")
```

**真实案例**:
- "唏嘛香 金虹桥店" / "唏嘛香金虹桥店" / "唏嘛香  金虹桥店" → 同一 normalized "唏嘛香金虹桥店" → 命中
- "招牌青花椒鱼（一吃）" / "招牌青花椒鱼(一吃)" → 半角 / 全角统一 → 命中

#### 2.3.2 EmbeddingAgent (B1)

**职责**: 名字向量化 + 余弦相似度. 处理同义/拼写错误.

```python
# backend/python/smartbi/canonical/entity_resolution/agents/embedding.py
class EmbeddingAgent(BaseAgent):
    name = "embedding"
    ship_threshold = 0.90
    
    def __init__(self, dashscope_client):
        self._client = dashscope_client
        self._embedding_cache: dict[tuple[str, str], list[float]] = {}  # (factory_id, name) → vector
    
    async def resolve(self, input: ResolutionInput, pool) -> AgentResult:
        # 1. 算 raw_name 的 embedding (qwen text-embedding-v3, 1024 dim)
        query_emb = await self._embed(input.raw_name)
        
        # 2. 查 dim_{entity_type} 所有候选 (同 factory)
        async with pool.acquire() as conn:
            rows = await conn.fetch(f"""
                SELECT {input.entity_type.value}_id, name, normalized_name
                  FROM dim_{input.entity_type.value}
                 WHERE factory_id = $1
                 LIMIT 1000  -- 防止 factory 实体爆炸 (一般 < 200)
            """, input.factory_id)
        
        if not rows:
            return AgentResult(matched_entity_id=None, confidence=0.0, reasoning="dim 表为空")
        
        # 3. 算每个候选的 embedding (缓存) + 余弦相似度
        best_id, best_sim = None, 0.0
        for row in rows:
            cand_emb = await self._embed(row["normalized_name"], cache_key=(input.factory_id, row["normalized_name"]))
            sim = cosine_similarity(query_emb, cand_emb)
            if sim > best_sim:
                best_sim, best_id = sim, row[0]
        
        # 4. 输出
        return AgentResult(
            matched_entity_id=best_id if best_sim > 0.7 else None,
            confidence=best_sim,
            reasoning=f"cosine sim={best_sim:.3f} 最高匹配 entity_id={best_id}",
        )
    
    async def _embed(self, text: str, cache_key=None) -> list[float]:
        if cache_key and cache_key in self._embedding_cache:
            return self._embedding_cache[cache_key]
        # 调 DashScope
        result = await self._client.embed(text, model="text-embedding-v3")
        if cache_key:
            self._embedding_cache[cache_key] = result
        return result
```

**Embedding 缓存策略**:
- 内存缓存 (per-process, 重启清)
- key = `(factory_id, normalized_name)`
- TTL: 不限 (因为 normalized_name 不变, model 不升级时不会失效)
- 模型升级 → 整个 cache 清

**成本估算**: 100 tenants × ~200 distinct entities/tenant × ~50 tokens × ¥0.0007/1K = ~¥0.7/月. 微小.

#### 2.3.3 ContextualAgent (B2, 条件触发)

**职责**: 加上下文 (店名+城市+区+品牌, 菜名+品类+店) 重新 embed, 提高准确率.

```python
class ContextualAgent(BaseAgent):
    name = "contextual"
    ship_threshold = 0.85
    
    async def resolve(self, input: ResolutionInput, pool) -> AgentResult:
        # 1. 构造 context 字符串
        if input.entity_type == EntityType.STORE:
            context_str = f"{input.raw_name} {input.context.get('city', '')} {input.context.get('district', '')}"
        elif input.entity_type == EntityType.PRODUCT:
            context_str = f"{input.raw_name} {input.context.get('category', '')} 店:{input.context.get('store_name', '')}"
        elif input.entity_type == EntityType.STAFF:
            context_str = f"{input.raw_name} 店:{input.context.get('store_name', '')} 岗位:{input.context.get('role', '')}"
        else:
            return AgentResult(matched_entity_id=None, confidence=0.0, reasoning="不支持的实体类型")
        
        # 2. 跟 EmbeddingAgent 类似, 但 embed context_str 而非 raw_name
        query_emb = await self._embed(context_str)
        
        # 3. 查候选, 各自构造同样格式的 context_str, embed, 比相似度
        # ... (同 EmbeddingAgent 但用 context_str)
        
        return AgentResult(
            matched_entity_id=best_id,
            confidence=best_sim,
            reasoning=f"contextual sim={best_sim:.3f}, ctx={context_str!r}",
        )
```

**真实案例**:
- "金虹桥店" 单独可能匹配多个店 → 加 city="上海" + district="长宁" → 唯一
- "招牌青花椒鱼" 单独可能歧义 (川菜店和粤菜店都有) → 加 store="桂满陇" → 准确

#### 2.3.4 TransitiveAgent (B2)

**职责**: 处理传递性闭包. A=B, B=C → A=C.

```python
class TransitiveAgent(BaseAgent):
    name = "transitive"
    ship_threshold = 0.80
    
    async def resolve(self, input: ResolutionInput, pool) -> AgentResult:
        """读 entity_resolution_history 表 (新建), 找 'A=B, B=C → A=C' 链.
        
        先决条件: 之前的 EmbeddingAgent / DeterministicAgent 已经决策过的 (raw_name → entity_id)
        都记录在 history 里, TransitiveAgent 用这些建立 union-find 找连通分量.
        """
        async with pool.acquire() as conn:
            # 1. 找跟 raw_name 历史 sim > 0.85 的 (a candidates)
            candidates = await conn.fetch("""
                SELECT b_name, b_entity_id, sim
                  FROM entity_resolution_history
                 WHERE factory_id = $1 
                   AND a_name = $2 
                   AND sim > 0.85
                 ORDER BY sim DESC LIMIT 5
            """, input.factory_id, normalize(input.raw_name))
            
            # 2. 对每个 b, 看 b 是不是又跟其他 c 高 sim 配对过
            for cand in candidates:
                next_hop = await conn.fetch("""
                    SELECT b_name, b_entity_id, sim FROM entity_resolution_history
                     WHERE factory_id = $1 AND a_name = $2 AND sim > 0.85
                """, input.factory_id, cand["b_name"])
                if next_hop and next_hop[0]["b_entity_id"] == cand["b_entity_id"]:
                    # 连通: input → cand → next_hop, 都同一个 entity
                    return AgentResult(
                        matched_entity_id=cand["b_entity_id"],
                        confidence=min(cand["sim"], next_hop[0]["sim"]) * 0.95,  # 闭包稍打折
                        reasoning=f"传递性: '{input.raw_name}' → '{cand['b_name']}' → entity_id={cand['b_entity_id']}",
                    )
        
        return AgentResult(matched_entity_id=None, confidence=0.0, reasoning="无传递性闭包")
```

**`entity_resolution_history` 新表** (B 引入):

```sql
CREATE TABLE entity_resolution_history (
    id BIGSERIAL PRIMARY KEY,
    factory_id VARCHAR(50) NOT NULL,
    entity_type VARCHAR(20) NOT NULL,
    a_name VARCHAR(500) NOT NULL,        -- 输入名 (normalized)
    b_name VARCHAR(500) NOT NULL,        -- 匹配名 (normalized)
    b_entity_id BIGINT NOT NULL,         -- 决策的 entity_id
    sim NUMERIC(5,4),                    -- 相似度
    decided_by_agent VARCHAR(20),
    created_at TIMESTAMP DEFAULT NOW()
);
ALTER TABLE entity_resolution_history ENABLE ROW LEVEL SECURITY;
ALTER TABLE entity_resolution_history FORCE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON entity_resolution_history
  USING (factory_id = current_setting('app.factory_id', true));

CREATE INDEX idx_eh_factory_entity_a ON entity_resolution_history (factory_id, entity_type, a_name);
```

#### 2.3.5 LLMArbitrator (B2, 兜底)

**职责**: 最贵, 仅低置信度 case 上. 用 qwen-plus 不是 qwen-max (省钱).

```python
class LLMArbitrator(BaseAgent):
    name = "llm_arbitrator"
    ship_threshold = 0.70  # 最低门槛
    
    async def resolve(self, input: ResolutionInput, pool) -> AgentResult:
        # 1. 取上面 EmbeddingAgent 输出的 top-5 候选 (假设有上层传过来)
        candidates = input.context.get("top_5_candidates", [])
        if not candidates:
            return AgentResult(matched_entity_id=None, confidence=0.0, reasoning="无候选可仲裁")
        
        # 2. 构造 prompt
        prompt = f"""
你是餐饮数据实体识别专家. 判断"{input.raw_name}"是否与下列任一候选指同一{input.entity_type.value}:

候选 (按相似度排序):
{format_candidates(candidates)}

上下文: {input.context}

返回 JSON: {{"matched_id": <候选 id 或 null>, "confidence": <0-1>, "reasoning": "<简要原因>"}}
"""
        # 3. 调 qwen-plus
        response = await self._llm_client.chat(prompt, model="qwen-plus")
        result = parse_json(response)
        
        return AgentResult(
            matched_entity_id=result["matched_id"],
            confidence=result["confidence"],
            reasoning=f"LLM: {result['reasoning']}",
        )
```

**成本估算**: 仅低置信度灰色区调用. 100 tenants × 平均 100 灰色 case/月 × 1K tokens × ¥0.012/1K (blended) = ¥120/月. (在 roadmap §9.2 4 杠杆估算内.)

### 2.4 决策流程 visualization

```
                    ResolutionInput("唏嘛香金虹桥店", STORE, F001)
                              ↓
      ┌────────── DeterministicAgent ──────────┐
      ↓ confidence ≥ 0.95?                     │
      ✅ YES → ship                              │ NO
                                                ↓
                  ┌──────── EmbeddingAgent ──────┐
                  ↓ confidence ≥ 0.90?           │
                  ✅ YES → ship                   │ NO (B1 此时进 admin 队列)
                                                  ↓
                              ┌─── ContextualAgent (B2) ──┐
                              ↓ ≥ 0.85?                    │
                              ✅ YES → ship                  │ NO
                                                              ↓
                                          ┌── TransitiveAgent (B2) ──┐
                                          ↓ ≥ 0.80?                    │
                                          ✅ YES → ship                  │ NO
                                                                          ↓
                                                          LLMArbitrator (B2)
                                                          ≥ 0.70? → ship / 否则 admin 队列
```

---

## 3. Shape Detector + Router

### 3.1 7 个支持的形态

| Shape | 关键 column 特征 | Silver writer | MVP 模板 |
|---|---|---|---|
| `bill_flow` | 账单号 + 营业日期 + 商品信息 | 现有 backfill_silver.py + dim_resolver | 现有 dish_sales_top_n 等 |
| `product_summary` | 门店 + 商品 + 销售金额 (无账单/日期) | **B 新增**: ProductSummaryWriter | "商品销售 Top 10" |
| `review` | 商品/店 + 评分 + 评论文本 | **B 新增**: ReviewWriter | "评分趋势 + Top 关键词" |
| `finance` | 凭证号 + 收支金额 + 科目 | **B 新增**: FinanceWriter | "月度收支" |
| `inventory` | 食材/物资 + 库存量 + 盘点日期 | **B 新增**: InventoryWriter | "库存预警" |
| `schedule` | 员工 + 班次 + 日期 | (留 C 后扩展) | (留 C) |
| `member` | 会员 ID + 储值 + 消费 | (留 C 后扩展) | (留 C) |

### 3.2 Shape Detector (LLM-driven)

```python
# backend/python/smartbi/canonical/shape_detector.py
from enum import Enum
from typing import List, Optional
import json

class FileShape(Enum):
    BILL_FLOW = "bill_flow"
    PRODUCT_SUMMARY = "product_summary"
    REVIEW = "review"
    FINANCE = "finance"
    INVENTORY = "inventory"
    SCHEDULE = "schedule"
    MEMBER = "member"
    UNKNOWN = "unknown"

@dataclass
class DetectionResult:
    shape: FileShape
    confidence: float
    reasoning: str

class ShapeDetector:
    """LLM-driven shape 识别. 第一遍跑 rule-based 快路径,
    rule 不确定时 fallback to qwen-plus."""
    
    async def detect(self, sample_rows: List[dict], column_names: List[str]) -> DetectionResult:
        # Step 1: rule-based 快路径
        rule_result = self._rule_detect(column_names)
        if rule_result.confidence > 0.85:
            return rule_result
        
        # Step 2: LLM fallback
        return await self._llm_detect(sample_rows, column_names)
    
    def _rule_detect(self, column_names: List[str]) -> DetectionResult:
        """硬编码规则, 覆盖 70% 常见 case."""
        col_set = set(column_names)
        
        # bill_flow 特征
        if any(c in col_set for c in ["账单号", "结账号", "订单号"]) and \
           any(c in col_set for c in ["营业日期", "日期", "transaction_date"]) and \
           any(c in col_set for c in ["商品信息", "菜品明细"]):
            return DetectionResult(FileShape.BILL_FLOW, 0.95, "rule: 账单+日期+商品 三件套")
        
        # product_summary 特征
        if any(c in col_set for c in ["商品名称", "菜品名称"]) and \
           not any(c in col_set for c in ["账单号", "订单号"]) and \
           any(c in col_set for c in ["销售金额", "营业额"]):
            return DetectionResult(FileShape.PRODUCT_SUMMARY, 0.90, "rule: 商品+金额 + 缺账单")
        
        # review 特征
        if any(c in col_set for c in ["评分", "星级", "rating"]) and \
           any(c in col_set for c in ["评论", "评价", "review_text"]):
            return DetectionResult(FileShape.REVIEW, 0.92, "rule: 评分+评论文本")
        
        # finance 特征
        if any(c in col_set for c in ["凭证号", "voucher_no"]) and \
           any(c in col_set for c in ["科目", "subject"]):
            return DetectionResult(FileShape.FINANCE, 0.88, "rule: 凭证+科目")
        
        # inventory 特征
        if any(c in col_set for c in ["库存量", "盘点量", "stock_qty"]) and \
           any(c in col_set for c in ["食材", "原材料", "ingredient"]):
            return DetectionResult(FileShape.INVENTORY, 0.85, "rule: 库存+食材")
        
        # 不确定
        return DetectionResult(FileShape.UNKNOWN, 0.50, "rule: 无明显特征, 需 LLM")
    
    async def _llm_detect(self, sample_rows, column_names) -> DetectionResult:
        """用 qwen-plus 看前 5 行 + 列名."""
        prompt = f"""
你是餐饮数据形态识别专家. 给定 Excel/CSV 列名 + 前 5 行样本, 判断它是哪种形态:

支持的形态:
1. bill_flow (账单流水): 每行=一笔顾客订单, 含账单号+日期+菜品组合
2. product_summary (商品销售汇总): 每行=一道菜在某段时间的总销售
3. review (评论): 含评分+评论文本
4. finance (财务凭证): 含凭证号+科目+收支
5. inventory (库存盘点): 含食材+库存量
6. schedule (排班): 含员工+班次+日期
7. member (会员): 含会员 ID+储值
8. unknown: 都不是

列名: {column_names}
前 5 行样本: {sample_rows[:5]}

返回 JSON: {{"shape": "<one of above>", "confidence": <0-1>, "reasoning": "<简要原因>"}}
"""
        response = await self._llm_client.chat(prompt, model="qwen-plus")
        result = json.loads(response)
        return DetectionResult(
            shape=FileShape(result["shape"]),
            confidence=result["confidence"],
            reasoning=f"LLM: {result['reasoning']}",
        )
```

**置信度阈值**:
- ≥ 0.85: 自动路由
- 0.50-0.85: 进 admin 审核队列, 客户/内部确认
- < 0.50: 标 UNKNOWN, 用户必须手工选

### 3.3 Shape Router

```python
# backend/python/smartbi/canonical/shape_router.py

WRITER_REGISTRY = {
    FileShape.BILL_FLOW: "backfill_silver",                    # 现有
    FileShape.PRODUCT_SUMMARY: "product_summary_writer",       # B 新增
    FileShape.REVIEW: "review_writer",                         # B 新增
    FileShape.FINANCE: "finance_writer",                       # B 新增
    FileShape.INVENTORY: "inventory_writer",                   # B 新增
    FileShape.SCHEDULE: None,                                  # C 后扩展
    FileShape.MEMBER: None,                                    # C 后扩展
    FileShape.UNKNOWN: None,                                   # 进 admin 队列
}

async def route_upload(upload_id: int, factory_id: str, pool) -> dict:
    """识别 + 路由 + 写 Silver. 返回执行 summary."""
    # 1. 取 sample
    async with pool.acquire() as conn:
        cols = await conn.fetch(
            "SELECT original_name FROM smart_bi_pg_field_definitions WHERE upload_id=$1",
            upload_id
        )
        rows = await conn.fetch(
            "SELECT row_data FROM smart_bi_dynamic_data WHERE upload_id=$1 LIMIT 5",
            upload_id
        )
    
    # 2. 识别形态
    detector = ShapeDetector()
    result = await detector.detect([r["row_data"] for r in rows], [c["original_name"] for c in cols])
    
    # 3. 路由
    writer_name = WRITER_REGISTRY.get(result.shape)
    if writer_name is None or result.confidence < 0.85:
        # 进 admin 队列
        await _queue_for_admin(upload_id, result)
        return {"routed_to": None, "shape": result.shape.value, "confidence": result.confidence, "queued_for_admin": True}
    
    # 4. 调 writer
    writer = get_writer(writer_name)
    write_summary = await writer.write(upload_id, factory_id, pool)
    
    return {
        "routed_to": writer_name,
        "shape": result.shape.value,
        "confidence": result.confidence,
        "write_summary": write_summary,
    }
```

---

## 4. 4 个新 Silver Writer

每个 writer 独立模块. 用法:
```python
from smartbi.canonical.writers import ProductSummaryWriter
await ProductSummaryWriter().write(upload_id, factory_id, pool)
```

### 4.1 ProductSummaryWriter

**目标 schema**: `agg_product_period` (新表)

```sql
CREATE TABLE agg_product_period (
    id BIGSERIAL PRIMARY KEY,
    factory_id VARCHAR(50) NOT NULL,
    upload_id BIGINT REFERENCES smart_bi_pg_excel_uploads(id),
    product_id BIGINT REFERENCES dim_product(product_id),
    store_id BIGINT REFERENCES dim_store(store_id),
    period_start DATE,                   -- Sheet Merger 推断 (§5)
    period_end DATE,
    qty_sold NUMERIC(15,4),
    revenue NUMERIC(15,2),
    avg_unit_price NUMERIC(10,2),
    refund_qty NUMERIC(15,4) DEFAULT 0,
    refund_amount NUMERIC(15,2) DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(factory_id, upload_id, product_id, store_id, period_start)
);
ALTER TABLE agg_product_period ENABLE ROW LEVEL SECURITY;
ALTER TABLE agg_product_period FORCE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON agg_product_period
  USING (factory_id = current_setting('app.factory_id', true));

CREATE INDEX idx_app_factory_period ON agg_product_period (factory_id, period_start, period_end);
CREATE INDEX idx_app_factory_product ON agg_product_period (factory_id, product_id);
```

**Writer 实现**:

```python
# backend/python/smartbi/canonical/writers/product_summary_writer.py
class ProductSummaryWriter:
    async def write(self, upload_id: int, factory_id: str, pool) -> dict:
        """从 smart_bi_dynamic_data + field_definitions 读, resolve 实体, 写 agg_product_period."""
        # 1. 取 raw rows + field_mappings
        async with pool.acquire() as conn:
            rows = await conn.fetch(
                "SELECT row_data FROM smart_bi_dynamic_data WHERE upload_id=$1",
                upload_id
            )
            # ... 取 field_definitions 推 ALIAS 映射
        
        # 2. 实体解析 (用 EntityResolutionOrchestrator)
        orchestrator = EntityResolutionOrchestrator(pool, [DeterministicAgent(), EmbeddingAgent(...)])
        
        # 3. 推 period (用 Sheet Merger 写入的 merge_inferred_period_*)
        period = await self._infer_period(upload_id, conn)
        
        # 4. 写 agg_product_period
        for row in rows:
            store_id = await self._resolve_store(row, factory_id, orchestrator)
            product_id = await self._resolve_product(row, factory_id, orchestrator)
            qty = row.get("单卖数量") or row.get("销量") or 0
            revenue = row.get("销售金额") or 0
            await conn.execute("""
                INSERT INTO agg_product_period 
                  (factory_id, upload_id, product_id, store_id, period_start, period_end, qty_sold, revenue, avg_unit_price)
                VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
                ON CONFLICT (factory_id, upload_id, product_id, store_id, period_start) DO UPDATE SET
                  qty_sold = EXCLUDED.qty_sold,
                  revenue = EXCLUDED.revenue
            """, factory_id, upload_id, product_id, store_id, period.start, period.end, qty, revenue, revenue/qty if qty else 0)
        
        return {"rows_written": len(rows), "store_count": ..., "product_count": ...}
```

### 4.2 ReviewWriter

**目标 schema**: `dim_review_summary` + `fact_review_event`

```sql
-- 评论汇总 (按 product/store 维度)
CREATE TABLE dim_review_summary (
    id BIGSERIAL PRIMARY KEY,
    factory_id VARCHAR(50) NOT NULL,
    product_id BIGINT REFERENCES dim_product(product_id),  -- nullable: 综合评价
    store_id BIGINT REFERENCES dim_store(store_id),
    period_start DATE,
    period_end DATE,
    avg_rating NUMERIC(3,2),
    total_count INT,
    positive_count INT,
    negative_count INT,
    top_keywords JSONB,                  -- ["味道好", "服务慢", ...]
    created_at TIMESTAMP DEFAULT NOW()
);

-- 单条评论 (可选, 评论量少时存原文)
CREATE TABLE fact_review_event (
    id BIGSERIAL PRIMARY KEY,
    factory_id VARCHAR(50) NOT NULL,
    upload_id BIGINT,
    product_id BIGINT,
    store_id BIGINT,
    rating NUMERIC(3,2),
    review_text TEXT,
    review_date DATE,
    created_at TIMESTAMP DEFAULT NOW()
);

-- RLS + indexes 略 (同 pattern)
```

**Writer 关键**: 用 LLM 抽 top_keywords (5-10 个), 计算 avg_rating, positive/negative count.

### 4.3 FinanceWriter

**目标 schema**: `fact_finance_voucher`

```sql
CREATE TABLE fact_finance_voucher (
    id BIGSERIAL PRIMARY KEY,
    factory_id VARCHAR(50) NOT NULL,
    upload_id BIGINT,
    voucher_no VARCHAR(100),
    voucher_date DATE,
    subject_name VARCHAR(200),           -- 科目 (现金/应收/营业收入)
    debit_amount NUMERIC(15,2) DEFAULT 0,
    credit_amount NUMERIC(15,2) DEFAULT 0,
    description TEXT,
    store_id BIGINT REFERENCES dim_store(store_id),  -- nullable: 总部
    created_at TIMESTAMP DEFAULT NOW()
);
-- RLS + indexes 略
```

### 4.4 InventoryWriter

**目标 schema**: `fact_inventory_snapshot`

```sql
CREATE TABLE fact_inventory_snapshot (
    id BIGSERIAL PRIMARY KEY,
    factory_id VARCHAR(50) NOT NULL,
    upload_id BIGINT,
    ingredient_id BIGINT REFERENCES dim_ingredient(ingredient_id),
    store_id BIGINT REFERENCES dim_store(store_id),
    snapshot_date DATE,
    stock_qty NUMERIC(15,4),
    unit VARCHAR(20),
    safe_stock_qty NUMERIC(15,4),        -- 安全库存
    reorder_point NUMERIC(15,4),
    created_at TIMESTAMP DEFAULT NOW()
);
-- RLS + indexes 略
```

### 4.5 RLS 强制 + CI 检查

每个新表必须 `FORCE ROW LEVEL SECURITY` + tenant_isolation policy. CI 加自动检查:

```python
# backend/python/scripts/check_rls.py (CI 脚本)
async def check_all_tables_have_rls(pool):
    """所有 dim_*/fact_*/agg_* 表必须有 FORCE RLS + factory_id policy."""
    rows = await pool.fetch("""
        SELECT c.relname AS table_name, c.relforcerowsecurity AS force_rls
          FROM pg_class c
         WHERE c.relname LIKE 'dim_%' OR c.relname LIKE 'fact_%' OR c.relname LIKE 'agg_%'
    """)
    failures = [r["table_name"] for r in rows if not r["force_rls"]]
    if failures:
        raise SystemExit(f"❌ 表缺 FORCE RLS: {failures}")
```

---

## 5. Sheet Merger (`merge_status`)

(从 A v1.4 §11 继承, 这里是 B 实施详情)

### 5.1 Schema migration

```sql
-- migration: 2026_04_25_sheet_merger.sql

ALTER TABLE smart_bi_pg_excel_uploads
  ADD COLUMN merge_status VARCHAR(50) DEFAULT NULL,
  ADD COLUMN merge_target_id BIGINT REFERENCES smart_bi_pg_excel_uploads(id),
  ADD COLUMN merge_inferred_period_start DATE,
  ADD COLUMN merge_inferred_period_end DATE;

-- merge_status 取值:
--   NULL              = 正常, A capability 包含此 upload
--   'SUPERSEDED'      = 同时间段被新版覆盖, A 不再 union 此 upload 字段
--   'MERGED_INTO_<id>' = 与另一 upload 合并 (id 是 target_id)

CREATE INDEX idx_uploads_merge_status_active 
  ON smart_bi_pg_excel_uploads(factory_id, merge_status)
  WHERE merge_status IS NULL;

CREATE INDEX idx_uploads_period 
  ON smart_bi_pg_excel_uploads(factory_id, merge_inferred_period_start, merge_inferred_period_end)
  WHERE merge_status IS NULL;
```

### 5.2 SheetMergeAnalyzer 触发位置

```
upload 完成 (smart_bi_pg_excel_uploads.upload_status = 'COMPLETED')
   ↓
field_definitions detection (现有 excel_async.py 逻辑) — 必须先跑完
   ↓
SheetMergeAnalyzer.analyze(upload_id) — B 新增 (此处插入)
   ├── infer_sheet_time_window(upload_id) → period
   ├── 找同 factory 同 shape 的其它 upload (period 重叠 / 相邻 / 不重叠)
   └── 决定: 集成 / 覆盖 / 独立; 写 merge_status / merge_target_id
   ↓
schedule_materialization (现有, 但消费 merged 结果)
   ↓
A 的 capability calculator 自动看到 `merge_status IS NULL` 的最新结果
```

### 5.3 时间推断 (3-priority fallback)

详见 A v1.4 §11.2 完整伪代码. 关键: 必须在 field_definitions detection 后跑, ALIAS lookup raw column → canonical 'date'.

### 5.4 决策矩阵

```python
# backend/python/smartbi/canonical/sheet_merger.py
async def decide_merge(new_upload, factory_id, conn):
    """对新 upload 跟同 factory 同 shape 的其它 upload 比较, 决定 merge_status."""
    new_period = (new_upload.merge_inferred_period_start, new_upload.merge_inferred_period_end)
    
    # 1. 找同 factory 同 shape 的活跃 (merge_status IS NULL) upload
    others = await conn.fetch("""
        SELECT id, merge_inferred_period_start AS p_start, merge_inferred_period_end AS p_end, created_at
          FROM smart_bi_pg_excel_uploads
         WHERE factory_id = $1
           AND id != $2
           AND merge_status IS NULL
           AND detected_table_type = $3   -- 同 shape
    """, factory_id, new_upload.id, new_upload.detected_table_type)
    
    for other in others:
        other_period = (other["p_start"], other["p_end"])
        relation = compare_periods(new_period, other_period)
        
        if relation == "EQUAL" or relation == "SUPERSET":
            # 新版覆盖旧版
            await conn.execute("""
                UPDATE smart_bi_pg_excel_uploads SET merge_status = 'SUPERSEDED' WHERE id = $1
            """, other["id"])
        elif relation == "DISJOINT":
            # 不重叠, 各自独立, 不 merge
            pass
        elif relation == "OVERLAP":
            # 部分重叠, 标 MERGED_INTO_<new_id>, 客户裁决
            await conn.execute("""
                UPDATE smart_bi_pg_excel_uploads
                SET merge_status = $1, merge_target_id = $2 WHERE id = $3
            """, f'MERGED_INTO_{new_upload.id}', new_upload.id, other["id"])
            await _queue_for_admin_review(other["id"], new_upload.id, "PERIOD_OVERLAP")
```

---

## 6. 标记数据集 SOP (B1 末尾, 决定是否进 B2)

(从 roadmap §4.2.1 + A 审计 NS-2 继承)

### 6.1 Sources

| 实体 | 真实样本 (从 prod) | 合成扰动 | 总计 |
|---|---|---|---|
| 店名 | 250 (从 dim_store 149 行随机抽 + 重复) | 250 (简繁/空白/语序/缩写, 程序生成) | 500 |
| 菜名 | 250 (从 dim_product 2,703 行抽样) | 250 (一吃/二吃/招牌/特价 perturbations) | 500 |
| 人名 | 0 (dim_staff 实际 0 行) | 200 (全合成, 偏旁/拼音/外号) | 200 |

### 6.2 双盲流程

```
Day 1: 数据准备
  - 真实样本随机抽 + 程序生成扰动
  - 输出 entity_resolution_labels 表 (新建), 列: 
    pair_id, factory_id, entity_type, name_a, name_b, gold_label

Day 2-3: 标注
  - 2 名标注者 (内部团队) 独立标 yes / no / maybe
  - UI 复用现有 admin queue 模式 (从 dim_discount.parsed_ok 演变)
  - 标完写入 entity_resolution_labels.label_a / label_b

Day 4: 裁决 + split
  - label_a == label_b → gold_label
  - label_a != label_b → 第三人裁决 (PM 或 senior dev)
  - 一致率 < 90% → SOP 不清晰, 重做
  - 60/20/20 split: train (调阈值) / dev (验证 baseline) / holdout (M2 smoke gate 用, 永不看)
```

### 6.3 量化阈值

| 实体 | dev set 准确率 | holdout set 准确率 (smoke gate) |
|---|---|---|
| 店名 | ≥ 90% (B1 ship) / ≥ 92% (B2 ship) | ≥ 88% (B1) / ≥ 90% (B2) |
| 菜名 | ≥ 90% / ≥ 92% | ≥ 88% / ≥ 90% |
| 人名 | ≥ 85% / ≥ 88% | ≥ 80% / ≥ 85% (合成数据更难) |

不达标 → 不 ship, 进 B2 升级.

---

## 7. 并发模型 (advisory_xact_lock + asyncio.Lock + cache 失效)

(从 A v1.4 §4.2.2 继承)

### 7.1 三层防御

```python
# backend/python/smartbi/canonical/concurrency.py
import asyncio
import hashlib
from collections import defaultdict

_factory_locks: dict[str, asyncio.Lock] = defaultdict(asyncio.Lock)

async def with_factory_serialization(factory_id: str, conn, work):
    """三层防御: asyncio.Lock + pg_advisory_xact_lock + cache invalidation.
    
    Layer 1 (asyncio.Lock): Python 进程内, 防 同 factory 任务并发跑同步函数
    Layer 2 (pg_advisory_xact_lock): Postgres 层, 防 multi-worker 并发
    Layer 3 (cache invalidate): 清 dim_resolver 内存缓存, 防 stale entity_id
    """
    async with _factory_locks[factory_id]:           # Layer 1
        lock_key = int(hashlib.md5(factory_id.encode()).hexdigest()[:8], 16) % (2**31)
        async with conn.transaction():
            await conn.execute("SELECT pg_advisory_xact_lock($1)", lock_key)  # Layer 2
            result = await work(conn)
        # Layer 3: 清 in-memory cache (现有 dim_resolver 有 cache 不线程安全)
        from smartbi.canonical.dim_resolver import clear_cache
        clear_cache(factory_id)
        return result
```

### 7.2 在哪里用

```python
# backend/python/smartbi/canonical/shape_router.py
async def route_upload(upload_id, factory_id, pool):
    async with pool.acquire() as conn:
        await with_factory_serialization(factory_id, conn, 
            lambda c: _route_upload_inner(upload_id, c)
        )
```

包裹整个 entity resolution + sheet merger + writer 写入. 同 factory 上传必须串行, 不同 factory 可以并行.

---

## 8. Admin UI (低置信度配对裁决)

### 8.1 现有 pattern 复用

`dim_discount.parsed_ok` admin queue 模式已经存在 (从 Apr 23 `field_registry` migration). B 扩展:

```typescript
// web-admin/src/views/system/data-fabric/entity-resolution-queue.vue
// 显示低置信度实体配对, 客户人工 confirm / reject

interface ResolutionPair {
  pairId: number;
  rawName: string;          // 用户上传的
  candidateName: string;    // 系统建议匹配的
  candidateEntityId: number;
  confidence: number;
  decidedByAgent: string;
  reasoning: string;
}

// 操作:
// - Confirm: 写 dim_{type}.normalized_name, mapper_method='manual'
// - Reject: 创建新 entity, mapper_method='manual'
// - Defer: 留队列, 之后看
```

### 8.2 通知

每日凌晨 cron 扫一次, 队列 > 10 个未处理时邮件 admin.

---

## 9. 4 个 MVP 模板 (3-4 天/个)

(从 A v1.4 §5 继承 NS-1 修正后的工作量)

| Shape | MVP 模板 code | 描述 | requires |
|---|---|---|---|
| product_summary | `product_summary_top_n` | 商品销售 Top 10 | `[store_name, product_name, revenue]` |
| review | `review_sentiment_summary` | 评分趋势 + Top 关键词 | `[rating, review_text]` |
| finance | `monthly_revenue_expense` | 月度收支 | `[voucher_date, debit_amount, credit_amount, subject_name]` |
| inventory | `inventory_alert` | 库存预警 (低于 safe_stock_qty) | `[ingredient_name, stock_qty, safe_stock_qty]` |

每个模板模版同现有 `dish_sales_top_n.py` (168 行 Python + 测试 + ECharts config + applies()), 估 3-4 天/个.

**注意 (A v1.4 §13 S-v3-5)**: 这 4 个模板会引入新 canonical 字段 (`product_name`, `rating`, `review_text`, `voucher_date`, `debit_amount`, `credit_amount`, `subject_name`, `ingredient_name`, `stock_qty`, `safe_stock_qty`). 必须**同步**:
1. 扩展 `smartbi/canonical/aliases.py` 的 `ALIAS_TO_ATTR` (中文 → canonical 映射)
2. 扩展 `_generate_cta` 字典加这些字段的中文 hint
3. CI 加 coverage 检查 (新 canonical 字段必须有 ALIAS + CTA hint)

---

## 10. 实施分阶段计划

### Phase 0: 标注数据准备 (Day 0-3, 4 天)

| 天 | 任务 |
|---|---|
| Day 0 | 从 prod 抽 250 真实店名/菜名 + 生成 250 扰动 + 200 合成人名 |
| Day 1 | 建 `entity_resolution_labels` 表, 双人独立标 yes/no/maybe |
| Day 2 | 第三人裁决, 60/20/20 split |
| Day 3 | 训练集 → dev 集验证 manually 推 baseline 阈值 |

### Phase 1: B1 Baseline (Day 4-13, 10 天)

| 天 | 任务 |
|---|---|
| Day 4-5 | DeterministicAgent (升级 alias_normalizer) + 单测 |
| Day 6-7 | EmbeddingAgent (Qwen embedding API + 缓存 + 余弦) + 单测 |
| Day 8 | EntityResolutionOrchestrator (2-agent 版) + 集成测试 |
| Day 9-10 | Shape Detector (rule + LLM hybrid) + ShapeRouter + 单测 |
| Day 11 | 4 个 Silver writer 骨架 + RLS + RLS CI 检查 |
| Day 12 | Sheet Merger (`merge_status` migration + 时间推断 + decide_merge) |
| Day 13 | 并发模型 (advisory_xact_lock + asyncio.Lock + cache invalidate) + 集成测试 |

### Phase 2: B1 dev set 验证 + 决策 (Day 14-15, 2 天)

| 天 | 任务 |
|---|---|
| Day 14 | 跑 dev set: 店名/菜名/人名各 100 对 |
| Day 15 | 评估: ≥ 90/90/85 → ship B1 跳 Phase 3; 否则进 Phase 3 |

### Phase 3 (条件): B2 升级 (Day 16-25, 10 天) — 仅 B1 不达标时跑

| 天 | 任务 |
|---|---|
| Day 16-17 | ContextualAgent (店名+地理 / 菜名+品类 上下文 embed) |
| Day 18-20 | TransitiveAgent + `entity_resolution_history` 表 + union-find 算法 |
| Day 21-22 | LLMArbitrator (qwen-plus 仲裁 + prompt 调优) |
| Day 23 | Orchestrator 升级 5-agent + 集成测试 |
| Day 24 | dev set 重测; 不达标 → 急停, 重设计 |
| Day 25 | 评估: ≥ 92/92/88 → ship B2; 否则继续调 |

### Phase 4: 4 MVP 模板 (Day 26-37, 12 天 = 3-4 天/个)

| 天 | 任务 |
|---|---|
| Day 26-29 | ProductSummaryWriter + product_summary_top_n 模板 + ALIAS 扩展 |
| Day 30-33 | ReviewWriter + review_sentiment_summary 模板 + LLM 关键词抽取 |
| Day 34-36 | FinanceWriter + monthly_revenue_expense 模板 |
| Day 37 | InventoryWriter + inventory_alert 模板 (库存模板较简单) |

### Phase 5: 集成 + 测试 + 部署 (Day 38-44, 7 天)

| 天 | 任务 |
|---|---|
| Day 38 | Admin UI (低置信度配对裁决页) |
| Day 39 | E2E 7 项 smoke (M2) + 回归现有 44 模板 |
| Day 40 | Holdout set 验证 (M2 smoke gate 6) |
| Day 41 | Test 环境部署 + 24h soak |
| Day 42 | Prod 灰度 (F001 + RES_3101_009 同 A 灰度名单) |
| Day 43-44 | Monitor + bugfix |

**总计**: Day 0-44 = **5.5-7 周** (Phase 3 不跑则 5 周; 跑则 7 周)
**+ Phase 0** 4 天可并行 Phase 1 = **5-7 周** 实际墙钟时间

与 roadmap §1 的 5.5-8 周一致.

---

## 11. 测试计划

### 11.1 Python 单元测试

每个 agent + writer + Sheet Merger 独立单测:

```python
# tests/test_entity_resolution_orchestrator.py
async def test_deterministic_first_then_embedding(pool):
    """同名走 Deterministic, 不同写法走 Embedding."""
    
async def test_admin_queue_when_all_low_confidence(pool):
    """全部 agent 都低置信度 → 进 admin 队列, 不 ship."""

async def test_holdout_accuracy_threshold(pool, holdout_dataset):
    """跑 100 对 holdout, 准确率 ≥ 88%."""
    
# tests/test_shape_detector.py
async def test_rule_detect_bill_flow():
async def test_rule_detect_product_summary():
async def test_llm_fallback_for_ambiguous():

# tests/test_sheet_merger.py
async def test_supersede_same_period():
async def test_disjoint_periods_independent():
async def test_overlap_queues_admin():

# tests/test_silver_writers.py
async def test_product_summary_writer_writes_agg():
async def test_review_writer_extracts_keywords():
# ... etc
```

### 11.2 集成测试

```python
# tests/test_b_e2e.py
async def test_upload_xmx_product_summary_full_flow(pool):
    """完整: 上传 xmx_real.csv → ShapeDetector 识别 → ProductSummaryWriter → 实体解析 → 写 agg_product_period."""
    
async def test_concurrent_uploads_same_factory_serialized(pool):
    """同 factory 2 个 upload 并发, 验证 advisory_xact_lock 串行执行, 不出 dim 重复."""
    
async def test_sheet_merger_supersedes_old_version(pool):
    """上传 sheet1 (2025-01) 后再上 sheet2 (同 2025-01 修正版), sheet1.merge_status='SUPERSEDED'."""
```

### 11.3 E2E (Playwright)

```javascript
// tests/e2e-comprehensive/data-fabric-b.mjs

// M2 smoke verification (7 项):
// 1. 上传 3 种不同写法的"唏嘛香金虹桥店" → 跨上传识别为同 store_id (准确率 ≥ 88% on holdout)
// 2. 上传"招牌青花椒鱼(一吃)/(二吃)/(三吃)" → 归一化为同 product_id
// 3. 上传评论 xlsx → ShapeDetector 识别 'review', 路由到 ReviewWriter, dim_review_summary 有数据
// 4. 上传财务凭证 xlsx → 识别 'finance', 路由到 FinanceWriter
// 5. Admin UI: 低置信度配对队列出现, 人工 confirm 后入 mapper_method='manual'
// 6. Holdout 准确率: 店名 ≥ 88% / 菜名 ≥ 88% / 人名 ≥ 80%
// 7. 并发回归: 同 factory 2 upload 并发, dim_store/product 无重复行
```

---

## 12. 风险与未决问题

### 12.1 风险

| # | 风险 | 缓解 |
|---|---|---|
| B1 | 标记数据集质量不达 90% 一致率 | SOP 重做 + 增加裁决人数 + 加示例规范 |
| B2 | LLM 形态识别准确率不稳定 | rule-based 快路径优先, LLM 仅 fallback; admin 队列兜底 |
| B3 | dim_staff 0 行 → 人名实体解析样本完全合成 | 预期: 真实数据上线后准确率会跌 5-10pp, 进 B2 升级 |
| B4 | 4 个新 Silver writer 引入 5 张新表, RLS 漏配 | CI 自动 RLS 检查 (§4.5) |
| B5 | Sheet Merger 时间推断错误率高 | admin 裁决兜底 + 时间窗 < 30 天才自动合并, 否则进队列 |
| B6 | TransitiveAgent 算闭包死循环 | 限制 hop 深度 ≤ 3, history 表加 created_at 限定时间窗 |
| B7 | 并发模型在多 worker 时 advisory_xact_lock 失效 (Postgres 内存锁不跨 worker) — 实际上 Postgres advisory_lock 是跨 connection 的, 没问题 | 已验证 |
| B8 | LLM 成本超预算 | 复用 `agent_budget_daily` cap, 灰色区每日 < 1000 调用 |

### 12.2 未决问题 (送审用户)

1. **B1 阈值是否调整**: 当前 dev set 店名/菜名 ≥ 90%, 人名 ≥ 85%. 是否接受这个数字? 太严格会触发 B2 (额外 1-1.5 周), 太松准确率不足.

2. **审核队列通知机制**: 每日 cron 扫 + 邮件 / 实时 webhook / Slack? (建议每日 cron, MVP)

3. **schedule + member 形态的优先级**: 留 C 后扩展, 还是 B 一起做? (建议留 C, B 已经够重)

4. **LLM 模型选型 for ContextualAgent embedding**: 仍用 text-embedding-v3 (1024 dim), 还是更大的 m3e-base (768 dim, 中文专用)? (建议 v3, 因为 DashScope 已集成)

5. **`detected_table_type` 列**: 现有 `smart_bi_pg_excel_uploads` 已有这列 (rule-based detector 写). B 是覆盖原值还是写新列? (建议覆盖, 用 ShapeDetector v2 替换原 RestaurantRuleDetector)

---

## 13. 已知未决 / 留给 implementer

| # | 问题 | 实施时如何处理 |
|---|---|---|
| BU1 | dim_resolver in-memory cache 在多 worker 时不一致 | A v1.4 §3.4 提到 — 当前 worker=1 OK, 后期切 Redis |
| BU2 | EmbeddingAgent cache TTL 策略 | 默认不限, 模型升级时清; 实施时加 model_version 字段 |
| BU3 | LLM rate limit (DashScope 配额) | 复用 `agent_budget_daily` 但 token 计算更细 |
| BU4 | Sheet Merger 撤销操作 | 客户后悔合并: 加 admin "回滚" 按钮, 改 merge_status 回 NULL |
| BU5 | `entity_resolution_labels` 表的 RLS | 应该是 internal (不分租户), implementer 决定 |
| BU6 | C 阶段升级 merge_status → field_provenance 的兼容路径 | C spec 时再写 migration |
| BU7 | B2 升级触发条件可调性 (假如客户感觉准确率差但 B1 dev set 显示达标) | 加 admin 强制升级 button, 触发 Phase 3 |
| BU8 | DashScope 宕机时 EmbeddingAgent / LLMArbitrator fallback | 复用 Apr 25 `call_chain` migration 模式; degrade 到 deterministic only + admin 队列加大 |

---

## 14. 与 Roadmap + A spec 的对应

| 来源 | 引用章节 | B spec 章节 |
|---|---|---|
| roadmap §4.2 B 范围 | §1, §2, §3, §4 | 全部 |
| roadmap §4.2.1 标注数据集 | 标注 SOP | §6 |
| roadmap §4.2.2 并发模型 | 并发 | §7 |
| A v1.4 §11 Sheet Merger | merge_status migration + 时间推断 | §5 |
| A v1.4 §13 S-v3-5 字段扩展 | _generate_cta + ALIAS 同步 | §9 注解 |
| roadmap §6 依赖图 | B → C → D 串行 | §1.2 |
| roadmap §8 风险 #1-2 (实体解析准确率) | B1/B2 渐进 + 量化阈值 | §2.1, §6.3 |
| roadmap §9 LLM 成本 | EmbeddingAgent ¥0.7/月 + LLMArbitrator ¥120/月 | §2.3 |

---

## 15. 后续步骤

B spec v1.0 通过审核 → 启动 C spec (`04-C-字段血统与继承.md`).

C spec 必须包含:
- field_provenance 表 schema (cell-level)
- 升级 B 的 `merge_status` → `field_provenance` cascade
- 冲突解决规则 (优先级 + 时间窗 + 客户裁决)
- 继承 cascade engine
- Trust Indicator UI

B 实施 chat (新 chat) 内部 subagent 并行: agents (5 个) / writers (4 个) / detector / merger / 测试 / 标注数据集.

---

**文档状态**: v1.0 待审核
