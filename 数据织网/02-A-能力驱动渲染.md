# Sub-Project A: 能力驱动渲染契约层 (Capability Contract Layer)

**所属**: 数据织网 (Data Fabric) 4 个 Sub-Project 之 A
**版本**: v1.1
**日期**: 2026-04-25
**关联**: `01-总览路线图.md` v1.2
**工作量**: **2-2.5 周** (单工, v1.1 校准, 与 roadmap §1 同步)
**关键依赖**: 无 (可与 B 并行启动)
**关键产出**: A 是 B/C/D 的契约层, 后三者完成新功能时**必须**填 A 的 metadata

---

## 修订记录

- **v1.0 (2026-04-25 下午)**: 初稿
- **v1.1 (2026-04-25 晚上)**: 经 superpowers:code-reviewer 第一轮审计 + 后端真实情况验证后修订:
  - **C1 manifest 鸿沟** — `card_status` 改在 FE 计算, 不需要 ship manifest 到后端
  - **C2 env 前缀** — `VUE_APP_*` → `VITE_*` (验证: 项目已用 `VITE_API_BASE_URL`)
  - **C3 cache desync** — 当前 prod **uvicorn worker=1** (已查 systemd), per-process dict 安全; 未来 scale 时换 Redis (加 §3.4 注解)
  - **C4 RLS 泄露** — `set_config(..., true)` + `async with conn.transaction():` 包裹
  - **C5 ALIAS import** — 搬 `_ALIAS_TO_ATTR` 到 `smartbi/canonical/aliases.py`, `scripts/backfill_silver.py` 反过来 import (verified 仅 1 个外部 importer)
  - **C6 Timeline 一致** — 2-2.5 周 (与 roadmap 校准)
  - **S1-11 全部修复** — 详见各 §
  - **NEW** — 实施 Day 1 加 ALIAS coverage pre-flight audit (用 prod 12 factories 真实 field_definitions 跑覆盖率检查, < 80% 时扩 ALIAS 表才能继续)

---

## 1. 目标与边界

### 1.1 目标

客户上传任何 CSV/xlsx 后, Dashboard / 模板 / KPI 卡片**只渲染当前数据能算的内容**, 不显示空 0 / 误导, 缺数据时友好引导客户补充.

### 1.2 在 Data Fabric 体系中的角色

A 是**契约层 (Contract Layer)**, 不是下游消费层. 即:
- A 定义全系统统一的 `requires: [field_list]` 元数据格式
- A 提供 `CapabilityCalculator(factory_id) → set[canonical_fields]` 接口
- B/C/D 在做新功能 (新 fact 表 / 新 template) 时**必须填 requires 元数据**
- A 的渲染层 pre-filter 自动决定每张卡片 / 模板是否显示

```
Bronze 层 (已 Production)
       ↓
A: Capability Contract Layer  ← 本 spec, week 1-2
   - requires: metadata schema
   - CapabilityCalculator
   - FE pre-filter + CTA
       ↓
B/C/D 完成新功能 → 自动填 A 的契约 → A 的渲染自动解锁
```

### 1.3 in-scope vs out-of-scope

**In Scope**:
- ✅ `requires:` YAML 格式定义
- ✅ Python `CapabilityCalculator` 类
- ✅ FastAPI 端点 `GET /api/smartbi/capability/{factory_id}`
- ✅ Vue composable `useCapability()` + `<CapabilityGate>` 组件
- ✅ "解锁更多分析" CTA 组件
- ✅ 现有 44 个 template + ~30 个 Dashboard 卡片打 metadata
- ✅ Admin 审计页 (列出所有 `requires` 元数据 + 当前 factory 的可用字段)

**Out of Scope (留给 B/C/D)**:
- ❌ 新 Silver writer (B)
- ❌ 实体解析 (B)
- ❌ 字段血统 (C)
- ❌ Federated Query (D)
- ❌ Java 后端 capability gate (Java 是 CRUD, 不做 UI 渲染决策)

---

## 2. `requires:` Schema 设计

### 2.1 权威 canonical 字段表

来源: `backend/python/scripts/backfill_silver.py` 的 `_ALIAS_TO_ATTR` 字典.

**v1.0 (本 sub-project) 锁定的 canonical 字段** (snake_case English):

| 字段 | 中文别名 (映射后) | 来源表 | 说明 |
|---|---|---|---|
| `store_name` | 门店, 门店名称, 店铺, shop_name | `dim_store.name` | 门店实体 |
| `source_bill_no` | 订单号, 单号, 账单号, 结账号, 外部单号 | `fact_pos_transaction.source_bill_no` | 账单 ID |
| `date` | 日期, 营业日期, transaction_date, 交易日期 | `fact_pos_transaction.date` | 营业日期 |
| `staff_name` | 收银员, 服务员, 销售员 | `dim_staff.name` | 员工 |
| `gross_amount` | 应收金额, 营业额, 原价, 商品折前金额 | `fact_pos_transaction.gross_amount` | 应收 |
| `discount_amount` | 优惠金额, 折扣金额, 折扣额 | `fact_pos_transaction.discount_amount` | 优惠 |
| `net_amount` | 实收金额, 实收额, 商品折后金额 | `fact_pos_transaction.net_amount` | 净额 |
| `actual_receive` | 收款金额, 实收 | `fact_pos_transaction.actual_receive` | 实收 |
| `customer_count` | 人数, 就餐人数, 客流量 | `fact_pos_transaction.customer_count` | 人数 |
| `table_no` | 桌号, 桌位 | `fact_pos_transaction.table_no` | 桌位 |
| `order_type` | 订单类型 | `fact_pos_transaction.order_type` | 类型 |
| `channel_origin` | 来源, 订单来源 | `fact_pos_transaction.channel_origin` | 渠道 |
| `combo_string` | 菜品明细, 商品, 商品信息, 订单明细 | `fact_pos_transaction.combo_string` | 菜品组合 |

**B 阶段会扩展** (本 spec 仅声明, 不实现):

| 字段 | 来源 | B 时机 |
|---|---|---|
| `product_name`, `qty`, `unit_price` | `fact_pos_item.*` | B-baseline |
| `payment_channel`, `payment_amount` | `fact_pos_payment.*` | B-baseline |
| `voucher_code`, `voucher_amount` | `fact_pos_discount.*` | B-baseline |
| `review_text`, `rating` | `dim_review_summary.*` | B 评论形态 |
| `voucher_value`, `voucher_face` | `fact_finance_voucher.*` | B 财务形态 |
| `inventory_item`, `stock_qty` | `fact_inventory_snapshot.*` | B 库存形态 |
| `period` | 文件名/metadata 推断的时间窗 | B 商品汇总形态 |

**v1.0 不锁定 B 阶段字段命名**, 因为 B 实施时可能微调. A 只承诺**机制可扩展** (新增字段时只需填 ALIAS 表).

### 2.2 metadata 在哪里存

3 种声明位置, 各有用途:

#### 2.2.1 Python template (现有 44 个 + B 新增)

加在 template class 作为 ClassVar:

```python
# backend/python/smartbi/services/materialized_analytics/templates/dish_sales_top_n.py
from smartbi.capability.contract import RequiresSpec

class DishSalesTopN(AnalysisTemplate):
    code = "dish_sales_top_n"
    
    # v1.0 新增: capability metadata
    requires: ClassVar[RequiresSpec] = RequiresSpec(
        all=["store_name", "combo_string"],
        any=[],  # 不需要 OR 关系
        description="按菜品销量 / 销售额排行 (Top 20)",
    )
    
    sample_queries: ClassVar[List[str]] = ["菜品销量排名", "卖得最多的菜"]
    
    def applies(self, schema: DataSchema) -> bool:
        # 现有 applies() 保留, A 不替换
        return "商品信息" in schema.field_names_set or "combo_string" in schema.canonical_fields
    
    def compute(self, backend, schema): ...
```

**与 `applies()` 的关系**:
- `applies(schema)` = 模板内部"基于本次上传的 schema 是否适用"
- `requires` = "本卡片要在 factory 视图层显示, 需要 factory 至少上传过哪些字段"
- 二者**互补不互斥**: applies() 决定是否对单次上传产生 result; requires 决定 result 是否在 UI 显示

#### 2.2.2 Vue Dashboard 卡片

每张卡 component 加 `data().requires` 字段:

```vue
<!-- web-admin/src/views/Dashboard.vue 内某个卡片组件 -->
<script setup lang="ts">
import { useCapability } from '@/composables/useCapability';

const { hasAll, available } = useCapability();

const requires = ['store_name', 'date', 'net_amount'];
</script>

<template>
  <CapabilityGate :requires="requires" card-id="avg_bill_value">
    <!-- 实际卡片内容 -->
    <KPICard label="客单价" :value="avgBillValue" />
  </CapabilityGate>
</template>
```

#### 2.2.3 Card manifest 在 FE 维护 (v1.1 修正 C1)

**v1.0 设计漏洞**: 把卡片 manifest 写到 YAML 让 Python 计算 `card_status`, 但 YAML 在 `web-admin/` 下, **永远不会 ship 到 Python**. 后端无数据可算.

**v1.1 修法**: **`card_status` 改在 FE 计算**. 后端只返 `available_fields` + `template_status` (templates 在 Python 代码里, 后端能算). FE 用本地 manifest TS 文件 + `available_fields` 自己算 card_status.

```typescript
// web-admin/src/capability/card-manifest.ts (单一 source of truth)
export interface CardManifestEntry {
  id: string;
  page: string;
  title: string;
  requires: string[];
  description?: string;
}

export const CARD_MANIFEST: CardManifestEntry[] = [
  { id: 'avg_bill_value', page: '/dashboard', title: '客单价',
    requires: ['source_bill_no', 'net_amount'],
    description: '净销售额 / 账单数' },
  { id: 'revenue_trend_30d', page: '/dashboard', title: '30 天营收趋势',
    requires: ['date', 'net_amount'] },
  { id: 'top_dishes', page: '/restaurant/analytics/menu', title: '菜品 Top 10',
    requires: ['store_name', 'combo_string'] },
  // ... ~30 个卡片
];
```

manifest 由 **CI 自动校验** (新加 lint rule):
- `<CapabilityGate :card-id="X">` 中的 X **必须**在 CARD_MANIFEST 里
- CARD_MANIFEST 中的 id **必须**有对应的 `<CapabilityGate>` 用法
- 不一致时 CI 失败

**Calculator API 不再返 card_status**, 改返**纯字段集合**:

```json
{
  "available_fields": ["store_name", "combo_string", "net_amount"],
  "template_status": { ... },          // 后端能算 (templates 在 Python)
  "unlock_suggestions": [ ... ]         // 后端能算 (基于 templates)
}
```

FE 在 useCapability composable 内算 card_status (见 §5.1 修订版).

v1.1 不上 vite plugin 自动收集, manifest 手工维护 + CI lint 兜底. v1.2 可以再做自动化.

### 2.3 RequiresSpec 类型定义

```python
# backend/python/smartbi/capability/contract.py
from dataclasses import dataclass, field
from typing import List, Optional

@dataclass(frozen=True)
class RequiresSpec:
    """声明一个卡片/模板需要哪些 canonical 字段才能渲染."""
    
    all: List[str] = field(default_factory=list)
    """所有字段必须在 factory 可用集合中. 任一缺失则隐藏."""
    
    any: List[str] = field(default_factory=list)
    """至少一个字段在 factory 可用集合中. 全缺则隐藏. 默认空表示无 OR 约束."""
    
    description: Optional[str] = None
    """人类可读说明 (用于 admin 审计页 + CTA tooltip)"""
    
    def is_satisfied_by(self, available: set[str]) -> bool:
        """判断 factory 当前可用字段集合是否满足此 spec."""
        all_ok = all(f in available for f in self.all)
        any_ok = (not self.any) or any(f in available for f in self.any)
        return all_ok and any_ok
    
    def missing_fields(self, available: set[str]) -> List[str]:
        """列出缺失字段 (用于 CTA 提示)."""
        return [f for f in self.all if f not in available]
```

```typescript
// web-admin/src/types/capability.ts
export interface RequiresSpec {
  all: string[];
  any?: string[];
  description?: string;
}
```

---

## 3. CapabilityCalculator 设计

### 3.0 ALIAS 表搬家 (v1.1 修复 C5)

**Day 1 第一件事**: 把 `_ALIAS_TO_ATTR` 从 `scripts/backfill_silver.py` 搬到 `smartbi/canonical/aliases.py` (新文件), `scripts/backfill_silver.py` 反过来 import. 1 个外部 importer (`tests/test_backfill_silver.py`) 同步改 import 路径.

**搬家不破坏现有功能**, 因为 `_ALIAS_TO_ATTR` 是纯字典. 这个 refactor 让 capability/* 模块不依赖 scripts/, 路径干净.

```python
# 新建: backend/python/smartbi/canonical/aliases.py
"""Canonical 字段别名表. 单一 source of truth."""
from __future__ import annotations
from typing import Dict

# 中文/英文别名 → canonical 字段 (snake_case English)
ALIAS_TO_ATTR: Dict[str, str] = {
    # store
    "store_name": "store_name",
    "门店": "store_name",
    "门店名称": "store_name",
    # ... (从 scripts/backfill_silver.py 搬全部 ~80 条)
}

def to_canonical(raw_column: str) -> str | None:
    """中文/英文列名 → canonical. 无映射返 None."""
    return ALIAS_TO_ATTR.get(raw_column)
```

```python
# 修改: backend/python/scripts/backfill_silver.py
# 删除 _ALIAS_TO_ATTR 定义 (line 50-131)
from smartbi.canonical.aliases import ALIAS_TO_ATTR as _ALIAS_TO_ATTR
```

```python
# 修改: backend/python/tests/test_backfill_silver.py line 27
- from scripts.backfill_silver import _ALIAS_TO_ATTR
+ from smartbi.canonical.aliases import ALIAS_TO_ATTR as _ALIAS_TO_ATTR
```

### 3.1 接口 (v1.1 修复 C4 RLS + C5 import)

```python
# backend/python/smartbi/capability/calculator.py
import asyncio
import time
from typing import Optional
import asyncpg
from fastapi import HTTPException
from smartbi.canonical.aliases import to_canonical
from smartbi.tenant_ctx import get_factory_id

class CapabilityCalculator:
    """聚合 factory 所有 upload 的 field_definitions, 返回 canonical 字段集合.
    
    Concurrency model:
    - Per-process in-memory dict cache (current prod uvicorn worker=1, OK)
    - 5 分钟 TTL + event-driven invalidate
    - Inflight Promise dedup 防 stampede
    
    多 worker scale 时切 Redis (见 §3.4).
    """
    
    def __init__(self, pool: asyncpg.Pool):
        self._pool = pool
        self._cache: dict[str, tuple[set[str], float]] = {}
        self._cache_ttl = 300  # 5 分钟
        self._inflight: dict[str, asyncio.Future] = {}  # 防 stampede
    
    async def get_capabilities(self, factory_id: str) -> set[str]:
        """返回 factory 当前可用的 canonical 字段集合."""
        # Defensive: 上下文 factory_id 必须匹配 (防 IDOR)
        ctx_fid = get_factory_id()
        if ctx_fid and ctx_fid != factory_id:
            # v1.1 修复 S9: 不 raise ValueError (会变 HTTP 500)
            raise HTTPException(status_code=403, detail="Cross-tenant access denied")
        
        # 缓存命中
        cached = self._cache.get(factory_id)
        if cached and time.time() - cached[1] < self._cache_ttl:
            return cached[0]
        
        # Inflight dedup: 多个并发请求同一 factory 时只跑一次 query
        if factory_id in self._inflight:
            return await self._inflight[factory_id]
        
        future: asyncio.Future = asyncio.Future()
        self._inflight[factory_id] = future
        try:
            result = await self._fetch_capabilities(factory_id)
            self._cache[factory_id] = (result, time.time())
            future.set_result(result)
            return result
        except Exception as e:
            future.set_exception(e)
            raise
        finally:
            self._inflight.pop(factory_id, None)
    
    async def _fetch_capabilities(self, factory_id: str) -> set[str]:
        """v1.1 修复 C4: transaction-scoped set_config + 显式 transaction 包裹."""
        async with self._pool.acquire() as conn:
            async with conn.transaction():
                # set_config(..., true) = transaction-scoped, 事务结束自动清掉
                # 不会泄露到 pool 下一个 acquirer
                await conn.execute(
                    "SELECT set_config('app.factory_id', $1, true)",
                    factory_id
                )
                rows = await conn.fetch("""
                    SELECT DISTINCT fd.original_name
                      FROM smart_bi_pg_field_definitions fd
                      JOIN smart_bi_pg_excel_uploads u ON fd.upload_id = u.id
                     WHERE u.factory_id = $1
                       AND u.upload_status = 'COMPLETED'
                """, factory_id)
        
        canonical_fields: set[str] = set()
        for row in rows:
            canonical = to_canonical(row["original_name"])
            if canonical:
                canonical_fields.add(canonical)
        return canonical_fields
    
    def invalidate(self, factory_id: str) -> None:
        """上传完成后调用, 失效缓存."""
        self._cache.pop(factory_id, None)
```

### 3.2 缓存失效时机

| 时机 | 调用 | 文件 |
|---|---|---|
| 新上传完成 (status → COMPLETED) | `calculator.invalidate(factory_id)` | `excel_async.py` `_finalize_upload()` |
| 上传删除 | 同上 | `excel_async.py` `delete_upload()` |
| 手动 admin 操作 | API: `POST /api/smartbi/capability/{factory_id}/invalidate` | 新建 `capability/api.py` |
| field_definitions 编辑 (admin 修映射) | 同 invalidate | 现有 admin 端点加 hook |

### 3.3 RLS 兼容性 (v1.1)

`smart_bi_pg_field_definitions` 表**没有 factory_id 列** (FK 到 upload). RLS 要靠 join `smart_bi_pg_excel_uploads`. SQL 已正确处理.

**v1.1 修复 C4 关键变化**: 用 `set_config(..., true)` (transaction-scoped) + 显式 `async with conn.transaction():` 包裹. 事务结束 GUC 自动清, **不会**泄露到 pool 下一个 acquirer.

如果用 `set_config(..., false)` (session-scoped) 会**留在连接上**, 下一个借连接的 tenant 看到 factory_id=A 的 RLS context, 错读 A 的数据. 这是 v1.0 的 bug.

`tenant_ctx.set_pg_connection_tenant` 是 pool setup callback, 在每次 acquire 时自动设. 但本 calculator 走显式事务, 不依赖 setup callback (二者并存不冲突).

### 3.4 多 worker scale 注意 (v1.1 新增)

**当前 prod uvicorn worker = 1** (查 `/etc/systemd/system/cretas-python.service` ExecStart 无 `--workers` 参数), per-process dict cache 安全.

**未来 scale 到 worker > 1 时**, 各 worker 的 cache 不同步, `invalidate(factory_id)` 在 worker 1 跑, worker 2-N 仍 5min 内返旧数据. 解决方案:

| 方案 | 工作量 | 何时切 |
|---|---|---|
| Redis (port 6379 已有 systemd 跑着) | 1-2 天 | scale > 1 worker 前 |
| Pub/Sub invalidate (Postgres LISTEN/NOTIFY) | 0.5 天 | 同上 |
| 缩 TTL 到 30s + 吞两次查询 | 0 天 | 临时方案 |
| 干脆不缓存 (12 factories 直接查很快) | 0 天 | 测一下查询耗时, 若 < 50ms 直接拿掉缓存 |

**推荐**: A v1.1 不上 Redis, 但 **Day 2 单测加 SLO**: cold cache 查询 p95 < 50ms; 若达标, 后期可直接拿掉缓存 (简化代码).

---

## 4. FastAPI 端点设计

### 4.1 路由 (v1.1 修复 S4 auth pattern + 移除 card_status)

**v1.1 修复**:
- 用现有 `request.state.factory_id` 模式 (auth_middleware 设置), 不发明 `verify_jwt_factory_id` 依赖
- 移除 `card_status` (改 FE 算, 见 §2.2.3 + §5.1)
- `_suggest_unlocks` 算法显式定义 (§4.2)

```python
# backend/python/smartbi/capability/api.py
from fastapi import APIRouter, HTTPException, Request, Depends
from smartbi.capability.calculator import CapabilityCalculator
from smartbi.capability.template_status import compute_template_status, suggest_unlocks

router = APIRouter(prefix="/api/smartbi/capability", tags=["Capability"])

# Calculator 单例 (依赖 main.py lifespan 创建)
_calculator: CapabilityCalculator | None = None

def get_calculator() -> CapabilityCalculator:
    if _calculator is None:
        raise HTTPException(503, "Capability service not initialized")
    return _calculator


@router.get("/{factory_id}")
async def get_capability(
    factory_id: str,
    request: Request,
    calc: CapabilityCalculator = Depends(get_calculator),
):
    """返回 factory 当前可用的 canonical 字段 + 模板解锁状态.
    
    Note: card_status 不在此返回, FE 用本地 manifest 自行计算 (见 capability-card-manifest.ts).
    """
    # 现有 auth pattern: request.state.factory_id by auth_middleware
    user_factory_id = getattr(request.state, "factory_id", None)
    if not user_factory_id:
        raise HTTPException(401, "Tenant context not set")
    if user_factory_id != factory_id:
        raise HTTPException(403, "Cross-tenant access denied")
    
    available = await calc.get_capabilities(factory_id)
    template_status = compute_template_status(available)
    suggestions = suggest_unlocks(available, template_status)
    
    return {
        "factory_id": factory_id,
        "available_fields": sorted(available),
        "template_status": template_status,
        "unlock_suggestions": suggestions,
    }


@router.post("/{factory_id}/invalidate")
async def invalidate_cache(
    factory_id: str,
    request: Request,
    calc: CapabilityCalculator = Depends(get_calculator),
):
    user_factory_id = getattr(request.state, "factory_id", None)
    if user_factory_id != factory_id:
        raise HTTPException(403)
    calc.invalidate(factory_id)
    return {"status": "ok"}
```

### 4.2 `_suggest_unlocks` 算法 (v1.1 修复 S3)

```python
# backend/python/smartbi/capability/template_status.py
from typing import Iterable
from smartbi.services.materialized_analytics.registry import all_templates

def compute_template_status(
    available: set[str]
) -> dict[str, dict]:
    """对每个已注册 template 算 (satisfied, missing) 状态."""
    status = {}
    for tpl in all_templates():
        if tpl.requires is None:
            # v1.1 修复 S2: (待定) templates 显式标 None, audit 页特殊处理
            status[tpl.code] = {"satisfied": None, "missing": [], "deferred": True}
            continue
        spec = tpl.requires
        sat = spec.is_satisfied_by(available)
        missing = spec.missing_fields(available)
        status[tpl.code] = {"satisfied": sat, "missing": missing}
    return status

def suggest_unlocks(
    available: set[str],
    template_status: dict[str, dict],
) -> list[dict]:
    """对每个未满足的 template, 按缺失字段聚合, 返回 top 5 建议.
    
    算法:
    1. 收集所有未满足 template 的 missing 字段
    2. groupby missing field → 列出能解锁的 templates
    3. 按 unlocks_count 降序排
    4. 返回 top 5, 每条带 CTA 文案 + 例子
    """
    from collections import defaultdict
    field_to_templates: dict[str, list] = defaultdict(list)
    
    for code, st in template_status.items():
        if st.get("deferred") or st.get("satisfied"):
            continue
        for missing_field in st["missing"]:
            field_to_templates[missing_field].append(code)
    
    suggestions = []
    for missing_field, template_codes in field_to_templates.items():
        if not template_codes:
            continue
        # 取头 3 个 template 当例子
        examples = template_codes[:3]
        cta = _generate_cta(missing_field, len(template_codes))
        suggestions.append({
            "missing_field": missing_field,
            "unlocks_count": len(template_codes),
            "unlocks_examples": examples,
            "cta": cta,
        })
    
    # 按 unlocks_count 降序
    suggestions.sort(key=lambda x: -x["unlocks_count"])
    return suggestions[:5]  # top 5

def _generate_cta(missing_field: str, count: int) -> str:
    """生成人类可读 CTA."""
    field_to_hint = {
        "date": "上传账单流水或交易日报",
        "source_bill_no": "上传账单流水",
        "channel_origin": "上传带'订单来源'列的数据",
        "combo_string": "上传含'商品信息'的订单数据",
        "customer_count": "上传含'就餐人数'的数据",
        # ... (扩展)
    }
    hint = field_to_hint.get(missing_field, f"上传含 '{missing_field}' 的数据")
    return f"{hint} 即可解锁 {count} 个分析"
```

**例子输出**:

```json
{
  "missing_field": "date",
  "unlocks_count": 12,
  "unlocks_examples": ["monthly_trend", "anomaly_detection", "time_slot_revenue"],
  "cta": "上传账单流水或交易日报 即可解锁 12 个分析"
}
```

### 4.3 响应格式 example (v1.1 移除 card_status)

```json
{
  "factory_id": "R_XMX_FRESH3",
  "available_fields": ["store_name", "combo_string", "net_amount"],
  "template_status": {
    "dish_sales_top_n":  {"satisfied": true,  "missing": []},
    "monthly_trend":     {"satisfied": false, "missing": ["date"]},
    "channel_analysis":  {"satisfied": false, "missing": ["channel_origin"]},
    "refund_analysis":   {"satisfied": null,  "missing": [], "deferred": true}
  },
  "unlock_suggestions": [
    {
      "missing_field": "date",
      "unlocks_count": 12,
      "unlocks_examples": ["monthly_trend", "anomaly_detection", "time_slot_revenue"],
      "cta": "上传账单流水或交易日报 即可解锁 12 个分析"
    },
    {
      "missing_field": "channel_origin",
      "unlocks_count": 3,
      "unlocks_examples": ["channel_analysis", "groupon_channel_breakdown", "promotion_impact"],
      "cta": "上传带'订单来源'列的数据 即可解锁 3 个分析"
    }
  ]
}
```

注意: `card_status` **不在响应中**, 由 FE useCapability 用本地 CARD_MANIFEST 算 (§2.2.3).

### 4.4 Java 后端如何用?

Java 后端 (Spring Boot) **不直接调用** Python capability API. 因为 Java 服务的是 CRUD endpoint, 不做 UI 渲染决策. 如果未来需要, 可加 `RestTemplate` 调用 Python 8083, 但本 spec 不实现.

---

## 5. Vue 前端集成

### 5.1 Composable (v1.1 修复 S5 inflight, S6 loading, S7 fail-open, S8 auth race)

```typescript
// web-admin/src/composables/useCapability.ts
import { ref, computed } from 'vue';
import { useAuthStore } from '@/store/auth';
import apiClient from '@/services/apiClient';
import { CARD_MANIFEST } from '@/capability/card-manifest';

interface CapabilityResponse {
  factory_id: string;
  available_fields: string[];
  template_status: Record<string, { satisfied: boolean | null; missing: string[]; deferred?: boolean }>;
  unlock_suggestions: Array<{
    missing_field: string;
    unlocks_count: number;
    unlocks_examples: string[];
    cta: string;
  }>;
}

// 单例 module-level state
const _capabilityCache = ref<CapabilityResponse | null>(null);
const _loading = ref(false);
const _error = ref<Error | null>(null);   // v1.1 修复 S7
let _inflight: Promise<CapabilityResponse | null> | null = null;  // v1.1 修复 S5

export function useCapability() {
  const authStore = useAuthStore();
  
  /**
   * Fetch capability. Dedups concurrent calls via _inflight.
   * Fail-open: 错误时返 null, hasAll/missingOf 用 fail-open 兜底.
   */
  async function fetchCapability(force = false): Promise<CapabilityResponse | null> {
    if (_capabilityCache.value && !force) return _capabilityCache.value;
    
    // v1.1 修复 S5: inflight dedup
    if (_inflight) return _inflight;
    
    // v1.1 修复 S8: auth-ready guard
    const factoryId = authStore.factoryId;
    if (!factoryId) {
      console.warn('[capability] auth not ready, skipping fetch');
      return null;
    }
    
    _loading.value = true;
    _error.value = null;
    
    _inflight = (async () => {
      try {
        const res = await apiClient.get(`/api/smartbi/capability/${factoryId}`);
        _capabilityCache.value = res.data;
        return res.data;
      } catch (e) {
        _error.value = e as Error;
        console.error('[capability] fetch failed, falling back to fail-open mode:', e);
        return null;  // fail-open
      } finally {
        _loading.value = false;
        _inflight = null;
      }
    })();
    
    return _inflight;
  }
  
  /**
   * v1.1 修复 S7 fail-open semantics:
   * - 缓存有数据 → 正常判断
   * - 缓存空 + loading → 假定满足 (避免初始 flash 隐藏)
   * - 缓存空 + error → fail-open, 假定满足 (好过整个 dashboard 空白)
   */
  const available = computed(() => {
    if (!_capabilityCache.value) return null;  // 表示"尚未确定"
    return new Set(_capabilityCache.value.available_fields);
  });
  
  function hasAll(requires: string[]): boolean {
    if (available.value === null) {
      // 加载中或错误 → fail-open
      return true;
    }
    return requires.every(f => available.value!.has(f));
  }
  
  function missingOf(requires: string[]): string[] {
    if (available.value === null) return [];
    return requires.filter(f => !available.value!.has(f));
  }
  
  /**
   * v1.1 新增: card_status 在 FE 算 (§2.2.3 修法).
   */
  const cardStatus = computed(() => {
    const status: Record<string, { satisfied: boolean; missing: string[] }> = {};
    for (const card of CARD_MANIFEST) {
      status[card.id] = {
        satisfied: hasAll(card.requires),
        missing: missingOf(card.requires),
      };
    }
    return status;
  });
  
  return {
    fetchCapability,
    available,
    hasAll,
    missingOf,
    cardStatus,
    loading: _loading,
    error: _error,
    suggestions: computed(() => _capabilityCache.value?.unlock_suggestions ?? []),
  };
}
```

**关键变化总结**:
| 修复 | v1.0 | v1.1 |
|---|---|---|
| S5 dedup | 无 | `_inflight` Promise singleton |
| S6 loading | `available={}`, hasAll 返 false → 闪空 | `available=null` 时 `hasAll` 返 **true** (fail-open) |
| S7 error | `_capabilityCache` 留 null → 全 false → 全空 | catch + log + return null + fail-open 兜底 |
| S8 auth race | `factoryId` 可能 null → API 调 `/null` | guard 检查 + log + skip |

### 5.2 CapabilityGate 组件 (v1.1 加 loading slot)

```vue
<!-- web-admin/src/components/CapabilityGate.vue -->
<script setup lang="ts">
import { computed } from 'vue';
import { useCapability } from '@/composables/useCapability';

const props = defineProps<{
  requires: string[];
  cardId: string;
  fallback?: 'hide' | 'placeholder';
}>();

const { hasAll, missingOf, available, loading } = useCapability();

const isLoading = computed(() => loading.value && available.value === null);
const satisfied = computed(() => hasAll(props.requires));
const missing = computed(() => missingOf(props.requires));
const fallbackMode = computed(() => props.fallback ?? 'hide');
</script>

<template>
  <!-- v1.1: 加载中显式渲染 loading slot, 防止页面初次加载时所有卡片闪空 -->
  <slot v-if="isLoading" name="loading">
    <!-- 默认骨架 -->
    <el-skeleton :rows="3" animated />
  </slot>
  
  <!-- 已加载且满足 → 正常渲染 -->
  <slot v-else-if="satisfied" />
  
  <!-- 已加载且不满足 + placeholder 模式 -->
  <div v-else-if="fallbackMode === 'placeholder'" class="capability-placeholder">
    <el-empty 
      :description="`此分析需上传含 ${missing.join('/')} 的数据`" 
      :image-size="60" 
    />
  </div>
  
  <!-- 已加载且不满足 + hide 模式: 渲染空 -->
</template>
```

**v1.1 修复**:
- S6 loading: 加 `isLoading` 检查, 渲染 skeleton 而非空 (Dashboard 不会闪空)
- 配合 useCapability 的 fail-open: 错误时 `hasAll` 返 true, 卡片正常显; 加载中 `available===null` 时 `isLoading` 触发 skeleton

**Slot 用法升级**:
```vue
<CapabilityGate :requires="..." card-id="...">
  <template #loading>
    <CustomSkeleton />  <!-- 可选 override 默认骨架 -->
  </template>
  <KPICard ... />        <!-- 默认 slot, 满足时渲染 -->
</CapabilityGate>
```

**默认 hide** = 缺数据时卡片消失 (Dashboard 天然只有这么多), 不显示空白误导客户.

**placeholder 模式**用于关键卡 (例如 Dashboard 的"客单价"), 即使没数据也显示占位.

### 5.3 UnlockMoreCTA 组件

```vue
<!-- web-admin/src/components/UnlockMoreCTA.vue -->
<script setup lang="ts">
import { useCapability } from '@/composables/useCapability';
import { useRouter } from 'vue-router';

const { suggestions, available } = useCapability();
const router = useRouter();

function handleUpload() {
  router.push('/smart-bi/upload');
}
</script>

<template>
  <el-card v-if="suggestions.length > 0" class="unlock-cta">
    <template #header>
      <span>✨ 解锁更多分析</span>
    </template>
    
    <p class="current-state">
      你当前数据支持 {{ available.size }} 个 canonical 字段.
    </p>
    
    <div class="suggestions">
      <div 
        v-for="(s, i) in suggestions.slice(0, 3)" 
        :key="i" 
        class="suggestion-item"
      >
        <strong>{{ s.cta }}</strong>
        <p class="examples">解锁: {{ s.unlocks_examples.slice(0, 3).join(' / ') }}</p>
      </div>
    </div>
    
    <el-button type="primary" @click="handleUpload">一键上传</el-button>
  </el-card>
</template>
```

**位置**: Dashboard 底部 / 各分析子页底部 (Trends / Restaurant Analytics 等).

### 5.4 现有 Dashboard.vue 改造范围

`web-admin/src/views/Dashboard.vue` (~600 行) 改造点:

1. setup() 顶部加 `const { fetchCapability } = useCapability(); onMounted(fetchCapability);`
2. 每个 `<el-col>` 包裹的 KPI 卡 → 包一层 `<CapabilityGate :requires="..." :card-id="...">`
3. 底部加 `<UnlockMoreCTA />`

预估改动: ~30 处 wrap, ~80 行 diff.

类似改造也要应用于:
- `views/restaurant/analytics/overview.vue`
- `views/restaurant/analytics/menu-board.vue`
- `views/analytics/trends/index.vue`
- `views/analytics/kpi/index.vue`
- 其他 Dashboard 性质页面

---

## 6. ~80 处 metadata 标注清单

### 6.1 现有 44 个 template (Python) (v1.1 修正 S2 + 时间)

每个 template class 加 `requires: ClassVar[RequiresSpec]`. **真实估时**: 20-30 分钟/模板 (读 applies → 推 requires → 交叉对照 ALIAS), 44 × 25 min = ~18 小时 (~2.5 工作日).

**v1.1 修复 S2**: `(待定)` / `(B 引入)` 模板**不阻塞 A**, 显式标 `requires=None` 表示"capability 未确定", audit 页特殊处理 (显 "deferred to B"):

```python
class RefundAnalysis(AnalysisTemplate):
    code = "refund_analysis"
    requires: ClassVar[RequiresSpec | None] = None   # B 阶段引入 refund_amount 字段后再填
    description: ClassVar[str] = "退款分析 (B 阶段启用)"
```

`compute_template_status` 对 `None` 返 `{"satisfied": null, "missing": [], "deferred": true}`, FE 不显示, audit 页归为"待 B 阶段定义".

清单 (基于现有审计, 可能微调):

| Template | requires.all |
|---|---|
| `top_n_by_dim` | `[store_name]` (动态指定 measure) |
| `monthly_trend` | `[date]` + 至少 1 个 measure |
| `category_distribution` | (依赖 schema, 可能无强制) |
| `anomaly_detection` | `[date]` + 1 measure |
| `pareto_analysis` | 1 measure + 1 dim |
| `dish_sales_top_n` | `[store_name, combo_string]` |
| `dish_slow_movers` | `[store_name, combo_string]` |
| `time_slot_revenue` | `[date, net_amount]` (or gross) |
| `dish_category_breakdown` | `[combo_string]` |
| `member_consumption` | `[customer_count, net_amount]` |
| `table_type_comparison` | `[table_no, net_amount]` |
| `staff_performance` | `[staff_name, net_amount]` |
| `store_performance` | `[store_name, net_amount]` |
| `channel_analysis` | `[channel_origin, net_amount]` |
| `promotion_impact` | `[discount_amount, gross_amount]` |
| `dish_time_slot_matrix` | `[date, combo_string]` |
| `refund_analysis` | (待定: B 引入 refund 字段) |
| `profit_loss_statement` | `[date, gross_amount, discount_amount, net_amount]` |
| `revenue_management_report` | `[date, gross_amount, net_amount]` |
| `stored_value_card_consumption` | (B 引入) |
| `groupon_channel_breakdown` | `[channel_origin, gross_amount]` |
| `business_overview_summary` | `[date, net_amount]` |
| `member_deep_analytics` | `[customer_count]` |
| `reviews_sentiment_summary` | (B 引入: review_text, rating) |
| `payment_method_mix` | (B 引入: payment_channel) |
| `kitchen_dispatch_heatmap` | `[date, combo_string]` |
| `period_comparison_trend` | `[date, net_amount]` |
| `dish_store_drill` | `[store_name, combo_string, net_amount]` |
| `monthly_anomaly` | `[date, net_amount]` |
| `weekday_weekend_pattern` | `[date, net_amount]` |
| ... (剩余 ~14 个) | |

### 6.2 Dashboard 卡片 (Vue, ~30 处)

| 卡片 (Dashboard.vue) | requires |
|---|---|
| 本月销售额 | `[date, net_amount]` |
| 订单数量 | `[date, source_bill_no]` |
| 客单价 | `[source_bill_no, net_amount]` |
| 活跃客户 | `[customer_count]` (待 B) |
| 区域销售分布 | `[store_name, net_amount]` |
| 部门排行 | `[staff_name, net_amount]` (餐饮场景下"部门"=门店) |
| 销售趋势图 | `[date, net_amount]` |
| 产品占比 | `[combo_string, net_amount]` |
| 库存预警 | (B 引入) |
| 任务完成率 | (无业务关联, 不加 requires) |

类似清单需要给:
- restaurant/analytics/overview.vue (~5 卡)
- restaurant/analytics/menu-board.vue (~6 卡)
- analytics/trends/index.vue (~4 卡)
- analytics/kpi/index.vue (~5 卡)
- finance pages (~3 卡)

合计约 30 卡 × 5 分钟 = 2.5 小时.

### 6.3 Admin 审计页

新增 `web-admin/src/views/system/data-fabric/capability-audit.vue`:

```
+-------------------------------------------+
| 当前 factory: F001                        |
| 可用 canonical 字段 (12)                  |
| store_name, source_bill_no, date, ...     |
+-------------------------------------------+
| 模板 (44)        | 满足 (38)  | 缺 (6)   |
| Dashboard 卡(30) | 满足 (24)  | 缺 (6)   |
+-------------------------------------------+
| 解锁建议:                                 |
| 上传含 channel_origin 的文件 → 解锁 3 个 |
| ...                                       |
+-------------------------------------------+
```

工作量: 4-6 小时 (用现有 Element UI 组件).

---

## 7. 实施分阶段计划

### Phase 0: ALIAS Pre-flight Audit (Day 0, ~半天) (v1.1 新增)

⚠️ **必须先做**, 否则全盘逻辑可能站不住:

```
查 prod 12 factories 的所有 smart_bi_pg_field_definitions
统计每个 original_name 出现频次
对比 ALIAS_TO_ATTR 覆盖率:
  - 如果 ≥ 80% original_name 能映射 → 继续 Day 1
  - 如果 < 80% → 必须先扩 ALIAS_TO_ATTR 表 (B 工作的子集前置)
  - 如果 < 60% → roadmap 重审, A 不能独立交付
```

理由: A 的整个逻辑基于 ALIAS_TO_ATTR. 如果客户数据里 70% 的列 ALIAS 不认识, capability 算出来全空, A 上线即等于"全 fail-open", 没价值.

### Phase 1: 基础设施 (Day 1-3)

| 天 | 任务 | 交付 |
|---|---|---|
| Day 1 | C5 重构: 搬 `_ALIAS_TO_ATTR` 到 `smartbi/canonical/aliases.py` + Python: `capability/` 模块骨架 + 单元测试空跑 | refactor + 骨架 |
| Day 2 | `CapabilityCalculator` 实现 (含 C4 RLS transaction 包裹 + S5 inflight + S7 fail-open 测试) + 单测 (覆盖空 factory / 单文件 / 多文件 / 缓存 TTL / RLS / 错误 / 并发) | 单测全绿 |
| Day 3 | FastAPI 端点 (S4 用 request.state pattern) + `compute_template_status` + `suggest_unlocks` 算法 + main.py 注册 + e2e 测试 | API prod 可调用 |

### Phase 2: 模板 metadata (Day 4-6) (v1.1 多加 1 天)

| 天 | 任务 | 交付 |
|---|---|---|
| Day 4 | 给 alphabetical 前 15 个 template 加 `requires` (按 template.code 字母序, 防 batch 划分混乱) | 15 文件 |
| Day 5 | 中间 15 个 template (S1 修复: 显式 alphabet 顺序) | 30 文件 |
| Day 6 | 剩余 14 个 + 4 个 deferred (`requires=None`) + RAG 索引 / 模板注册表更新 | 全部 44 个 |

### Phase 3: Vue 前端 (Day 7-10) (v1.1 多加 1 天)

| 天 | 任务 | 交付 |
|---|---|---|
| Day 7 | composable (S5 inflight + S6 loading + S7 fail-open + S8 auth race + S10 真 vi.mock 测试) + CapabilityGate + UnlockMoreCTA + CARD_MANIFEST | 单文件可挂载 + Vitest 真测 reactive |
| Day 8 | Dashboard.vue 改造 (30 卡 wrap) + CARD_MANIFEST 同步 + CI lint rule (检查 :card-id 与 manifest 一致) | Dashboard 跑通 |
| Day 9 | restaurant/analytics + analytics/trends + finance 改造 + UnlockMoreCTA 路由 scope (仅 analytics 路由显示) | 4-5 个页面跑通 |
| Day 10 | Admin 审计页 (含 deferred templates 区块) + CI 全绿 | 审计页可视化 |

### Phase 4: 集成 + 测试 + 部署 (Day 11-13) (v1.1 多加 1 天)

| 天 | 任务 | 交付 |
|---|---|---|
| Day 11 | E2E 5 项 + load test (S11: p95 < 200ms @ 100 concurrent) + 回归 (44 模板不能 regress) | M1 全过 |
| Day 12 | Test 环境部署 + 24h soak | test 环境绿 |
| Day 13 | Prod 灰度 (`VITE_CAPABILITY_GATE_ENABLED=true` for 50% factories via 部署变量) + 监控 | prod 绿 |

**总计**: Day 0-13 = **2.5-2.8 周** (含周末和 deploy/test/debug overhead).

**与 roadmap §1 同步**: roadmap 写 "A: 1.5-2 周", v1.1 spec 详细分解后实际 **2-2.5 周** (Day 0-13). 建议 roadmap §1 加注 "spec 校准为 2-2.5 周". 这是诚实数字, 不预留 buffer 后期会踩.

---

## 8. 测试计划

### 8.1 Python 单元测试

```python
# backend/python/tests/test_capability_calculator.py

@pytest.mark.asyncio
async def test_empty_factory_returns_empty_set(pool):
    calc = CapabilityCalculator(pool)
    result = await calc.get_capabilities("F_NEW_EMPTY")
    assert result == set()

@pytest.mark.asyncio
async def test_single_upload_returns_canonical_fields(pool, qhj_test_upload):
    calc = CapabilityCalculator(pool)
    result = await calc.get_capabilities(qhj_test_upload.factory_id)
    assert "store_name" in result
    assert "date" in result

@pytest.mark.asyncio
async def test_multiple_uploads_returns_union(pool, factory_with_2_uploads):
    calc = CapabilityCalculator(pool)
    result = await calc.get_capabilities(factory_with_2_uploads.factory_id)
    # 第一上传只有 product 字段, 第二有 date 字段 → 并集
    assert "product_name" in result
    assert "date" in result

@pytest.mark.asyncio
async def test_rls_isolation(pool, factory_a, factory_b):
    """A 的 calculator 调用不能看到 B 的 fields."""
    calc = CapabilityCalculator(pool)
    a_fields = await calc.get_capabilities(factory_a.id)
    b_fields = await calc.get_capabilities(factory_b.id)
    # 确保 b_fields 里有的 b-only 字段, 不在 a_fields 里
    assert "b_only_field" in b_fields
    assert "b_only_field" not in a_fields

@pytest.mark.asyncio
async def test_cache_ttl(pool, monkeypatch):
    """5 分钟内重复调用走缓存; 过期重查."""
    calc = CapabilityCalculator(pool)
    calc._cache_ttl = 1  # 加速测试
    
    result1 = await calc.get_capabilities("F001")
    # 修改 DB
    await pool.execute(...)
    result2 = await calc.get_capabilities("F001")
    assert result2 == result1  # 缓存命中
    
    await asyncio.sleep(1.1)
    result3 = await calc.get_capabilities("F001")
    assert result3 != result1  # 缓存过期, 重查

@pytest.mark.asyncio
async def test_invalidate_clears_cache(pool):
    calc = CapabilityCalculator(pool)
    await calc.get_capabilities("F001")
    assert "F001" in calc._cache
    calc.invalidate("F001")
    assert "F001" not in calc._cache

@pytest.mark.asyncio
async def test_inflight_dedup(pool):
    """v1.1 修复 S5: 并发同 factory 请求只跑一次 query."""
    calc = CapabilityCalculator(pool)
    
    # 用 monkeypatch 计 _fetch_capabilities 调用次数
    call_count = 0
    original = calc._fetch_capabilities
    async def counting(fid):
        nonlocal call_count
        call_count += 1
        await asyncio.sleep(0.05)  # 慢一点, 模拟真实查询
        return await original(fid)
    calc._fetch_capabilities = counting
    
    # 并发 3 个请求
    results = await asyncio.gather(
        calc.get_capabilities("F001"),
        calc.get_capabilities("F001"),
        calc.get_capabilities("F001"),
    )
    
    assert all(r == results[0] for r in results)
    assert call_count == 1  # 仅 1 次 SQL, 其它走 inflight

@pytest.mark.asyncio
async def test_rls_no_session_leak(pool):
    """v1.1 修复 C4: 跨 factory 调用不能泄露 RLS context."""
    calc = CapabilityCalculator(pool)
    
    # 1. F001 调用, 设 GUC=F001 in transaction
    await calc.get_capabilities("F001")
    
    # 2. 直接拿连接查 GUC, 应该是空 (transaction 结束已清)
    async with pool.acquire() as conn:
        gusing = await conn.fetchval("SELECT current_setting('app.factory_id', true)")
        # current_setting with second arg true 返 NULL 表示未设
        assert gusing in (None, "", "__internal__")  # 取决于 pool setup callback

@pytest.mark.asyncio
async def test_cross_tenant_blocked():
    """v1.1 修复 S9: ctx 不匹配返 403, 不是 500."""
    from smartbi.tenant_ctx import set_factory_id
    set_factory_id("F001")
    
    calc = CapabilityCalculator(pool)
    with pytest.raises(HTTPException) as exc_info:
        await calc.get_capabilities("F002")  # 不同的 factory
    assert exc_info.value.status_code == 403
```

### 8.2 Vue 组件测试 (v1.1 修复 S10: 真测 reactive)

```typescript
// web-admin/src/components/__tests__/CapabilityGate.spec.ts
import { mount, flushPromises } from '@vue/test-utils';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import CapabilityGate from '@/components/CapabilityGate.vue';
import * as useCapMod from '@/composables/useCapability';

describe('CapabilityGate', () => {
  beforeEach(() => {
    vi.resetAllMocks();
  });

  it('renders default slot when all required fields available', async () => {
    // v1.1 修复 S10: 用 vi.spyOn 替换实现, 但保持响应性
    vi.spyOn(useCapMod, 'useCapability').mockReturnValue({
      hasAll: () => true,
      missingOf: () => [],
      available: ref(new Set(['store_name'])) as any,
      loading: ref(false),
      error: ref(null),
      cardStatus: computed(() => ({})),
      suggestions: computed(() => []),
      fetchCapability: vi.fn(),
    });
    
    const wrapper = mount(CapabilityGate, {
      props: { requires: ['store_name'], cardId: 'test' },
      slots: { default: '<div class="content">Visible</div>' },
    });
    await flushPromises();
    expect(wrapper.find('.content').exists()).toBe(true);
  });
  
  it('renders skeleton when loading and available is null', async () => {
    vi.spyOn(useCapMod, 'useCapability').mockReturnValue({
      hasAll: () => true,  // fail-open during loading
      missingOf: () => [],
      available: ref(null) as any,
      loading: ref(true),
      // ...
    } as any);
    
    const wrapper = mount(CapabilityGate, {
      props: { requires: ['date'], cardId: 'test' },
      slots: { default: '<div class="content">Should not show during loading</div>' },
    });
    await flushPromises();
    expect(wrapper.find('.el-skeleton').exists()).toBe(true);
    expect(wrapper.find('.content').exists()).toBe(false);
  });
  
  it('fail-open on error: hasAll returns true', async () => {
    vi.spyOn(useCapMod, 'useCapability').mockReturnValue({
      hasAll: () => true,  // 错误兜底
      missingOf: () => [],
      available: ref(null) as any,  // 错误时仍 null
      loading: ref(false),
      error: ref(new Error('500')) as any,
      // ...
    } as any);
    
    const wrapper = mount(CapabilityGate, {
      props: { requires: ['date'], cardId: 'test' },
      slots: { default: '<div class="content">Visible despite API error</div>' },
    });
    await flushPromises();
    expect(wrapper.find('.content').exists()).toBe(true);
  });
});
```

### 8.3 Load Test (v1.1 新增, S11)

```bash
# 用 hey (or k6) 模拟 100 concurrent factories
hey -n 1000 -c 100 -H "X-Factory-Id: F001" \
    http://localhost:8083/api/smartbi/capability/F001

# SLO:
#   p50 < 50ms
#   p95 < 200ms
#   p99 < 500ms
#   error rate < 0.1%
```

如果 SLO 不达标:
- 加 Redis cache (§3.4)
- 优化 SQL (加 covering index 在 (factory_id, upload_status))
- 简化算法 (干脆不缓存, 直接查)

### 8.3 E2E 测试 (复用 tests/e2e-comprehensive)

新建 `tests/e2e-comprehensive/capability-rendering.mjs`:

```javascript
// 5 项 M1 smoke verification
import { chromium } from '@playwright/test';

async function testProductSummaryUpload() {
  // 1. 登录 R_XMX_FRESH3 (商品汇总场景)
  // 2. 访问 Dashboard
  // 3. 断言: 客单价 / 订单数 / 销售趋势 卡片 NOT 渲染
  // 4. 断言: Top 菜品 卡片渲染 (无日期但有商品+店)
  // 5. 断言: UnlockMoreCTA 显示 "上传账单流水可解锁 X 个"
}

async function testBillFlowUpload() {
  // 1. 登录 F001 (账单流水场景)
  // 2. 访问 Dashboard
  // 3. 断言: 12+ KPI 卡片全部渲染
  // 4. 断言: UnlockMoreCTA 不显示 (或显示 evolved suggestions)
}

async function testMixedUpload() {
  // 1. factory 同时有商品汇总 + 账单流水
  // 2. capability calculator 返回字段并集
  // 3. 全部支持的卡片都渲染
}

async function testDeleteUpload() {
  // 1. 删除账单流水, factory 只剩商品汇总
  // 2. capability invalidate 触发
  // 3. 客单价等卡片再次隐藏
}

async function testTenantIsolation() {
  // 1. F001 访问自己的 capability
  // 2. F002 访问自己的, 字段集合不同
  // 3. F001 跨调 F002 capability → 403
}
```

---

## 9. 部署与回滚

### 9.1 Test 环境部署

```bash
# 后端 Python
./scripts/deploy/deploy-smartbi-python.sh --env test

# 前端
cd web-admin && npm run build && rsync dist/ ...test/
```

### 9.2 Prod 灰度部署

**Phase 1: 后端先上 (无行为变化)**
- Python `/api/smartbi/capability/{factory_id}` 端点上线
- 旧前端不调用, 无 visible change

**Phase 2: 前端 50% 灰度** (v1.1 修复 C2 env 前缀)

```bash
# .env.production (build time)
VITE_CAPABILITY_GATE_ENABLED=true     # 必须 VITE_ 前缀, Vite 才认
```

```typescript
// web-admin/src/composables/useCapability.ts
const ENABLED = import.meta.env.VITE_CAPABILITY_GATE_ENABLED === 'true';

export function useCapability() {
  if (!ENABLED) {
    // feature off: 返 fail-open stub
    return {
      hasAll: () => true,
      missingOf: () => [],
      // ...
    };
  }
  // 正常实现
}
```

**灰度策略**: 一开始用全部署(`VITE_CAPABILITY_GATE_ENABLED=false`)+ 内部 admin pre-test, 然后切 `true` 100%. 不做 50% A/B (Vue 客户端 build 是单一构件, 不像后端可按 tenant 路由).

替代方案: **后端按 factory_id 灰度** —`/api/smartbi/capability/{factory_id}` 在某些 factory_id 返 503, FE 走 fail-open. 这样不用重 build:

```python
GRADUAL_ROLLOUT_FACTORIES = {"F001", "F003", "RES_3101_009"}  # 50% 灰度名单

@router.get("/{factory_id}")
async def get_capability(factory_id: str, ...):
    if factory_id not in GRADUAL_ROLLOUT_FACTORIES:
        raise HTTPException(503, "Capability layer not enabled for this factory")
    # ...正常逻辑
```

FE useCapability 收到 503 → 走 fail-open → 老体验. 50% 灰度通过修改 `GRADUAL_ROLLOUT_FACTORIES` 实现, 不需 redeploy FE.

**Phase 3: 100% 上线**
- 后端 `GRADUAL_ROLLOUT_FACTORIES = ALL` (或删变量)
- 监控正常 1 周后, FE 也彻底删除 `VITE_CAPABILITY_GATE_ENABLED` flag

### 9.3 回滚

后端: 上线前用 deploy-backend.sh 自动备份机制 (现有 `*.bak.YYYYMMDD_HHMMSS` pattern). 紧急回滚 = 改 `GRADUAL_ROLLOUT_FACTORIES = set()` + restart cretas-python (5 分钟).

前端: feature flag `VITE_CAPABILITY_GATE_ENABLED=false` 在下次 build 关闭. 紧急时直接后端 503 兜底, FE 走 fail-open.

---

## 10. 风险与未决问题

### 10.1 风险 (v1.1 调整)

| # | 风险 | 缓解 |
|---|---|---|
| A1 | `_ALIAS_TO_ATTR` 现存别名不全 | **Day 0 强制 pre-flight audit** (§7 Phase 0). 命中率 < 80% 时不允许进 Day 1. |
| A2 | metadata 标注 ~80 处错标 | Admin 审计页 + 上线后 1 周每日 check + CI lint rule 防 card-id 漂移 |
| A3 | Dashboard 改造引发 element-plus 布局 bug | 后端 503 灰度 (5 个 factory 起步) + 监控 dashboard 加载错误 |
| A4 | manifest 手工 vs 自动 (drift 风险) | v1.1 手工 + CI lint (检查 `<CapabilityGate :card-id="X">` X 在 CARD_MANIFEST). 不上 vite plugin |
| A5 (新) | Worker scale 后 cache desync | 当前 worker=1 安全; 切多 worker 前先上 Redis (§3.4) 或拿掉 cache |

### 10.2 未决问题 (v1.1 收敛)

**已决定 (从 v1.0 §10.2 移走)**:
- ✅ Java 后端不做 capability gate (§1.3 已写)
- ✅ Manifest v1.0 手工维护 + CI lint (§2.2.3, §10.1 A4)
- ✅ CTA 位置: Dashboard 底部 el-card + 仅 analytics 路由 (S 系列修复)

**剩余待用户拍**:
1. **placeholder mode 默认开关**: 默认 hide (我倾向, 客户 Dashboard 干净) 还是 placeholder (帮客户理解"这里本应有 X")? — 影响 30 个 Dashboard 卡片的显示风格
2. **多 sheet xlsx 处理**: 现状每 sheet 独立 upload, capability calculator 自动 union. 是否接受?
3. **灰度名单 GRADUAL_ROLLOUT_FACTORIES 起步**: 第一波放哪几个 factory? (建议 F001 测试 + RES_3101_009 友好客户, 共 2-3 家)

---

## 11. 与 Roadmap 的对应

| Roadmap §11 决策点 | 本 spec 章节 |
|---|---|
| §11.2 A 契约层范围 + 与 B 并行 | §1.2 |
| §11.7 requires schema 用 canonical English + ALIAS | §2.1, §2.3 |
| §11 (8 项) MVP-A 是否包含 A | 是, A 是 MVP-A 的第一阶段 (week 1-2 of 5-6) |

---

## 12. 后续步骤

A spec 通过审核 → 开实施 chat (新开 chat, 内部用 subagent 并行 Python + Vue + 测试) → 完成 M1 5 项 smoke gate → 启动 B spec 编写.

---

**文档状态**: v1.0 待审核 → 用户确认 §10.2 5 个未决问题后进入实施
