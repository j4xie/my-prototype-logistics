# /www/wwwroot/cretas/.env.test (chmod 600)
# 测试环境变量 — 用于 cretas-backend-test.service
# 来源: restart-test.sh inline export 提取
# 跟 .env.prod 区别: DB_NAME=cretas_db, SMARTBI_DB_NAME=smartbi_db, JWT_SECRET 不同

# Database (test 数据)
DB_PASSWORD=cretas123
DB_NAME=cretas_db
SMARTBI_DB_PASSWORD=smartbi_secure_password_2025
SMARTBI_DB_NAME=smartbi_db

# Auth
JWT_SECRET=cretas-jwt-secret-key-2026-test
INTERNAL_API_SECRET=cretas-internal-sec-87a9caca9f57b1f2

# LLM
CRETAS_AI_DASHSCOPE_API_KEY=sk-da3b827e6a00404a8bc869296f8690bc

# 3rd-party (test env 用 placeholder, prod 用真值)
IFLYTEK_APPID=placeholder
IFLYTEK_API_KEY=placeholder
IFLYTEK_API_SECRET=placeholder
ALIBABA_ACCESSKEY_ID=placeholder
ALIBABA_SECRET_KEY=placeholder

# Phase 3 SmartBI Gold
SMARTBI_GOLD_READ_PRIMARY_ENABLED=true
