# 食品加工(通用)行业基准数据
# 当无法检测到具体子行业时使用的 fallback
# 与 _common.yaml 相同指标，但此文件作为 sub_sector="食品加工" 的显式入口

source: "中国食品工业协会 / 国家统计局食品制造业年报"
year: 2024

# 不覆盖任何指标 — 直接使用 _common.yaml 全部默认值
# 如需调整通用食品加工的范围，在此处添加 metrics 覆盖
metrics: {}
