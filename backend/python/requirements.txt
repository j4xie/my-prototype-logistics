# Compatible with Python 3.8+

# FastAPI and server
fastapi>=0.70.0,<1.0.0
cachetools>=5.3.0
uvicorn>=0.15.0,<1.0.0
gunicorn>=21.2.0,<23.0.0
slowapi>=0.1.9,<1.0.0
python-multipart>=0.0.5

# Data processing
pandas>=1.3.0,<2.0.0
openpyxl>=3.0.0,<4.0.0
numpy>=1.20.0,<2.0.0
xlrd>=2.0.0,<3.0.0

# Statistical analysis and forecasting
statsmodels>=0.13.0,<1.0.0
scipy>=1.7.0,<2.0.0

# LLM integration
httpx>=0.20.0,<1.0.0
aiohttp>=3.8.0,<4.0.0
openai>=1.0.0  # For OpenAI-compatible API (DashScope)

# Pydantic for data validation
pydantic>=2.5,<3

# JWT Authentication
PyJWT>=2.6.0,<3.0.0

# RSA + X.509 — required by ota.services.signing for Expo Updates v1 manifest
# code-signing. Server hot-fixed via `pip install cryptography` after Phase 1
# deploy crashed (2026-05-12 P0 incident); this line + the import smoke
# test below prevent recurrence.
cryptography>=42.0.0

# YAML for knowledge base
PyYAML>=6.0,<7.0.0

# Utilities
python-dateutil>=2.8.0,<3.0.0
typing-extensions>=3.7.4
python-dotenv>=0.19.0

# PostgreSQL (for dynamic data storage)
sqlalchemy>=1.4.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0
asyncpg>=0.27.0,<1.0.0

# gRPC client (Phase 2B-α: ai/embedding.py → embedding-service:9090)
grpcio>=1.59.0,<2.0.0
# grpcio-tools is BUILD-time only (regenerate stubs from .proto via
# scripts/phase2b/regen-embedding-stub.sh); not loaded at runtime.
grpcio-tools>=1.59.0,<2.0.0

# pgvector asyncpg type adapter (Phase 2B-α B1: stage 5 SEMANTIC SQL uses
# `embedding <=> $1::vector`). Currently passing vec as text fallback so this
# is reserved for future native binding.
pgvector>=0.2.4

# PPT generation
python-pptx>=0.6.23

# PDF generation
reportlab>=4.0
# C-PRT-1: 二维码 (采购单 PDF, 供仓管员扫码入库). 失败 graceful (无 QR 仍出 PDF).
qrcode[pil]>=7.4

# Pydantic settings (for environment variable loading)
pydantic-settings>=2.0.0 ; python_version >= "3.9"

# Prometheus metrics for FastAPI
prometheus-fastapi-instrumentator>=5.0.0,<6.0.0  # 6.0+ requires Python 3.9+

# Foreign Object Detection (YOLO ONNX inference)
onnxruntime>=1.14.0,<2.0.0
Pillow>=9.0.0,<11.0.0

# Intent Classifier (BERT chinese-roberta-wwm-ext)
# CPU-only torch to keep install size reasonable; model path configured in intent_classifier.py
# NOTE: If venv is rebuilt, these MUST be reinstalled or /api/classifier/health returns 404
# and Java ClassifierIntentMatcher floods the error log with "BERT分类器服务不可达".
torch==2.4.1 ; python_version == "3.8"
transformers>=4.40,<5.0
tokenizers>=0.20,<1.0
safetensors>=0.4,<1.0
huggingface-hub>=0.20,<1.0

# Materialized analytics compute backend (W1 Task 3)
polars>=1.0.0,<2.0.0

# Testing
pytest-asyncio>=0.21.0
