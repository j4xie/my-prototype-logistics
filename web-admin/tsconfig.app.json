{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "types": ["vite/client", "node"],
    "verbatimModuleSyntax": false,
    /* Tier 3 vue-tsc cleanup (2026-05-10): @vue/tsconfig defaults lib to ES2020 which
     * lacks Array.prototype.at, structured clone, etc. Enable ES2022 to match
     * tsconfig.node.json target. Vite already targets ES2022 in builds. */
    "lib": ["ES2022", "DOM", "DOM.Iterable"],

    /* Path Aliases */
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },

    /* Strict mode: incremental rollout */
    /* Phase A (enabled): low-breakage flags */
    "strict": false,
    "noImplicitThis": true,
    "alwaysStrict": true,
    "strictBindCallApply": true,
    /* Phase B (enabled): function type safety */
    "strictFunctionTypes": true,
    /* Phase C (enabled): no implicit any — explicit types required */
    "noImplicitAny": true,
    /* Phase D (future): strictNullChecks — required to also enable strictPropertyInitialization */
    // "strictNullChecks": true,
    // "strictPropertyInitialization": true,

    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"]
}
