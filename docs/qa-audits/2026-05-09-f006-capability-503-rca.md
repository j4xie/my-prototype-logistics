# `/api/smartbi/capability/{factory}` 503 — Root Cause Analysis

**Date**: 2026-05-09 CST
**Author**: ops chat 2 (F006 capability 503 investigation)
**Trigger**: Organizer Playwright 2026-05-09 ~06:22 CST observed `f006_admin` session getting 503 from capability endpoint while all other F006 endpoints returned 200; frontend fell back to permissive mode (`[capability] fetch failed, falling back to permissive mode`).
**Verdict**: **NOT A BUG**. Intentional gradual rollout gate via `CAPABILITY_ROLLOUT_FACTORIES` env var (per 数据织网 02-A 能力驱动渲染 spec §9.2). Default whitelist = `F001,RES_3101_009` (Day 13 starter cohort, ~17% of 12 factories). Frontend permissive fallback is the designed counterpart.

---

## 1. Background

### 1.1 Organizer Playwright capture

`f006_admin` session navigating to `/smart-bi/finance` page:

| Endpoint | Status | Note |
|---|:---:|---|
| `/smartbi-api/api/smartbi/capability/F001` (F001 session) | 200 | Reference |
| `/smartbi-api/api/smartbi/capability/F006` (f006_admin session) | **503** | This investigation |
| `/api/mobile/F006/canvas/role-module-override` | 200 | Other F006 endpoints OK |
| `/api/mobile/F006/config/disabled-modules` | 200 | |
| `/smartbi-api/api/smartbi/gold/analysis-results?factory_id=F006` | 200 | |
| `/api/mobile/F006/smart-bi/analysis/finance` | 200 (state B empty) | Per Pattern B 3-state |

Frontend log: `[capability] fetch failed, falling back to permissive mode` — graceful degradation worked.

### 1.2 Initial framing

Marching order treated the 503 as latent backend issue worth investigating ("doc-only, Phase 2A 不阻塞但是值得 follow-up"). Suspected causes: missing config table row / database query failure / specific factory tier handling.

---

## 2. Reproduction

Direct curl to Python prod 8083 with `X-Internal-Secret` + `X-Factory-Id`:

| Factory | HTTP | Body |
|---|:---:|---|
| F001 | **200** | Full capability payload (available_fields / template_status / unlock_suggestions / time_coverage) |
| F002 | **503** | `{"success":false,"message":"Capability layer not enabled for factory=F002 (gradual rollout)","code":"INTERNAL_ERROR"}` |
| F003 | 503 | Same shape |
| F004 | 503 | Same shape |
| F006 | 503 | Same shape |
| F999 | 503 | Same shape |
| R001 | 503 | Same shape |
| **RES_3101_009** | **200** | Full capability payload |
| RES_GML_001 | 503 | Same shape |
| R_GML_DEMO | 503 | Same shape |

**Selective pattern: only F001 + RES_3101_009 return 200.** Identical to Issue #1's "leak" pattern — same 2 factories, no coincidence (see §5).

Error message string is intentional and self-documenting: `"(gradual rollout)"`.

---

## 3. Code path — gradual rollout gate

### 3.1 `backend/python/smartbi/capability/api.py:21-29`

```python
# spec §9.2: backend per-factory 灰度 — return 503 for non-whitelisted factories.
# FE useCapability 收到 503 → fail-open → 老体验. 灰度通过 env var 调整, 不需 redeploy FE.
# Default = "F001,RES_3101_009" (Day 13 starter cohort, ~17% of 12 factories).
# Set CAPABILITY_ROLLOUT_FACTORIES="*" to enable all (post-cohort observation period).
import os
_ROLLOUT_RAW = os.environ.get("CAPABILITY_ROLLOUT_FACTORIES", "F001,RES_3101_009")
_ROLLOUT_FACTORIES: Optional[set[str]] = None if _ROLLOUT_RAW.strip() == "*" else {
    f.strip() for f in _ROLLOUT_RAW.split(",") if f.strip()
}
```

### 3.2 `_check_rollout` gate (line 56-64)

```python
def _check_rollout(factory_id: str) -> None:
    """spec §9.2 backend 灰度 gate: 503 for non-whitelisted factory.
    FE 收到 503 → fail-open → 老体验, 不需 redeploy FE.
    """
    if _ROLLOUT_FACTORIES is not None and factory_id not in _ROLLOUT_FACTORIES:
        raise HTTPException(
            503,
            f"Capability layer not enabled for factory={factory_id} (gradual rollout)",
        )
```

### 3.3 Wired into both routes (line 67-98)

- `GET /{factory_id}` → calls `_check_factory_access` then `_check_rollout`
- `POST /{factory_id}/invalidate` → same pattern

Both routes 503 for non-whitelisted factories before any DB query runs. Failure mode is purely a cohort gate, not a query/data error.

---

## 4. Production env var state (verified)

```
=== /www/wwwroot/cretas/.env.prod ===
(CAPABILITY_ROLLOUT_FACTORIES not set)

=== /etc/systemd/system/cretas-python.service ===
(CAPABILITY_ROLLOUT_FACTORIES not set)

=== Running uvicorn process /proc/<pid>/environ ===
(CAPABILITY_ROLLOUT_FACTORIES not set)
```

→ Code default `"F001,RES_3101_009"` is the active whitelist on prod.

To enable additional factories: set env var (in `.env.prod` or systemd) and restart `cretas-python` service. To enable all: set to `"*"`.

---

## 5. Cross-reference to Issue #1 (same 2 factories, not coincidence)

The capability rollout whitelist (F001 + RES_3101_009) **exactly matches** the factories with populated Gold POS data per Issue #1 RCA (`docs/qa-audits/2026-05-09-java-gold-cross-factory-leak-rca.md`).

Reasonable hypothesis: 数据织网 02-A 能力驱动渲染 rollout cohort was intentionally aligned with factories that have populated POS data, because:

- Capability layer's job (per spec) is to expose what canonical fields / templates are unlocked by the tenant's actual uploaded data
- Factories with no POS data have nothing to compute capability over
- Starting the cohort with F001 (staging tenant for 青花椒) + RES_3101_009 (production tenant for same chain, seeded Apr 23) gives the only meaningful test surface

This is internally consistent design, not coincidence. The same gradual-rollout gate would be a natural pattern for any new feature that depends on Silver/Gold data — Day 13 cohort = 2 populated factories; full rollout post-observation.

---

## 6. Reframe — what's actually happening

| Claim / framing | Reality |
|---|---|
| "F006 capability endpoint 503" | True surface symptom |
| "Latent backend issue" | NO — endpoint is working as spec'd; 503 is the designed signal for cohort gating |
| "Missing config table row" hypothesis | Disproved — gating is env-var-only; no DB lookup involved |
| "Database query fail" hypothesis | Disproved — `_check_rollout` runs before any DB access |
| "Specific factory tier handling" hypothesis | Disproved — no tier mapping; pure whitelist set |
| "F006-specific" framing | Misleading — same 503 for F002/F003/F004/F006/F999/R001/RES_GML_001/R_GML_DEMO. F006 was just the Playwright session in scope at capture time. |

---

## 7. Impact assessment

### 7.1 Customer-facing impact = 0

- Frontend `useCapability` hook explicitly handles 503 by falling back to "permissive mode" (老体验 / legacy UX) per code comment line 22-23
- Playwright capture confirms FE fallback log: `[capability] fetch failed, falling back to permissive mode`
- Customer continues to see SmartBI page with legacy capabilities; only the new "capability-driven rendering" affordances are absent for the gated cohort
- This is the entire point of cohort rollout: ramp the new feature gradually without breaking existing UX

### 7.2 T6.4 cutover risk = 0

- `/api/smartbi/capability/{factory}` is unrelated to T6.4 nginx vhost cutover (T6.4 routes `/api/mobile/{factory}/smart-bi/analysis/*` Java→Python)
- The capability endpoint is on a separate path (`/smartbi-api/api/smartbi/capability/*`) served by Python prod 8083
- Cutover stages 1-5 do not affect capability rollout state

### 7.3 Phase 2A close interaction

Phase 2A close ~50-60 min from this investigation kickoff (per dispatch). Capability rollout is **independent** of Phase 2A — different feature track (数据织网 02-A vs SmartBI analysis port). No interaction.

---

## 8. Recommendations

### 8.1 No code fix needed

`smartbi/capability/api.py` is implementing the spec exactly as designed. No PR with code/DB change required.

### 8.2 Memory hygiene (low priority)

Add reference memory entry capturing:
- Capability rollout cohort = `F001,RES_3101_009` (default code env)
- Override mechanism: `CAPABILITY_ROLLOUT_FACTORIES` env var; set `"*"` to enable all post-observation
- 503 + FE permissive fallback is the designed graceful-degradation path
- Same 2 factories aligned with Issue #1 populated-Gold-data set (not coincidence)

### 8.3 When to flip rollout to all factories (deferred to feature owner)

The 数据织网 02-A spec implies a "post-cohort observation period" before flipping to `"*"`. Triggers for the flip:

- F001 + RES_3101_009 capability layer stable for N days (spec §9.2 to define N)
- No P1 capability-related customer reports
- Frontend permissive fallback metrics confirm acceptable degradation rate
- Other factories acquire POS data → capability layer becomes meaningful for them

This decision belongs to the 数据织网 02-A feature owner, not Phase 2A team. Recommend:
- File a follow-up ticket scoped to "evaluate capability rollout expansion post-observation"
- Defer to feature owner; not a blocker for any current work stream

### 8.4 Process learning (organizer practice)

This is the **second consecutive investigation** turning up "intentional design, not bug":
- Issue #1 (`2026-05-09-java-gold-cross-factory-leak-rca.md`): Apr 23 deliberate data seed
- Issue #2 (this doc): gradual rollout cohort gate

Shared pattern: organizer / Playwright catches selective behavioral signal → marching order frames as latent bug → investigation reveals it's a planned rollout / migration / feature flag.

**Lesson**: when a "selective" pattern surfaces (some factories OK, others not), do a 30-second check first:
1. Grep for the selective factory IDs in code (e.g. `grep -rn "F001,RES_3101_009"` would have caught the rollout whitelist immediately)
2. Check for env vars / feature flags / config tables that gate behavior
3. Match the affected set against known rollout cohorts / data-seeded factories

If the selective set matches a known rollout cohort or data-seed cohort, the "bug" is likely the intentional design. This 30-second pre-check would have answered both Issue #1 and #2 before deeper investigation.

Per memory `feedback_organizer_projection_bug.md` extension: when "selective pattern" surfaces in Playwright / smoke / monitoring, do `grep -rn "<factory_id_set>"` in repo first. If found in env-var defaults or config defaults, it's likely intentional.

---

## 9. Open question for feature owner (deferred)

Per §8.3:
- Should `CAPABILITY_ROLLOUT_FACTORIES` env var be flipped to `"*"` once F002/F003/F004/F006/etc. acquire POS data?
- What's the observation-period exit criteria from 数据织网 02-A 能力驱动渲染 spec §9.2?
- Should env var be set explicitly in `.env.prod` for visibility (currently relies on code default), or is "use code default" deliberately keeping the cohort definition in source rather than ops config?

These are not blockers for any current work stream — pure feature-owner decisions for post-Phase-2A timeline.

---

## 10. Investigation summary

| Item | Outcome |
|---|---|
| Endpoint code review (`smartbi/capability/api.py`) | ✅ Clean — env-var-driven gradual rollout gate per spec §9.2 |
| Reproduction across 10 factories | F001 + RES_3101_009 = 200; F002/F003/F004/F006/F999/R001/RES_GML_001/R_GML_DEMO = 503 |
| Error message inspection | Self-documenting `"Capability layer not enabled for factory=X (gradual rollout)"` |
| Env var state on prod (`/www/wwwroot/cretas/.env.prod` + systemd + process) | Not set → code default `F001,RES_3101_009` active |
| Frontend behavior verification | `useCapability` fail-open to permissive mode (matches Playwright log) |
| Hypothesis "missing config row" | Disproved — no DB lookup before gate |
| Hypothesis "DB query fail" | Disproved — gate runs before query |
| Hypothesis "specific factory tier" | Disproved — pure whitelist set |
| Cross-reference to Issue #1 | Same 2 whitelisted factories — capability cohort intentionally aligned with populated-Gold-data factories |
| **Actual root cause** | **Intentional gradual rollout gate via `CAPABILITY_ROLLOUT_FACTORIES` env var; default `F001,RES_3101_009` (Day 13 cohort)** |
| Code fix needed | NO |
| DB fix needed | NO |
| Env var change | NO (defer to feature owner observation-period exit) |
| Customer-facing impact | 0 (FE permissive fallback handles it) |
| T6.4 cutover risk | 0 (unrelated paths) |

---

## 11. ⛔ HOLD blocks

- ⛔ **Doc-only**: this audit produces no code changes, no DB writes, no env var changes
- ⛔ **No env var flip in this PR**: `CAPABILITY_ROLLOUT_FACTORIES="*"` decision belongs to 数据织网 02-A feature owner, not Phase 2A team
- ⛔ **No spec doc edit**: 数据织网 02-A 能力驱动渲染.md §9.2 is the source-of-truth for rollout cadence; this audit references it, doesn't modify it
- ⛔ **No memory edit in this PR**: §8.2 memory hygiene is a separate follow-up by future chat (memory edits live in `~/.claude/memory/`, not in repo)

---

**End of investigation. NOT a bug — intentional gradual rollout cohort gate; default `F001,RES_3101_009` whitelisted. Frontend permissive fallback is the designed graceful-degradation counterpart.**

🤖 Generated with [Claude Code](https://claude.com/claude-code)
