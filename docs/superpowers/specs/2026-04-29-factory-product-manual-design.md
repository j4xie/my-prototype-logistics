# 工厂版操作手册 — 产品手册化重组 设计文档

**Date**: 2026-04-29
**Status**: Draft → 待用户 review
**Owner**: stevenj4xie + Claude
**Branch**: `main`
**前置依赖**: 餐饮版 Phase 0+1+2 已完成（`docs/plans/2026-04-29-phase2-handoff.md`）

---

## 1. 背景

工厂版操作手册当前文件 `docs/plans/factory-operation-manual.html`：
- 7236 行，20 章，已入库 KB 287 chunks（subcategory='factory'）
- 风格偏 dev/QA 流程式（"Step 7.1: 进入生产计划页面" → 1) 打开 → 2) 左侧菜单...）
- 详细描述了按钮名、字段、菜单路径，对工厂内部 onboarding 价值很高

但 vs 已上线的餐饮版手册（24 章 + 4 培训路径 + 6 决策合成）：
- 没有"概述/入门怎么用/管理怎么解读/FAQ"产品手册式结构
- 没有培训路径
- 没有决策合成（跨章节场景）
- 章节模板不统一

目标：把工厂手册重组到跟餐饮版同一水平，**保留现有 Step 详细内容作为深度参考**，章节头加产品手册式结构，让客户（厂长、班组长、操作工）也能用，而不只是 QA 内部用。

---

## 2. 总体架构（最终目标，分阶段交付）

### 2.1 章节结构（22 章）

保留现有 20 章顺序 + 头部加 2 个产品级章节：

| 章节 | 内容 | 状态 |
|------|------|------|
| **§0A** | 智能数据分析 (AI Query) | 新增（本次） |
| **§0B** | 财务 PBI 看板 | 新增（本次） |
| §1 | 系统概览与测试账号 | 现存 |
| §2 | 员工管理与工牌生成 | 现存 |
| §3 | 客户管理 | 现存 |
| §4 | 原料类型管理 | 现存 |
| §5 | 产品模板与 BOM 配置 | 现存 |
| §6 | SKU 组装与原料入库 | 现存 |
| §6C | 销售订单管理 | 现存 |
| §6D | 供应商管理 | 现存 |
| §6E | 采购订单管理 | 现存 |
| §6F | 质检记录管理 | 现存 |
| §7 | 创建生产计划 + 生成调拨单 | 现存 |
| §8 | 仓储审批 + 发货 | 现存 |
| §9 | 车间签收 + 领料确认 | 现存 |
| §10 | 员工签到 + 报工 + 签退 | 现存 |
| §11 | 查看分析报表 | 现存 |
| §12 | AI 对话 + 常见问题 | 现存 |
| §13 | SmartBI 智能分析 | 现存 |
| §14 | 财务管理 | 现存 |
| §15 | 设备管理 | 现存 |
| §16 | 数据分析 | 现存 |
| §17 | 调度管理 | 现存 |
| §18 | 补充模块 | 现存 |
| §19 | 研发管理 | 现存 |
| §20 | 系统管理 | 现存 |

### 2.2 培训路径（5 条）

| 路径 | 角色 | 时长 | 内容 |
|------|------|------|------|
| **A1** | 车间操作工首周 | 2 小时 | 移动端签到、领料、报工、签退（§10 + §9） |
| **A2a** | 仓库管理员首周 | 2 小时 | 入库、调拨、盘点（§4 + §6 + §8 + §9） |
| **A2b** | 质检员首周 | 2 小时 | 质检记录、不合格处理、标准对照（§6F） |
| **B** | 班组长 / 调度员首月 | 4 小时 | 排程、订单、报工监控、KPI（§7 + §17 + §10 + §11） |
| **C** | 厂长月度回顾 | 1 小时 | 财务、效率、合规、设备 OEE（§14 + §11 + §15 + §0A） |

### 2.3 §B 决策合成（24 个跨章节场景）

| # | 场景 | 跨章节 |
|---|------|--------|
| 1 | 产品成本异常上升 | §4 + §6E + §5 + §14 |
| 2 | 生产计划延期 | §17 + §9 + §8 + §6F |
| 3 | 质检不合格率高 | §6F + §4 + §5 + §15 |
| 4 | 设备故障频繁 | §15 + §17 + §10 |
| 5 | 库存积压 | §4 + §6E + §6C + §13 |
| 6 | 月度财务核算偏差 | §14 + §13 + §11 |
| 7 | 客户投诉 / 退货处理 | §6C + §6F + §10 + §11 |
| 8 | 原料供应中断 / 涨价 | §6E + §6D + §4 + §17 |
| 9 | 食安溯源 / 客户审厂应对 | §6 + §10 + §6F + §11 |
| 10 | 新品试产 / 小批量打样 | §5 + §19 + §6F + §6 |
| 11 | 销售订单交期紧急 / 插单 | §6C + §17 + §9 + §8 |
| 12 | 员工绩效考核 / 计件工资 | §2 + §10 + §11 |
| 13 | 季节性产能扩大 / 临时增产 | §2 + §15 + §17 |
| 14 | 出货异常 / 物流延误 | §8 + §6C + §11 |
| 15 | 月度盘点差异处理 | §4 + §18 + §14 |
| 16 | 多客户多 SKU 切换 | §17 + §5 + §6F + §8 |
| 17 | 设备 OEE 提升 / 利用率分析 | §15 + §10 + §17 |
| 18 | 能耗 / 水电气超标 | §15 + §11 + §14 |
| 19 | 跨厂区 / 跨车间调拨 | §8 + §17 + §10 |
| 20 | 资质认证审核（HACCP/ISO） | §6F + §10 + §11 + §20 |
| 21 | 生产损耗率分析（黄金标准对比） | §10 + §5 + §14 |
| 22 | 加班 / 排班合规（劳动法） | §2 + §10 + §20 |
| 23 | 批次召回应急（食安事故） | §6 + §6C + §6F + §20 |
| 24 | 新工厂启动 / 老厂迁移数据 | §20 + 全部 |

### 2.4 章节内部结构（8-section 模板，仿餐饮版 §1/§2 Tier 1）

每个 Tier 1 章节遵循：

```
<section id="ch{N}">
  <h1>§N 标题 (英文)</h1>
  <p class="overview">概述 200-300 字...</p>

  <!-- 入门 / 怎么用 (新员工视角) -->
  <h2 id="ch{N}-1">§N.1 进入路径</h2>
  <h2 id="ch{N}-2">§N.2 主界面 layout</h2>
  <h2 id="ch{N}-3">§N.3 常用操作步骤</h2>
  <h2 id="ch{N}-4">§N.4 常见错误处理</h2>

  <!-- 管理 / 怎么解读 (班组长/厂长视角) -->
  <h2 id="ch{N}-5">§N.5 关键指标速查</h2>
  <h2 id="ch{N}-6">§N.6 业务判断框架</h2>
  <h2 id="ch{N}-7">§N.7 跨章节联动</h2>
  <h2 id="ch{N}-8">§N.8 常见决策场景</h2>

  <h2 id="ch{N}-faq">§N FAQ</h2>
</section>
```

---

## 3. 本次 Phase 0 范围（约 2-3 小时 / 1-2 sessions）

### 3.0 已就绪基础设施（不需要再做）

- ✅ `ManualChatRequest` 已含 `category: Optional[str]` 和 `image_base64: Optional[str]` 字段（main 分支 commit `d6330fad5`+ 后续）
- ✅ `manual_chat` endpoint 在 `request.category in ("restaurant", "factory")` 时显式设 `subcategories=[request.category]`（main 第 552-564 行）
- ✅ aiassist 前端按 sessionStorage 选择填 `category`，工厂版必带 `"category":"factory"`
- ✅ 数据库已有 287 chunks subcategory='factory'，路由生效

### 3.1 必做项

1. **写 §0A 智能数据分析 (AI Query)** — Tier 1 production-grade，约 200 行 HTML
2. **写 §0B 财务 PBI 看板** — Tier 1 production-grade，约 200 行 HTML
3. **更新目录** — TOC 加 §0A + §0B 链接，HTML 锚点统一用全小写：`id="ch0a"` / `id="ch0a-1"` / `id="ch0a-faq"` / `id="ch0b"` / `id="ch0b-1"` 等
4. **更新 last-verified meta** — 文件头 `<!-- last-verified-against-product: 2026-04-29 -->`，§0A / §0B / §6 / §7 / §10 / §14 章节内也加章级 meta
5. **重新入库** — 触发 `manual_ingester.py` 重入 factory manual（atomic swap，全部替换；预计新 total 约 309-315 chunks，比当前 287 多 22-28 个）
6. **部署** — `factory-operation-manual.html` scp 到 139（aiassist 子域名 + web-admin 都读这个）
7. **Smoke test**（必经全过）：
   - **Test A**：工厂版 aiassist 问 "AI Query 怎么用"，response 含至少 1 个工厂 KPI 关键词（OEE/良品率/损耗率/工序合格率/OTIF）
   - **Test B**：工厂版 aiassist 问 "财务看板看什么"，response 含至少 1 个 §0B 指标关键词（损益瀑布/桑基/应收账龄）
   - **Test C**：餐饮版 aiassist 问 "怎么开始一个生产批次"，response 仍是"该功能在当前选择的版本（餐饮版）操作手册中未记录..."（跨域拒绝不被破坏）

### 3.2 不做项（留给后续 Phase）

- `_FACTORY_KEYWORDS` auto-detect（已经用 explicit category 路由，auto-detect 仅影响 web-admin AIQuery 等其他入口，不影响 aiassist）
- §1-§20 章节模板补强（Phase 1b）
- 5 条培训路径（Phase 1b）
- 24 个决策合成场景（Phase 1b）
- CI drift防腐 lint 脚本扩展到 factory manual（Phase 2）

---

**章节命名说明**：原本提议 §0 / §0.5，但 §0.5 与子节"§N.5 关键指标速查"命名格式冲突会引起混淆，改用 **§0A / §0B**。子节用 §0A.1 ~ §0A.8 / §0B.1 ~ §0B.8。

---

## 4. §0A AI Query 章节内容设计

### 4.1 概述（200-300 字）

智能数据分析 (AI Query) 是白垩纪 SaaS 的**数据分析入口**，允许工厂用户用自然语言直接提问，例如"本月哪条产线 OEE 最低？"或"上周哪个 SKU 损耗率最高？"，无需编写 SQL 或配置 BI 报表。系统自动识别字段、执行聚合、生成图表并输出文字解读。适合厂长、生产主管、品控经理等管理岗位日常监控经营数据。**与本手册 §12 "AI 对话" 章节的区别**：§12 描述的是系统内置 LLM 助手用于辅助操作（如 AI 创建生产计划、AI 推荐配方），属于**操作辅助**；§0A 是**Excel 上传 + 数据集分析**，专注于数据洞察 — 一个执行命令，一个解读数字，不重叠。让前线班组长也能独立获取数据洞察，无需依赖 IT 配置。

### 4.2 §0A.1 进入路径（2 条）

1. **菜单路径**：登录 Web Admin → 左侧"智能 BI"分组 → "智能数据分析"
2. **Dashboard 快捷入口**：首页"快捷分析"卡片 → 点击预设场景按钮（如"今日产量"、"本周良品率"）

### 4.3 §0A.2 主界面 layout（4 区块）

| 区块 | 位置 | 功能 |
|------|------|------|
| 顶部工具栏 | 页面顶部 | 数据集选择、上传 Excel、历史对话 |
| 中央查询输入区 | 页面中部 | 自然语言输入框 + 快捷问题按钮 |
| 结果展示区 | 输入区下方 | AI 回答 + 图表（柱/线/饼） |
| 左侧历史面板 | 页面左侧 | 历史问答列表（按时间倒序） |

### 4.4 §0A.3 常用操作步骤（8 步）

仿餐饮版结构，但替换工厂示例：
- 选择/上传数据集（生产报工、质检、设备运行数据）
- 输入问题（"本周哪条产线 OEE 最低？"）
- 查看回答 + 图表
- 追问（"该产线主要停机原因是什么？"）
- 导出结果（PNG / CSV）

### 4.5 §0A.4 常见错误处理（5+ 行）

仿餐饮版结构，针对工厂场景：
- "字段未识别" → 检查 Excel 列名是否清晰（"产线号"而非"line"）
- "回答与预期不符" → 加具体维度（产线号、SKU、班次）
- "数据集为空" → 联系系统管理员开通权限

### 4.6 §0A.5 关键指标速查（厂长视角）

| 指标 | 计算口径 | 健康区间（食品深加工） | 红线 |
|------|----------|----------|------|
| OEE (设备综合效率) | 可用率 × 性能率 × 良品率 | 食品加工 55-75%（连续生产线偏低于离散制造） | <50% 红线 |
| 良品率 | 合格件 / 投产件 | 95-99% | <93% 红线 |
| 工序合格率 | 合格批次 / 总批次 | 98%+ | <95% 红线 |
| 损耗率 | (实际用料 - 标准用料) / 标准用料 | <5% | >10% 红线 |
| 设备利用率 | 实际运行时间 / 计划时间 | 75-90% | <60% 红线 |
| **准时交付率 (OTIF)** | 准时全量交付订单数 / 总订单数 | 95%+ | <90% 红线（直接影响客户关系） |
| 人均产值 | 月营收 / 在线工人数 | 食品加工 ¥3-8 万/人/月（视品类） | 季度环比下降 >5% 警示 |

### 4.7 §0A.6 业务判断框架

3 个常见诊断场景：
- **OEE 突然下滑** → 拆 可用率 / 性能率 / 良品率 → 定位是停机、慢车、还是质量问题
- **损耗率超标** → 看 SKU 维度 → 查 BOM 配方 vs 实际领料 → 转 §5 / §10
- **某产线良品率低** → 看工序维度 → 查质检记录 → 转 §6F / §15

### 4.8 §0A.7 跨章节联动

5 行：链到 §13 SmartBI / §11 报表 / §14 财务 / §10 报工 / §6F 质检

### 4.9 §0A.8 常见决策场景

2 个：
- **场景 A**: 月度产值同比下滑
- **场景 B**: 客户审厂前的数据自查

### 4.10 §0A FAQ（5 个 Q&A）

- Q: AI Query 和 SmartBI 区别是什么？
- Q: 上传的数据集多久过期？
- Q: AI 答错了怎么办？
- Q: 能问跨数据集的复合问题吗？
- Q: 移动端能用吗？

---

## 5. §0B 财务 PBI 看板章节内容设计

### 5.1 概述（200-300 字）

财务 PBI 看板是白垩纪 SaaS 的**标准财务可视化解读入口**，自动从生产报工、采购入库、销售出货、人工工资等系统数据汇总成月度损益、成本结构、应收应付、现金流四大视图。**与本手册 §14 "财务管理" 章节的区别**：§14 是**财务操作模块**（开发票、记凭证、做付款、对账），属于会计文员日常操作；§0B 是**财务数据解读层**，看板自动按月聚合并展示趋势，给厂长和老板看不动手 — 一个录入数据，一个解读数字，互补不重叠。无需会计手工整理，看板自动按月聚合，支持多工厂切换、按 SKU/客户/产线下钻，让厂长和财务文员快速识别成本异常、回款风险和利润趋势。

### 5.2 §0B.1 进入路径（1 条）

菜单路径：Web Admin → "财务管理" → "财务 PBI"

### 5.3 §0B.2 主界面 layout（5 区块）

- 顶部时间筛选器（月/季/年）
- 4 大卡片：营收 / 成本 / 利润 / 现金流
- 中部图表：损益瀑布图、成本桑基图
- 左侧导航：损益表 / 应收账龄 / 应付清单 / 成本分析
- 右侧 AI 洞察面板（自动生成本期重点提示）

### 5.4 §0B.3 常用操作步骤（5 步）

- 选时间范围
- 选工厂
- 看 4 大卡片汇总
- 下钻图表（点柱状图某月跳到该月明细）
- 导出 PDF / Excel

### 5.5 §0B.4 常见错误处理（5 行）

### 5.6 §0B.5 关键指标速查

| 指标 | 计算 | 健康区间 |
|------|------|----------|
| 毛利率 | (营收 - 直接成本) / 营收 | **品类差异大**：初加工（分切/屠宰）8-15%；中等加工 20-28%；深加工（预制菜/调味品）30-45% |
| 营业利润率 | 营业利润 / 营收 | 食品加工综合 5-12% |
| 应收账龄 | 30/60/90/90+ 天分布 | 90+ <10% 健康，>20% 警示 |
| 现金流 | 经营 / 投资 / 筹资 三大类 | 经营现金流为正、覆盖投资 + 筹资 |
| 单位成本 | 总成本 / 产量 | 工厂自定（按 SKU 跟踪环比） |
| **回款周转 DSO** | 应收账款余额 × 365 / 年营收 | 30-60 天健康，>90 天警示 |

### 5.7 §0B.6 业务判断框架

3 个场景：毛利率下滑 / 应收恶化 / 现金流转负

### 5.8 §0B.7 跨章节联动

链到 §14 财务 / §13 SmartBI / §11 报表 / §0A AI Query

### 5.9 §0B.8 常见决策场景

2 个：月末关账核对 / 季度董事会汇报准备

### 5.10 §0B FAQ（5 个）

---

## 6. KB 入库流程

### 6.1 入库脚本

`backend/python/food_kb/services/manual_ingester.py` 已支持 factory manual：
```python
{
    "path": "docs/plans/factory-operation-manual.html",
    "source": "factory-operation-manual.html",
    "subcategory": "factory",
}
```

### 6.2 触发命令

```bash
# 服务器上
ssh root@47.100.235.168
cd /www/wwwroot/cretas/code/backend/python
source venv38/bin/activate
python -c "
import asyncio
from food_kb.services.manual_ingester import ingest_all_manuals
asyncio.run(ingest_all_manuals())
"
```

### 6.3 验证

```sql
SELECT subcategory, COUNT(*) FROM food_knowledge_documents
WHERE source = 'factory-operation-manual.html'
GROUP BY subcategory;
```

期望：subcategory='factory' chunks 增加约 **+22~28**（§0A ~11-14 chunks + §0B ~11-14 chunks，参考餐饮版 §1/§2 实际数量）。total 从 287 → 309~315。

---

## 7. 部署流程

1. 编辑 `docs/plans/factory-operation-manual.html`（本地）
2. 同步到 `web-admin/public/factory-operation-manual.html`（aiassist + web-admin 都读这个）
3. scp 到 139 服务器：
   ```bash
   scp web-admin/public/factory-operation-manual.html root@139.196.165.140:/www/wwwroot/web-admin/factory-operation-manual.html
   ```
4. 重新入库（前述 6.2）
5. Smoke test：
   ```bash
   echo '{"question":"AI Query 怎么用","category":"factory"}' | \
     curl -s --data-binary @- -H "Content-Type: application/json" -X POST \
     https://aiassist.cretaceousfuture.com/api/food-kb/manual-chat --max-time 60 | \
     python3 -c 'import json,sys;d=json.load(sys.stdin);print(d.get("answer","")[:300])'
   ```

期望返回：包含"智能数据分析"、"OEE"、"良品率"等工厂相关 KPI 的答案。

---

## 8. 验收标准

- [ ] §0A + §0B 两章在 HTML 中渲染正确（h1/h2 层级、表格、tip/warn 样式）
- [ ] TOC 更新含 §0A / §0B
- [ ] last-verified meta 含 §0A / §0B（如果决定也加 ch_intro 章节级 meta）
- [ ] KB 重新入库后 factory chunks 增加 ≥ 20（保守阈值，避免分块策略变化误报）
- [ ] aiassist 工厂版问 "AI Query 怎么用" 返回新写的 §0A 内容（不是引用 §12 旧 AI 章节）
- [ ] aiassist 工厂版问 "财务看板看什么" 返回 §0B 内容
- [ ] 餐饮版问相同问题不受影响（仍返回 restaurant chunks）

---

## 9. 风险与回滚

**风险 1**：§0A 内容跟现有 §12 (AI 对话) chunks 在 RAG 检索中竞争
- 缓解：§0A 概述明确写"§12 是操作辅助，§0A 是数据分析"，给 RAG 增加区分语义；smoke test Test A 用"AI Query 怎么用"专门验证 §0A 优先

**风险 2**：§0B 内容跟现有 §14 (财务管理) chunks 在 RAG 检索中竞争
- 缓解：§0B 概述明确写"§14 是财务操作，§0B 是数据解读"；smoke test Test B 用"财务看板看什么"专门验证 §0B 优先

**风险 3**：重新入库失败 / chunks 数量异常 / atomic swap 出错
- 缓解：`manual_ingester.py` 已支持 atomic swap（先 .NEW source 写入 → 全部成功后事务 DELETE 旧 + RENAME），失败自动回滚。先在 prod 跑一次 dry-run 确认 chunks 数量在 22-28 区间再真入库

**风险 4**：HTML 部署期间用户访问到不一致版本
- 缓解：scp 是原子的（先写临时文件再 mv），不会出现半文件状态。即使旧 HTML 还在浏览器缓存，刷新后会拿新版（nginx 已 set `Cache-Control: no-cache, no-store, must-revalidate`）

**风险 5**：Tier 1 章节模板不够工厂化（直接复制餐饮版导致工厂用户违和）
- 缓解：例子全用工厂场景（OEE/产线/SKU/批次），不是餐厅场景（翻台/客单价）；指标区间用食品加工实际行业数据（OEE 55-75% 而非通用 70-85%）

回滚：
- 文件：`git revert <commit-hash>` 恢复旧版 HTML
- 入库：`manual_ingester.py` 用 atomic swap 模式（写 `.NEW` source → 验证 → 事务 DELETE 旧 + RENAME `.NEW` 为 canonical）。所以**回滚就是用旧版 HTML 重跑 ingester**，让它再做一次 atomic swap。**不要**手工写 DELETE SQL（会按 source 删光所有 287+ chunks，没办法只删新增的）
- 部署：scp 旧版 HTML 回 139（路径 `/www/wwwroot/web-admin/factory-operation-manual.html`）

---

## 10. 后续 Phase（不在本次 spec 范围）

| Phase | 工作量 | 内容 |
|-------|--------|------|
| Phase 1a | 1-2 sessions | 选 §6 SKU组装 / §7 生产计划 / §10 报工 改写为产品手册风格 Tier 1 |
| Phase 1b | 2 sessions | 其余 17 章加"概述+入门+FAQ"模板 + 5 培训路径 + 24 决策合成 |
| Phase 2 | 1 session | CI drift防腐扩展到 factory manual（meta freshness + anchor consistency） |

---

## 11. Open Questions

实施前需用户确认（实施时遇到再问也行）：

1. **§0B 损益瀑布图 / 成本桑基图是否已 ship**？spec §5.3 layout 描述含这两个图，但需要核对 web-admin 财务 PBI 实际 UI。如果还在规划中而不在产品里，应改写为"瀑布图（规划中）/ 桑基图（规划中）"避免误导。
2. **§0A FAQ 数据集保留期限**：餐饮版 §1 提"历史问答最近 50 条"，工厂版应该保持一致还是分开？建议保持 50 条一致。
3. **章节级 last-verified meta 是否要给 §0A / §0B**？餐饮版 Phase 2 CI 监控 §1 / §2 / §11 三个 Tier 1 章节，工厂版同等的"Tier 1 关键章节"应该是 §0A / §0B / §6 / §7 / §10？还是只 §0A / §0B？建议先只 §0A / §0B（简单），其他章节 Phase 1a 时再加。
