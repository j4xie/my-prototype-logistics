<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.joolun.mall.mapper.NotificationTemplateMapper">
    <resultMap id="templateMap" type="com.joolun.mall.entity.NotificationTemplate">
        <id property="id" column="id"/>
        <result property="templateCode" column="template_code"/>
        <result property="templateName" column="template_name"/>
        <result property="templateType" column="template_type"/>
        <result property="category" column="category"/>
        <result property="titleTemplate" column="title_template"/>
        <result property="contentTemplate" column="content_template"/>
        <result property="smsTemplate" column="sms_template"/>
        <result property="smsTemplateId" column="sms_template_id"/>
        <result property="variables" column="variables"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByCode" resultMap="templateMap">
        SELECT * FROM notification_template
        WHERE template_code = #{code}
          AND status = 1
    </select>

    <select id="selectByCategory" resultMap="templateMap">
        SELECT * FROM notification_template
        WHERE category = #{category}
          AND status = 1
        ORDER BY id ASC
    </select>

    <select id="selectActiveTemplates" resultMap="templateMap">
        SELECT * FROM notification_template
        WHERE status = 1
        ORDER BY category, id
    </select>
</mapper>
